
# Line diff engine on similar

`WORK-p7e70a5mwf` · created 2026-09-14 · slug `line-diff-engine`
state `working` · maturity `DREAMED` · kind `preplanned` · source `ad-hoc` · discipline `coding` · variety `greenfield`
importance `high` · urgency `med` · priority `high` · epic `EPIC-77znwqj94w`

> **JSONL is authoritative.** This document is auto-generated from `WORK-p7e70a5mwf`'s `.workUnit.jsonl`. Do not hand-edit; changes will be overwritten on the next write.



## Status Summary

Implementation complete. The `dioxus-diff-viewer` crate lives at `rust-dioxus/` and carries the engine: `line_diff` takes two texts and LineDiffOptions and returns a LineDiff of paired entries plus the positions holding a change, with no presentation. All 19 tests pass, clippy is clean, and rustfmt is applied. Both worker to-do advice records are addressed: WORK:advice-nj1vrndsnf (Reference porting hazards) and WORK:advice-zaj371p040 (Test strategy for the engine). Next is post-work review by the Architect, the Code Whisperer, and the Rust Tester; the in-scope requirements stay where they are until that review's final stage advances them.

## Problem / Context

- The diff viewer needs per-line diff information to render: old and new lines paired, each marked unchanged, removed, added, or modified, numbered from the line offset, with inline changes on modified lines.
- The component also needs to know where changes sit, so it can fold the unchanged lines around them.
- The reference is `src/compute-lines.ts`; alignment follows `similar`, so the TypeScript tests are a source of cases, not expected output.

## Plan / Guidance

- Every in-scope requirement binds acceptance alongside the criteria.
- Defaults generally follow react-diff-viewer, per stakeholder steering: character comparison and inline changes are on unless the consumer chooses otherwise.
- The engine carries no presentation; views, folding, and styling belong to the component and theming work units.
- Risk signal carried from the epic: implementation-complexity.
- The crate is `dioxus-diff-viewer`; its public names use the requirement terms (unchanged, removed, added, modified, inline changes, compare method, line offset, line ending change), not the reference's `DEFAULT`, `diffLines`, or `disableWordDiff`.



## Required Skills

- [`skillz/coder/coder.SKILL.md`](../skillz/coder/coder.SKILL.md) — roles: `implementer`

- [`skillz/rust-coder/rust-coder.SKILL.md`](../skillz/rust-coder/rust-coder.SKILL.md) — roles: `implementer`

- [`skillz/rust-observability/rust-observability.SKILL.md`](../skillz/rust-observability/rust-observability.SKILL.md) — roles: `implementer`

- [`skillz/logging/logging-rs.SKILL.md`](../skillz/logging/logging-rs.SKILL.md) — roles: `implementer`

- [`skillz/rust-tester/rust-tester.SKILL.md`](../skillz/rust-tester/rust-tester.SKILL.md) — roles: `domain-tester`, `prework-advice`, `postwork-audit`

- [`skillz/code-whisperer/code-whisperer.SKILL.md`](../skillz/code-whisperer/code-whisperer.SKILL.md) — roles: `prework-advice`, `postwork-audit`

- [`skillz/architect/architect.SKILL.md`](../skillz/architect/architect.SKILL.md) — roles: `reviewer`, `prework-advice`, `postwork-audit`




## References

**ARCH**
- `ARCH-jf3s5npp9s` — line diff engine _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-exgfx6rwdt` — engine output _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-kjjykjtt5r` — paired line entry _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-n7wmjnmt65` — engine options _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-atczcqvdsz` — line diff hand-off _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH:dcisn-wma0h4mndz` — engine split decision _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`


**REQT**
- `REQT-rvcg21axa8` — compare methods _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-wqffp3d26e` — line changes _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-smd01rma2q` — line numbering _(role: scope)_ · `diff-viewer.reqts.jsonl`


**OTHER**
- `EPIC-77znwqj94w` — port epic _(role: context)_ · `rust-dioxus-port.workEpic.jsonl`

- `MISSION.md` — port mission _(role: context)_ · `MISSION.md`





## Focus Files

- `rust-dioxus/` — The crate the port lives in; the engine is its first code.

- `src/compute-lines.ts` — Reference line diff behavior being ported.

- `test/compute-lines-test.ts` — Reference cases to carry over where similar's alignment agrees.

- `rust-dioxus/tests/line_diff_engine.rs` — Rust Tester's pre-work test drafts, one named test per in-scope child requirement, plus the reference-case table and two property tests.






## Activities


### Establish the crate
<a id="work:activity-0ft8wqe8m7"></a>
`WORK:activity-0ft8wqe8m7`

Establish the `rust-dioxus/` crate the port lives in.





**Advice**
- _Code Whisperer_ · state `addressed` · type `design` · priority `med` — The work subject's names are unsettled: the crate has no name, and the reference's public vocabulary (`DiffType.DEFAULT` for unchanged lines, `diffLines` for changed row indexes, `disableWordDiff` for inline changes) differs from the requirement terms (unchanged, inline changes, compare method, line offset), with no glossary covering this project. `WORK:advice-98x4z0fcpf`
  - **evidence**: `src/compute-lines.ts:5-9`, `:33-36`, and `:145` carry the reference names; `.project/requirements/diff-viewer.reqts.md:61-70` and `:86` carry the requirement terms. MISSION.md and the port epic name only the directory `rust-dioxus/`. A search for `*.glossary.jsonl` outside `skillz/` and `node_modules/` returned no matches at the capture-time check.
  - **Crate name and public vocabulary** · decision `judgement-needed` · execution `agent-trivial`
    - _The crate is unnamed, and reference names such as `DEFAULT` and `diffLines` differ from the requirement terms._
    - **Context**:
      Re-orienting: names for the new crate.
      Catch-up:
      - Where: the Establish the crate activity.
      - Purpose: the names apps import and read.
      - Situation: no crate name; the reference vocabulary differs from requirements.
      - Goal: public names drawn from requirement terms.
    - **Key points**:
      - No glossary exists in this project.
      - Requirement terms: unchanged, removed, added, modified, inline changes, compare method, line offset.
      - Differing reference names: `DiffType.DEFAULT`, `diffLines`, `disableWordDiff`.
    - **Resolution points**:
      - `requirement-vocabulary`: public names follow the requirement terms, and the crate is named `dioxus-diff-viewer`.
    - **Substance**: Maintainability: public names are costly to change once apps depend on them.
    - **Actionability**: Workable.
    - **Specificity** `under-design`: Crate identity and the source of public names are unstated.
  - **Response**: WORK-p7e70a5mwf plan_guidance now names the crate `dioxus-diff-viewer` and draws its public names from the requirement terms rather than the reference's `DEFAULT`, `diffLines`, or `disableWordDiff`.
- The stakeholder accepted `dioxus-diff-viewer` as the crate name and the requirement terms as the source of public names.
- The finding was presented ahead of the candidate architecture finding because the architecture records and the Rust Tester test drafts both carry the crate name.



### Paired line information
<a id="work:activity-bd3teyamp7"></a>
`WORK:activity-bd3teyamp7`

Compute paired line information from two texts, per the in-scope line change and line number requirements.





**Advice**
- _Architect_ · state `addressed` · type `design` · priority `high` — REQT-hmsfnfe5wc (Line marking) defers alignment to `similar`'s line diff, but `similar` keeps line terminators inside line tokens, so a last line without a trailing newline never matches the same text followed by one; the reference line addition and line deletion cases would show their unchanged `test` line as modified. `WORK:advice-6prmspeaht`
  - **evidence**: `similar-3.2.0/src/text/abstraction.rs:164-190` (`tokenize_lines`) includes `\n` or `\r\n` in each line token. A scratch probe of `TextDiff::from_lines("test", "test\n    newLine")` returned Delete `test`, Insert `test\n`, Insert `    newLine`; `"a\nb"` against `"a\nb\nc"` returned Equal `a\n`, Delete `b`, Insert `b\n`, Insert `c`. The reference expects `test` unchanged at `test/compute-lines-test.ts:34-65` and `:67-98`. REQT-9tze98pt6g (Trailing whitespace) trims each text's end, as `src/compute-lines.ts:149-151` does, which guarantees both last lines lack terminators. `TextDiffConfig::newline_terminated` affects only unified diff generation (`similar-3.2.0/src/text/mod.rs:407-411`, `:436-439`).
  - **Line terminators break line matching** · decision `judgement-needed` · execution `agent-trivial`
    - _`similar` keeps newlines inside line tokens, so an unterminated last line never matches the same line followed by a newline._
    - **Context**:
      Re-orienting: line marking defers alignment to `similar`'s line diff.
      Catch-up:
      - Where: REQT-hmsfnfe5wc (Line marking) in the diff viewer requirements.
      - Purpose: decides which lines read as unchanged.
      - Situation: trimmed texts end without a newline.
      - Goal: equal line text reads unchanged.
    - **Key points**:
      - `similar` line tokens include `\n` or `\r\n`.
      - Trimming each text's end leaves its last line unterminated.
      - The reference line addition and deletion cases would show `test` as modified.
      - The requirement's words hold while its intent fails.
    - **Resolution points**:
      - `compare-without-terminators`: amend REQT-hmsfnfe5wc so lines are compared by their text without line terminators.
      - The same amendment settles whether `\r\n` and `\n` endings count as a difference.
    - **Substance**: Requirements coverage and contract correctness: the literal requirement yields spurious modified lines on common inputs.
    - **Actionability**: Workable: one statement amendment.
    - **Specificity** `under-design`: The requirement hands alignment wholly to `similar` without stating how line identity is compared.
  - **Response**: REQT-hmsfnfe5wc (Line marking) now compares lines by their text without line terminators, so `\r\n` and `\n` endings align as unchanged. Differing endings stay visible through REQT-rtwn1qresp (Line ending changes), REQT-4zyjfjrhd3 (Line ending chips), and REQT-8y5fp2aarz (Line ending chip color).
- The stakeholder accepted comparing lines without their terminators, so lines that differ only in `\r\n` versus `\n` read as unchanged for alignment.
- The stakeholder added that line ending changes must still show: each side's terminator as a compact chip reading `\n` or `\r\n`, in a theme color that contrasts with the line, adding a theme color when the palette has none.
- The Rust Tester noted a case pairing the same line under `\r\n` and `\n` endings for the drafted tests.



### Inline changes
<a id="work:activity-b1f86p3dwn"></a>
`WORK:activity-b1f86p3dwn`

Compute inline changes for modified lines under each compare method.






### Changed-line positions
<a id="work:activity-6aywz75j6h"></a>
`WORK:activity-6aywz75j6h`

Provide the changed-line positions the component needs for folding.





**Advice**
- _Work Planner_ · state `dismissed` · type `scope-refinement` · priority `low` — Activity development:
- Changed-line positions (WORK:activity-6aywz75j6h): positions identify the entries of the paired line information that hold a change, so the component can fold in either view; a narrower reading as one side's line numbers misses added-only and removed-only entries.

Set aside as unanchored:
- Whether apps may call the engine directly for line information without the component; nothing in the mission, epic, or requirements asks for it. `WORK:advice-90hfff28sa`
  - **evidence**: `src/compute-lines.ts:33-36` and `:184-187` return changed positions as indexes into `lineInformation`. `.project/requirements/diff-viewer.reqts.md:91` folds unchanged lines by distance from every change, which the component computes over paired entries.
  - **Develop the changed-line positions activity** · decision `obvious-essential` · execution `agent-trivial`
    - _Positions should identify the paired line information entries holding a change, not one side's line numbers._
    - **Context**:
      Re-orienting: the changed-line positions activity.
      Catch-up:
      - Where: WORK:activity-6aywz75j6h (Changed-line positions).
      - Purpose: tell the component where changes sit.
      - Situation: positions could be read as one side's line numbers.
      - Goal: positions usable for folding in either view.
    - **Key points**:
      - One side's line numbers miss added-only and removed-only entries.
      - The reference returns indexes into its line information.
    - **Resolution points**:
      - `entry-positions`: the activity reads "Provide the positions of the paired line information entries that hold a change, which the component needs for folding."
    - **Substance**: Contract correctness: a narrow reading breaks folding in the component.
    - **Actionability**: Workable.
    - **Specificity** `under-design`: The activity names positions without saying what they index.
  - **Response**: No change to WORK:activity-6aywz75j6h (Changed-line positions). ARCH-exgfx6rwdt (LineDiff) and ARCH-jf3s5npp9s (LineDiffEngine) already state that positions index the entries holding a change, so the expectation is closed in the records and the activity text stands as consented.
- The stakeholder judged the advice a minor-detail finding that should never have been filed, and asked whether its substance had been checked before presentation.
- The architecture written an hour after this filing already carries the contract it asks for, so the expectation was closed in the records and the remaining difference was activity wording, which pre-work leaves as consented (REQT-dbh4p81act).
- The review should have closed this candidate in its self-resolution pass rather than spending a presentation turn on it.



### Requirement tests
<a id="work:activity-xk81stsy2t"></a>
`WORK:activity-xk81stsy2t`

Cover every in-scope requirement with tests, drawing cases from `test/compute-lines-test.ts` where `similar`'s alignment agrees.





**Advice**
- _Rust Tester_ · state `addressed` · type `recommendation` · priority `high` — No test strategy is recorded, and greenfield work-ready requires one. Proposed strategy:
- Output-based tests against the engine's public entry point, with no Dioxus runtime involved.
- One focused test per in-scope child requirement, named as a plain-English behavior carrying the REQT id.
- Reference cases from `test/compute-lines-test.ts` as a table, each named by its reference case; a case that differs asserts the engine's own output and states its cause beside it.
- Property tests: each side's line texts rejoin to that side's trimmed input, and each side's line numbers run contiguously from the offset plus one.

Sketch:
```
rows = compute("test", "test\n    newLine", defaults)
assert rows == [unchanged(old 1, new 1, "test"), added(new 2, "    newLine")]   // REQT-hmsfnfe5wc
rows = compute("Hello World", "My Updated Name\nAlso this info", compare = words)
assert rows[0] is modified and its old-side tokens rejoin to "Hello World"      // REQT-xzc8n354h1
property(old, new, offset): old-side texts joined by newline == old.trim_end()
``` `WORK:advice-zaj371p040`
  - **evidence**: `coder/coder.codingDiscipline.reqts.md:129` lists an articulated test strategy as work-ready condition (d). The work unit carried no work_advice before this pass. Reference cases at `test/compute-lines-test.ts:5-412` cover trailing whitespace, addition, deletion, modification, inline changes, default and word compare methods, disabled inline changes, and line offset. `rust-tester/rust-tester.SKILL.md:105-107` ranks output-based testing first where the code under test is pure.
  - **Test strategy for the engine** · decision `advisory` · execution `not-needed`
    - _No test strategy is recorded; this proposes output-based, per-requirement, reference-table, and property tests for the worker._
    - **Context**:
      Re-orienting: engine test strategy.
      Catch-up:
      - Where: the Requirement tests activity.
      - Purpose: show every in-scope requirement holds.
      - Situation: greenfield work-ready needs an articulated strategy.
      - Goal: a strategy the worker follows and post-work can check.
    - **Key points**:
      - The engine is pure, so output-based tests fit.
      - Each child requirement gets a named test.
      - Reference cases run as a table, with causes stated for differences.
      - Property tests guard line numbering and text reconstruction.
    - **Resolution points**:
      - `adopt-strategy`: keep this advice open as the worker's test plan.
    - **Substance**: Test coverage: gives post-work review a checkable coverage map.
    - **Actionability**: Workable.
    - **Specificity** `under-design`: The test approach was unstated.
  - **Response**: The strategy was followed as written, in rust-dioxus/tests/line_diff_engine.rs. Output-based tests call the public entry point with no Dioxus runtime involved; one focused test per in-scope child requirement carries that REQT id in its doc comment; the reference cases run as a table named by each reference case, with two further tests recording the cases that differ and their causes per WORK:crit-rdjdbfyxnt; and both property tests hold, for line-text reconstruction and for contiguous numbering from the offset. The sketch's own assertions are among the passing tests.






## Acceptance Criteria

- [ ] The component work unit can build split and inline views, and folding, from the engine's output alone, without diffing again. · maturity `DREAMED`

- [ ] Each reference case in `test/compute-lines-test.ts` is carried over, or recorded as differing with its cause: `similar`'s alignment or a defect in the reference. · maturity `DREAMED`

- [ ] The TypeScript package at the repo root is unchanged. · maturity `DREAMED`




## Sign-Offs

### Pre-Work

- **Architect** — state `approved-with-notes` · 2026-09-15
  - Scope holds to the engine: the crate, its options, output, and entry, with views, folding, and styling excluded. Candidate architecture is recorded in dioxus-diff-viewer.arch.jsonl and binds the worker while staying DREAMED.

- **Code Whisperer** — state `approved-with-notes` · 2026-09-15
  - Public names follow the requirement terms. Notes for the worker: WORK:advice-nj1vrndsnf (Reference porting hazards) stays open as the hazard list, with the changed-row overcount, the one-space placeholder, and `similar`'s kept terminators.

- **Rust Tester** — state `approved-with-notes` · 2026-09-15
  - Test strategy articulated and drafted: rust-dioxus/tests/line_diff_engine.rs covers every in-scope child requirement by name, plus the reference-case table with its causes and two property tests. WORK:advice-zaj371p040 (Test strategy for the engine) stays open as the worker's plan.



### Post-Work

- **Architect** — state `findings-review` · audit `AUDIT:session-a6vrbc16ne`

- **Code Whisperer** — state `findings-review` · audit `AUDIT:session-a6vrbc16ne`

- **Rust Tester** — state `findings-review` · audit `AUDIT:session-a6vrbc16ne`





## Advice (work-unit scope)

- _Work Planner_ · state `addressed` · type `design` · priority `high` — Greenfield work-ready needs a person's consent to the in-scope requirements (REQT-2mge0tjtxq, Greenfield Work-Ready); the compare methods, line changes, and line numbers groups and every child under them carry maturity DREAMED, and no record shows that consent. `WORK:advice-gy0rr55bbd`
  - **evidence**: `.project/requirements/diff-viewer.reqts.md:59-70` and `:84-87` show DREAMED on REQT-rvcg21axa8 (Compare methods), REQT-wqffp3d26e (Line changes), REQT-yxkc61eq90 (Line numbers), and each of their children. `coder/coder.codingDiscipline.reqts.md:129` lists a person's consent as work-ready condition (a). The work unit's status_summary names that consent as still needed. `.project` commits 35643f9, 689612a, and 9a0699b shaped these requirements without advancing maturity.
  - **In-scope requirements await consent** · decision `obvious-essential` · execution `agent-trivial`
    - _Greenfield work-ready needs a person's consent to the compare method, line change, and line number requirements, all still DREAMED._
    - **Context**:
      Re-orienting: requirement consent gate for the engine.
      Catch-up:
      - Where: the in-scope requirement groups of this work unit.
      - Purpose: settle what the worker must deliver.
      - Situation: every group and child sits at DREAMED.
      - Goal: consented requirements before work starts.
    - **Key points**:
      - Greenfield work-ready condition (a) is a person's consent.
      - No record shows that consent; maturity stays DREAMED.
      - The line terminator amendment changes one of these requirements first.
    - **Resolution points**:
      - `consent-then-mark`: after the line terminator amendment, present the in-scope requirements, and on consent set their maturity to `consented`.
    - **Substance**: Requirements coverage: the gate that makes in-scope requirements binding is unmet.
    - **Actionability**: Workable once the line terminator finding resolves.
    - **Specificity** `calibrated`: Requirement text is specific; only its consent state is open.
  - **Response**: The stakeholder consented on 2026-09-15 to the engine's in-scope requirements as written: REQT-rvcg21axa8 (Compare methods) with its three children, REQT-wqffp3d26e (Line changes) with its five children, and REQT-smd01rma2q (Independent numbering). By stakeholder steering their maturity stays DREAMED; this record carries the consent.
  - depends on `WORK:advice-6prmspeaht`: Consent follows the line marking amendment
- The stakeholder found the eleven in-scope requirements fine as written and directed that their maturity stay DREAMED rather than move to `consented`.
- Asked which instructions pointed at consent and maturity, the review traced consent to REQT-2mge0tjtxq (Greenfield Work-Ready) condition (a), and the maturity move to an inference joining the pre-work rule that unrecorded expectations stay unclosed with the requirements consumer's check for `consented` maturity; the Work Planner's unsteered-draft rule and the maturity schema point the other way.
- _Architect_ · state `addressed` · type `design` · priority `high` — No candidate architecture exists for the crate, and `.project/architecture/` is absent, so the placement convention is unset; greenfield work-ready asks for suggested candidate architecture, and WORK:crit-113mtz82xb (engine output alone) has no stated output contract to be checked against. `WORK:advice-gy94sw8qae`
  - **evidence**: `ls .project` at capture time listed only `MISSION.md`, `requirements/`, and `work/`. `coder/coder.codingDiscipline.reqts.md:129` lists suggested candidate architecture as work-ready condition (b). `architect/architect.SKILL.md:344-346` requires offering the placement choice when `.project/architecture/` is missing. The Work Planner report of 2026-09-15 on this unit recorded the same absence. No REQT or ARCH record states what the engine's output holds.
  - **No candidate architecture or placement** · decision `judgement-needed` · execution `agent-nontrivial`
    - _The crate has no suggested architecture, and `.project/architecture/` does not exist to hold one._
    - **Context**:
      Re-orienting: candidate architecture for the new crate.
      Catch-up:
      - Where: `.project/architecture/`, not yet created.
      - Purpose: the structure the worker holds to.
      - Situation: greenfield work-ready asks for suggested architecture.
      - Goal: DREAMED records naming the engine output and its consumer.
    - **Key points**:
      - Placement needs a choice before any record lands.
      - The engine's output is unnamed, so the first acceptance criterion has nothing to check against.
      - The engine is a predictable function of two texts and options.
    - **Resolution points**:
      - `centralize-and-dream`: create `.project/architecture/` and write DREAMED records for the crate, the line diff engine, its line information output, and its hand-off to the component.
      - `co-locate`: a `USE_MAIN_WORKTREE` sentinel with records beside the crate; demoted because requirements already live in `.project/requirements/`.
    - **Substance**: Maintainability and contract correctness: without a stated output the component work unit risks rework.
    - **Actionability**: Workable.
    - **Specificity** `under-design`: The plan splits engine from component but states no structure between them.
  - **Response**: Architecture centralizes in `.project/architecture/`. `dioxus-diff-viewer.arch.jsonl` holds DREAMED records: ARCH-h2qwqte1g6 (dioxus-diff-viewer root), ARCH-jf3s5npp9s (LineDiffEngine), ARCH-m4dkxzw6hh (DiffViewer placeholder), ARCH-wdrt39xvh4 (similar), ARCH-n7wmjnmt65 (LineDiffOptions), ARCH-exgfx6rwdt (LineDiff), ARCH-kjjykjtt5r (PairedLineEntry), ARCH-atczcqvdsz (Line diff hand-off), and ARCH:dcisn-wma0h4mndz (Engine separate from component). The engine unit scopes the engine, its options, output, and entry; the component unit references the placeholder, output, and hand-off as context.
- The stakeholder accepted centralized placement in `.project/architecture/` and the proposed candidate records.
- The Architect judged the engine's architecture well understood, so the records are detailed before work and bind the worker, while staying DREAMED.
- _Rust Tester_ · state `addressed` · type `scope-refinement` · priority `med` — WORK:crit-rdjdbfyxnt (reference cases) allows only carried over or differing because of `similar`'s alignment, but some reference expectations are defects of the reference itself, leaving a worker to port the defect or fail the criterion. `WORK:advice-8aqew66ak8`
  - **evidence**: `test/compute-lines-test.ts:273-276`, `:335-338`, and `:407-410` expect `diffLines` of [0, 2] for two rows. Tracing `src/compute-lines.ts:197-211` with `:251`: the nested call that reads the paired added line increments `counter` before the outer line's own increment, so the second row's index lands at 2, past the last row. The component work unit's report records a separate reference defect at `src/index.tsx:507-515`.
  - **Reference defects need a disposition** · decision `obvious-essential` · execution `agent-trivial`
    - _The reference-case criterion allows only carried-over cases or differences caused by `similar`, but some reference expectations are defects._
    - **Context**:
      Re-orienting: the reference-case acceptance criterion.
      Catch-up:
      - Where: WORK:crit-rdjdbfyxnt (reference cases).
      - Purpose: account for every TypeScript test case.
      - Situation: three cases expect a changed-row index past the last row.
      - Goal: defects recorded as such, never ported.
    - **Key points**:
      - `diffLines` of [0, 2] over two rows comes from a counter overcount.
      - The criterion offers no honest home for such a case.
    - **Resolution points**:
      - `name-the-cause`: reword the criterion to "Each reference case in `test/compute-lines-test.ts` is carried over, or recorded as differing with its cause: `similar`'s alignment or a defect in the reference."
    - **Substance**: Test coverage and contract correctness: keeps a reference defect from being ported.
    - **Actionability**: Workable.
    - **Specificity** `under-design`: The criterion enumerates dispositions and misses one.
  - **Response**: WORK:crit-rdjdbfyxnt (reference cases) now accepts a defect in the reference as a recorded cause beside `similar`'s alignment, so the reference changed-row overcount is recorded as a defect rather than ported.
- The stakeholder accepted the reworded criterion as proposed.
- The review re-traced the overcount before presenting: jsdiff merges consecutive added tokens into one change, so the nested call for the paired added line and the outer removed line each increment the row counter, placing the second row's index at 2.
- The Rust Tester will draft those three reference cases with engine-computed expectations and the defect cause beside each.
- _Code Whisperer_ · state `addressed` · type `recommendation` · priority `med` — Porting line by line from `src/compute-lines.ts` carries hazards the requirements do not repeat: the changed-row counter overcounts after a modified pair; empty removed lines get a one-space placeholder, which is presentation; and `similar` line tokens keep their terminators (`\n`, `\r\n`, or a bare `\r`), so lines compared as `similar` tokenizes them would misalign text that differs only in its ending; REQT-hmsfnfe5wc (Line marking) compares lines without terminators, and REQT-rtwn1qresp (Line ending changes) still needs each side's terminator kept. `WORK:advice-nj1vrndsnf`
  - **evidence**: `src/compute-lines.ts:197-211` with `:251` (counter increments), `:192` (`line || ' '`), and `similar-3.2.0/src/text/abstraction.rs:164-190` (terminators, including a bare `\r`, kept in line tokens). The scratch probe of `TextDiff::from_lines` returned values such as `test\n` and `a\n`. `.project/requirements/diff-viewer.reqts.md:67` and `:71` state line marking without terminators and line ending changes.
  - **Reference porting hazards** · decision `advisory` · execution `not-needed`
    - _A row counter overcount, a one-space placeholder, and `similar`'s kept line terminators trap a line-by-line port._
    - **Context**:
      Re-orienting: hazards in the reference line computation.
      Catch-up:
      - Where: `src/compute-lines.ts`.
      - Purpose: the TypeScript source the engine ports.
      - Situation: its quirks are not requirements.
      - Goal: the engine avoids importing them.
    - **Key points**:
      - The changed-row counter overcounts after a modified pair (`src/compute-lines.ts:197-211`, `:251`).
      - `line || ' '` is display padding (`:192`).
      - `similar` line tokens keep their terminators, so alignment must compare lines without them while line ending changes keep each side's terminator.
    - **Resolution points**:
      - `worker-hazard-list`: keep this advice open as the worker's hazard list.
    - **Substance**: Contract correctness: each hazard produces wrong output if ported.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Hazard context for the worker, not design material.
  - **Response**: All three hazards are avoided, each guarded by a test. The changed-row counter is not ported: WORK:activity-6aywz75j6h (Changed-line positions) reports real entry positions, and the reference's overcount is recorded as a reference defect beside the cases expecting it, per WORK:crit-rdjdbfyxnt. The one-space placeholder is not ported: an empty line keeps its empty text, since padding is presentation ARCH-jf3s5npp9s (LineDiffEngine) excludes. The kept terminators are handled by splitting lines in the crate rather than through `similar`'s line tokenizer, so REQT-hmsfnfe5wc (Line marking) compares line text while REQT-rtwn1qresp (Line ending changes) keeps each side's terminator.
  - depends on `WORK:advice-6prmspeaht`: Terminator hazard shifts with the line marking amendment

- _Architect_ · state `dismissed` — REQT-dqxm8fa7ts (Modified lines) pairs removed lines immediately followed by added lines; `similar` might report insertions before deletions inside one change block, leaving such lines unpaired. `WORK:advice-rete70wt9r`
  - **evidence**: `similar-3.2.0/src/algorithms/replace.rs:5-10` states that text diffing uses the Replace hook to keep a consistent order of inserts and deletes; `:56-70` flushes a held deletion before a held insertion. The scratch probe listed Delete before Insert in every mixed block.
  - **Response**: No change needed: within a change block `similar` reports deletions before insertions, so the pairing rule in REQT-dqxm8fa7ts always sees the removed lines first.

- _Rust Tester_ · state `dismissed` — The compare method and trailing whitespace requirements might diverge from what `similar` provides: its word tokens, its default alignment algorithm, and whole-text trimming. `WORK:advice-6jb5e6y26b`
  - **evidence**: `similar-3.2.0/src/text/abstraction.rs:213-229` splits text into alternating whitespace and non-whitespace runs, matching REQT-xzc8n354h1 (Word comparison). `similar-3.2.0/src/text/mod.rs:367-378` makes Myers the default algorithm, the plain reading of alignment as `similar`'s line diff aligns them. `src/compute-lines.ts:149-151` trims each whole text's end, matching REQT-9tze98pt6g (Trailing whitespace).
  - **Response**: No change needed: each requirement states behavior that `similar`'s defaults or a whole-text trim deliver directly.

- _Work Planner_ · state `addressed` · type `procedural` — Pre-work presence checks over the work unit's records: reference pointers, requirement statuses, scope boundary, sign-offs, and skills. `WORK:advice-3rfpk301yz`
  - **evidence**: Pointer Value Rubric (`schemata-crafter/schemata-crafter.SKILL.md:139-148`): `diff-viewer.reqts.jsonl`, `rust-dioxus-port.workEpic.jsonl`, and `MISSION.md` each resolve to exactly one file, under `.project/`. In-scope requirements read BACKLOG (`.project/requirements/diff-viewer.reqts.md:59-87`), consistent with pre-work, and move to NEXT at pre-work exit. plan_guidance states what is out: views, folding, and styling. Pre-work and post-work sign-offs exist for Architect, Code Whisperer, and Rust Tester. work_skill leaves out the UI group, consistent with the skillz index loading it only for UI work, as the Work Planner report of 2026-09-15 recorded.
  - **Response**: Pass: pointers correct, statuses consistent with the phase, scope boundary stated, sign-offs and skills present.

- _Work Planner_ · state `dismissed` — README `compareMethod` lists seven jsdiff methods while the in-scope requirements define three, and this unit records no omission for the other four. `WORK:advice-rpqenvbxcb`
  - **evidence**: `README.md:151-165` lists the seven methods; `.project/requirements/diff-viewer.reqts.md:61-63` defines character, word, and line comparison. `.project` commit 689612a removed a MUST NOT on sentence, CSS, and trimmed-line comparison as redundant negative scope. WORK-6ds59btb76 (Diff viewer Dioxus component) carries the criterion mapping README props to Rust equivalents or recorded omissions.
  - **Response**: No change in this unit: the offered methods are its positive scope, and README prop mapping belongs to the component work unit.

- _Code Whisperer_ · state `dismissed` — The engine's code might take on Dioxus or presentation types, tying its tests to a renderer. `WORK:advice-rt2xn7dcd9`
  - **evidence**: plan_guidance on WORK-p7e70a5mwf (Line diff engine on similar) states the engine carries no presentation, and WORK:crit-113mtz82xb (engine output alone) makes the engine's output the component's only input.
  - **Response**: No change needed: plan guidance and the first acceptance criterion already keep the engine's output free of presentation.

- _Work Planner_ · state `addressed` · type `scope-separation` · priority `high` — The engine's scope reference WORK:ref-gsjqm0y082 (line numbers) points at the group REQT-yxkc61eq90 (Line numbers), which takes in REQT-m9r3k5b1ge (Hidden line numbers), a display choice the component work unit already scopes; the engine becomes accountable for a presentation behavior. `WORK:advice-2sw6j0rpmm`
  - **evidence**: `.project/requirements/diff-viewer.reqts.md:86-89` lists REQT-smd01rma2q (Independent numbering) and REQT-m9r3k5b1ge (Hidden line numbers) under REQT-yxkc61eq90. `.project/work/planning/20260915.diff-viewer-component.workUnit.md:63` scopes REQT-m9r3k5b1ge to WORK-6ds59btb76 (Diff viewer Dioxus component). The engine's plan_guidance states the engine carries no presentation.
  - **Engine scope takes in hidden line numbers** · decision `obvious-essential` · execution `agent-trivial`
    - _The engine's line numbers reference covers the whole group, including hidden line numbers, which is a component display choice._
    - **Context**:
      Re-orienting: the engine's line number scope reference.
      Catch-up:
      - Where: WORK:ref-gsjqm0y082, pointing at REQT-yxkc61eq90 (Line numbers).
      - Purpose: names what the engine must deliver.
      - Situation: the group holds numbering and hiding.
      - Goal: the engine owns numbering, the component owns hiding.
    - **Key points**:
      - A group reference binds every child under it.
      - Hiding line numbers is display, which the engine's plan excludes.
      - The component work unit already scopes REQT-m9r3k5b1ge.
    - **Resolution points**:
      - `narrow-to-numbering`: point WORK:ref-gsjqm0y082 at REQT-smd01rma2q (Independent numbering), labeled `line numbering`.
    - **Substance**: Requirements coverage: the engine would be accountable for behavior outside its plan.
    - **Actionability**: Workable: one reference edit.
    - **Specificity** `under-design`: Scope references were drawn at group level where one child belongs to another unit.
  - **Response**: WORK:ref-gsjqm0y082 now points at REQT-smd01rma2q (Independent numbering), labeled line numbering; REQT-m9r3k5b1ge (Hidden line numbers) stays with the component work unit alone.




## Audit Trail

### post-work

- **session** `AUDIT:session-a6vrbc16ne` — Line diff engine on similar (WORK-p7e70a5mwf): the dioxus-diff-viewer crate's engine, its options, output, and entry, against the work unit's plan, criteria, and in-scope requirements · audit `post-work` · 2026-09-15 · status `in-progress` · theme `Mismatches between what the plan, the requirements, and the architecture expect and what the crate delivers` · expertise `work-planner/work-planner.SKILL.md`, `work-planner/postwork.SKILL.md`, `architect/architect.SKILL.md`, `code-whisperer/code-whisperer.SKILL.md`, `rust-tester/rust-tester.SKILL.md`, `rust-coder/rust-coder.SKILL.md`
  - **focus**: rust-dioxus/src/, rust-dioxus/tests/line_diff_engine.rs, .project/architecture/dioxus-diff-viewer.arch.jsonl, .project/requirements/diff-viewer.reqts.jsonl
  - Related-architecture-set: dioxus-diff-viewer.arch.jsonl alone; expansion from ARCH-jf3s5npp9s, ARCH-exgfx6rwdt, ARCH-kjjykjtt5r, ARCH-n7wmjnmt65, ARCH-atczcqvdsz, and ARCH:dcisn-wma0h4mndz added no further file, since every FK resolves within that file and no module_surrogate or sub-arch exists. Deliverable verification passed on the reference-case criterion, the unchanged-TypeScript criterion, the nine in-scope child requirements, the worker's test and lint claims, the provisional-identifier sweep, the pointer check, and the glossary sweep; the engine-output-alone criterion passed except for the gap in AUDIT:finding-3tvc9b4x0m. Set aside as unanchored: whether the crate should carry a README or published documentation before the component lands, which nothing in the mission, the epic, or the requirements asks for.

- **finding** `AUDIT:finding-3tvc9b4x0m` (F1) · type `spec-divergence` · severity `high` · priority `crit` · status `fixed` · reviewer _Architect_ · In EntryBuilder::push (appends one entry and records its position when it holds a change) at rust-dioxus/src/line_diff_engine.rs:236-241
  - `changed_positions` records a position only when `entry.change != ChangeKind::Unchanged`, so an unchanged pair carrying a line ending change is absent from it. The component folds by distance from the positions in `changed_positions` (REQT-qcnxhemvhn, Folded unchanged lines) and must show each side's terminator as a chip (REQT-4zyjfjrhd3, Line ending chips), so an isolated line ending change is folded away and can never be seen.
  - **evidence**: rust-dioxus/src/line_diff_engine.rs:236-241 gates the `changed_positions.push` on `entry.change != ChangeKind::Unchanged`, and rust-dioxus/src/line_diff_engine.rs:187 sets `line_ending_change` on unchanged pairs. A scratch probe of `line_diff("a\rb", "a\nb", ...)` at the capture-time check returned entry 0 as Unchanged with `line_ending_change: Some(("\r", "\n"))` and `changed_positions: []`. ARCH-exgfx6rwdt (LineDiff) states the output holds `positions of entries holding a change` and lists REQT-qcnxhemvhn among its supported requirements (.project/architecture/dioxus-diff-viewer.arch.md:220-232). .project/requirements/diff-viewer.reqts.md:71 requires the pair be marked as carrying a line ending change, and :77 requires the chip in both views. The work unit's tests assert the ending change itself (rust-dioxus/tests/line_diff_engine.rs:208-229) but never assert its position.
  - **Line ending changes are missing from changed_positions** · decision `judgement-needed` · execution `agent-trivial`
    - _An unchanged pair carrying a line ending change is absent from changed_positions, so folding hides it and its chip never shows._
    - **Context**:
      Re-orienting: the engine's changed_positions, which the component folds by.
      Catch-up:
      - Where: EntryBuilder::push, rust-dioxus/src/line_diff_engine.rs:236-241.
      - Purpose: tells the component where the changes sit.
      - Situation: a line ending change lands on a pair marked unchanged.
      - Goal: every entry a reader must see stays out of a fold.
    - **Key points**:
      - An unchanged pair may carry a line ending change (line_diff_engine.rs:187).
      - changed_positions gates on the change kind alone (line_diff_engine.rs:236-241).
      - REQT-qcnxhemvhn (Folded unchanged lines) folds unchanged lines far from every change.
      - REQT-4zyjfjrhd3 (Line ending chips) needs the chip visible in both views.
      - ARCH-exgfx6rwdt (LineDiff) calls these the positions of entries holding a change.
    - **Resolution points**:
      - `ending-change-counts-as-change`: record the position when the entry is not unchanged or carries a line ending change, and assert it in a test. The entry keeps ChangeKind::Unchanged, so neither view's marking shifts.
      - `component-scans-entries`: leave the engine as it is and have the component scan every entry for an ending change while folding; demoted because ARCH-exgfx6rwdt (LineDiff) makes changed_positions the folding input and WORK:crit-113mtz82xb requires folding from the engine's output alone.
    - **Substance**: Contract correctness and requirements coverage: an isolated line ending change becomes unreachable in the component, failing REQT-4zyjfjrhd3 (Line ending chips) and weakening WORK:crit-113mtz82xb (engine output alone).
    - **Actionability**: Workable: one condition in EntryBuilder::push plus a test asserting the position.
    - **Specificity** `under-design`: ARCH-exgfx6rwdt (LineDiff) states what changed_positions holds in words that do not settle whether a line ending change counts as a change; the design material leaves the case unstated.
  - **resolution**: EntryBuilder::push now records a changed position when the entry is not unchanged or carries a line ending change, so such a pair stays out of a fold and its chip can show. The entry keeps ChangeKind::Unchanged, so neither view's row marking shifts. A new test asserts both halves: the changed position, and that the pair still reads unchanged on both sides.

- **finding** `AUDIT:finding-6pyv3h5w7r` (F2) · type `structural-mismatch` · severity `medium` · priority `high` · status `fixed` · reviewer _Architect_ · In split_lines (splits a text into lines, holding each terminator aside) at rust-dioxus/src/line_diff_engine.rs:100-139, against ARCH-atczcqvdsz (Line diff hand-off) in .project/architecture/dioxus-diff-viewer.arch.jsonl
  - The engine does its own line tokenization and hands `similar` only the line texts, which is the choice that makes line identity ignore terminators while keeping each terminator available. The architecture says only that the engine asks `similar` for alignment; no decision record carries the own-tokenizer choice or the LineDiff structure the implementation settled on, so plan, implementation, and architectural capture disagree.
  - **evidence**: rust-dioxus/src/line_diff_engine.rs:6-10 and :100-139 implement the crate's own line splitting, and :40 passes pre-split line texts to `TextDiff::from_slices`. .project/architecture/dioxus-diff-viewer.arch.md:180-190 (ARCH-atczcqvdsz, Line diff hand-off) states `the engine asks similar for alignment` with no mention of tokenization ownership, and :247-262 (ARCH:dcisn-wma0h4mndz) records only the engine-versus-component split. The choice is recorded in WORK:report-d70x7f2ecw under Decisions, which the Evidence rule of architect.AUDIT.post-work.md forbids as the resting place for architectural capture. ARCH-kjjykjtt5r (PairedLineEntry) names `inline change tokens` and `line ending change` without the token-kind and per-side structure the code settled (rust-dioxus/src/line_diff_output.rs:37-75).
  - **The engine's own line tokenization is uncaptured** · decision `obvious-essential` · execution `agent-nontrivial`
    - _The engine splits lines itself and hands similar only line texts; the architecture still says only that the engine asks similar for alignment._
    - **Context**:
      Re-orienting: the line diff hand-off record against the engine's tokenization.
      Catch-up:
      - Where: ARCH-atczcqvdsz (Line diff hand-off) in dioxus-diff-viewer.arch.jsonl.
      - Purpose: names how the engine and similar divide the work.
      - Situation: the engine kept tokenization and gave similar only line texts.
      - Goal: the architecture reads as what was built.
    - **Key points**:
      - split_lines is the engine's own tokenizer (line_diff_engine.rs:100-139).
      - That choice is what lets line identity ignore terminators.
      - ARCH-atczcqvdsz (Line diff hand-off) says only that the engine asks similar for alignment.
      - The reason lives in WORK:report-d70x7f2ecw, which the Architect's evidence rule excludes as architectural capture.
      - ARCH-kjjykjtt5r (PairedLineEntry) omits the token-kind and per-side structure that shipped.
    - **Resolution points**:
      - `capture-the-tokenization-decision`: add a decision record for the engine's own line tokenization, carrying the tension that similar keeps terminators inside line tokens and the consequence that line identity compares text alone; amend ARCH-atczcqvdsz (Line diff hand-off) to say the engine hands similar pre-split line texts; extend ARCH-kjjykjtt5r (PairedLineEntry) at gesture altitude to name the per-side inline tokens and their kinds.
    - **Substance**: Maintainability and contract correctness: a next worker reading the architecture would take similar's line tokenizer as the alignment input and reintroduce the terminator hazard WORK:advice-nj1vrndsnf named.
    - **Actionability**: Workable: one decision record and two field amendments in the existing architecture file.
    - **Specificity** `under-design`: ARCH-atczcqvdsz (Line diff hand-off) states the hand-off's payload and trigger but leaves the division of tokenization unstated.
  - **resolution**: ARCH:dcisn-zyt966vq09 (Engine tokenizes lines, similar aligns them) now carries the choice with its tension and consequence. ARCH-atczcqvdsz (Line diff hand-off) says the engine splits the lines itself and asks similar to align their texts, and its similar-facing edge label reads line-text and token alignment. ARCH-kjjykjtt5r (PairedLineEntry) names the per-side inline tokens, their marks, and that each side rejoins to its own text. ARCH-exgfx6rwdt (LineDiff) carries AUDIT:finding-3tvc9b4x0m's resolution: the positions include a pair whose terminators differ. All stay DREAMED by stakeholder steering, since this is capture of what the implementation settled rather than text reviewed line by line.

- **finding** `AUDIT:finding-zwe9mz92sf` (F3) · type `performance-concern` · severity `medium` · priority `high` · status `fixed` · reviewer _Rust Coder_ · In LineSide.text and InlineToken.text (the text each output record carries) at rust-dioxus/src/line_diff_output.rs:32 and :53, produced at rust-dioxus/src/line_diff_engine.rs:167, :176, and :285-304
  - Every line text and every inline token is an owned `String` copied out of the input texts, and an unchanged inline token is copied twice, once per side. Under the default character comparison a modified line allocates roughly two `String`s per character. A feasible restructuring exists: the output could borrow from the input texts through a lifetime parameter or carry `Cow<'_, str>`, and a shared `Arc<str>` of each input text with per-side ranges would keep the output `'static` for Dioxus state while removing every copy.
  - **evidence**: rust-dioxus/src/line_diff_output.rs:6-8 states the owned-text choice and its reason; :32 and :53 declare `text: String`. rust-dioxus/src/line_diff_engine.rs:167 and :176 call `line.text.to_owned()` per side, and :285-295 call `change.value().to_owned()` per token with a further `text.clone()` for each unchanged token. rust-dioxus/src/line_diff_engine.rs:33-34 shows the inputs are borrowed `&str`, so the line texts are already subslices of them. rust-coder.AUDIT.post-work.md Lens 1 asks whether a feasible restructuring would eliminate the copy and states that written justifications do not discharge the finding. No requirement or acceptance criterion names ownership; the only source for the choice is the module header and WORK:report-d70x7f2ecw.
  - **Every line text and inline token is copied** · decision `judgement-needed` · execution `agent-nontrivial`
    - _The output owns copies of text already held as subslices of the inputs; character comparison allocates about two Strings per character._
    - **Context**:
      Re-orienting: text ownership in the engine's output.
      Catch-up:
      - Where: LineSide.text and InlineToken.text, rust-dioxus/src/line_diff_output.rs:32 and :53.
      - Purpose: carries each line's and each token's text to the component.
      - Situation: the inputs are borrowed and their subslices are copied out.
      - Goal: the component holds the output across renders without per-character allocation.
    - **Key points**:
      - to_owned runs per side (line_diff_engine.rs:167, :176) and per token (:285-295).
      - An unchanged token is cloned once for each side.
      - The stated reason is that Dioxus state needs 'static.
      - An Arc<str> of each input with per-side ranges keeps 'static and drops the copies.
      - Rusty's zero-copy lens says a written justification does not discharge the finding.
    - **Resolution points**:
      - `record-the-ownership-tradeoff`: keep the owned text as shipped and record the tradeoff on ARCH-kjjykjtt5r (PairedLineEntry), so the choice stops reading as an unexamined default and the component work unit inherits it deliberately.
      - `share-the-input-texts`: carry each input text as an Arc<str> in the LineDiff with per-side byte ranges, keeping the output 'static while removing every copy; demoted because it changes the public shape WORK-6ds59btb76 (Diff viewer Dioxus component) is already planned against, and no measurement yet shows the allocation matters.
    - **Substance**: Maintainability and performance: the allocation grows with text size on every recomputation, and the ownership shape is public, so changing it later reaches the component.
    - **Actionability**: Workable either way: a recorded tradeoff, or a change to the output types before the component depends on them.
    - **Specificity** `under-design`: ARCH-exgfx6rwdt (LineDiff) and ARCH-kjjykjtt5r (PairedLineEntry) describe the output's content without stating ownership, so the material left the choice to the worker.
  - **resolution**: An InlineToken now carries a Range<usize> into its own side's line text instead of a copy, so the engine allocates per line rather than per character; compute_inline_changes walks cumulative old and new offsets and debug-asserts that each side's tokens cover its line. PairedLineEntry::old_inline_tokens and new_inline_tokens yield (TokenKind, &str) in line order, so a consumer reads text without touching a range, while InlineChanges stays public for one that wants positions. The 'static argument was found not to bear on the question at all: Arc<str> and a range into owned text are both 'static, and it ruled out only the LineDiff<'a> borrow-from-caller form. Line texts stay owned by stakeholder steering; the shared-input form for them is closed, not deferred.

- **finding** `AUDIT:finding-pkcg2qs22f` (F4) · type `maintainability-concern` · severity `medium` · priority `med` · status `fixed` · reviewer _Code Whisperer_ · In LineDiffOptions.inline_changes (the consumer's on-or-off choice) at rust-dioxus/src/line_diff_options.rs:25, against PairedLineEntry.inline_changes (the tokens themselves) at rust-dioxus/src/line_diff_output.rs:89
  - One spelling visible outside its module carries two semantics: on the options it is the consumer's choice whether to compute inline changes, on the entry it is the computed inline changes. The option is also a bare `bool` at a public interface, where a two-variant enum would say at each call site which way the switch points.
  - **evidence**: rust-dioxus/src/line_diff_options.rs:23-25 declares `pub inline_changes: bool` with the doc `Whether modified lines carry the tokens removed and added within them`; rust-dioxus/src/line_diff_output.rs:86-89 declares `pub inline_changes: Option<InlineChanges>` as the tokens. Both are public through rust-dioxus/src/lib.rs:13-16. code-whisperer.SKILL.md Naming Stewardship item 7 forbids one spelling carrying two semantics for any name visible outside its module. rust-coder.SKILL.md Specific Patterns prescribes replacing a `bool` parameter with a two-variant enum. The tests show the call-site reading: `inline_changes: false` at rust-dioxus/tests/line_diff_engine.rs:127, :150, :186, and six further sites. REQT-4nz35dscrn (Inline changes) names the behavior but not the option's spelling.
  - **inline_changes names both the switch and the tokens** · decision `judgement-needed` · execution `agent-trivial`
    - _One public spelling carries two semantics, and the option is a bare bool where a two-variant enum would read at the call site._
    - **Context**:
      Re-orienting: the inline changes option against the inline changes field.
      Catch-up:
      - Where: LineDiffOptions.inline_changes, rust-dioxus/src/line_diff_options.rs:25.
      - Purpose: lets the consumer turn inline changes off.
      - Situation: PairedLineEntry.inline_changes holds the tokens under the same name.
      - Goal: one spelling, one semantic, readable at every call site.
    - **Key points**:
      - Both names are public through lib.rs:13-16.
      - Naming Stewardship forbids one spelling carrying two semantics beyond its module.
      - `inline_changes: false` appears at nine call sites in the tests.
      - Rusty prescribes a two-variant enum in place of a public bool.
    - **Resolution points**:
      - `name-the-switch-by-its-question`: rename the option to `mark_inline_changes` and leave PairedLineEntry.inline_changes as the tokens, which separates the two semantics in one rename.
      - `two-variant-enum`: replace the bool with an InlineChanges enum of Marked and Unmarked; demoted because that name collides with the output struct InlineChanges and would need its own naming pass first.
    - **Substance**: Maintainability: these public names are the contract the component and downstream apps read, and renaming after they depend on it is costly.
    - **Actionability**: Workable: one field rename and nine call sites in the tests.
    - **Specificity** `under-design`: The crate's public vocabulary was settled at WORK:advice-98x4z0fcpf to follow the requirement terms; REQT-4nz35dscrn (Inline changes) names the behavior and leaves the option's own spelling unstated.
  - **resolution**: The option is now LineDiffOptions::mark_inline_changes, so a call site reads mark_inline_changes: false and the spelling inline_changes belongs to the tokens alone, beside old_inline_tokens and new_inline_tokens. Nine test call sites moved with it. Turning inline changes off stays its own option rather than a fourth CompareMethod variant: the requirements keep compare method and inline-changes-on-or-off in separate groups, and folding them would conflate how to compare with whether to compare.

- **finding** `AUDIT:finding-2tw2q28t0s` (F5) · type `traceability-gap` · severity `medium` · priority `med` · status `in-review` · reviewer _Work Planner_ · In the Files section (the requirements document's inventory of implementation files) of .project/requirements/diff-viewer.reqts.md:120
  - The requirements document's Files section is empty although the crate now implements nine in-scope child requirements across four source files. The traceability audit's reverse check reads that list to find the implementation, so with no `file` records the requirements have no recorded path to the code that satisfies them.
  - **evidence**: `.project/requirements/diff-viewer.reqts.md:120-121` renders `# Files` with no entries. reqm.SKILL.md Standard Sections item 8 states the section is auto-generated from JSONL `file` records. reqm.AUDIT.traceability.md names `the implementation files listed in the ## Files section` as its target artifact and its Procedure step 4 runs the reverse check over them. The code carries the per-requirement annotations already: rust-dioxus/src/line_diff_options.rs:8, :12-14, :24, :27, rust-dioxus/src/line_diff_output.rs:11, :28, :31, :36, :58, :68, :88, :91, and rust-dioxus/src/line_diff_engine.rs:31, :36, :71, :104, :146, :216, :253, :271-277 name all nine in-scope child requirements, so the inputs for the `file` records are in hand now.
  - **The requirements document lists no implementation files** · decision `obvious-essential` · execution `agent-trivial`
    - _The Files section is empty while four source files implement nine in-scope requirements, so the traceability reverse check has nothing to read._
    - **Context**:
      Re-orienting: the Files section of the diff viewer requirements.
      Catch-up:
      - Where: .project/requirements/diff-viewer.reqts.md:120.
      - Purpose: the recorded path from a requirement to the code satisfying it.
      - Situation: the crate shipped and no file record was added.
      - Goal: each in-scope requirement reachable from its implementation file.
    - **Key points**:
      - The section is generated from file records, of which there are none.
      - The code already carries per-requirement annotations in all four source files.
      - The reqm traceability audit reads this list for its reverse check.
      - The inputs are in hand, so #touch-paperwork-once closes this here rather than later.
    - **Resolution points**:
      - `add-the-four-file-records`: add file records for rust-dioxus/src/lib.rs, line_diff_engine.rs, line_diff_options.rs, and line_diff_output.rs, each naming what it implements.
    - **Substance**: Requirements coverage: without the file list, no reverse check can say which in-scope requirement has no implementation.
    - **Actionability**: Workable: four records in the existing requirements file.
    - **Specificity** `not-design-related`: Not design material: the Files section is generated inventory, and the gap is a missing record rather than an unstated choice.

- **finding** `AUDIT:finding-fb3tvpfhqq` (F6) · type `coverage-gap` · severity `medium` · priority `med` · status `in-review` · reviewer _Rust Tester_ · In the Properties section (the two tests asserting invariants over the engine's output) at rust-dioxus/tests/line_diff_engine.rs:527-607
  - Both tests under the Properties heading iterate a hard-coded list of three to five input pairs, so they are example tests wearing a property's name. The invariants they state, that each side's line texts rejoin to that side's trimmed input and that each side's numbers run contiguously from the offset, hold over all inputs and are exactly the shape generated inputs would exercise.
  - **evidence**: rust-dioxus/tests/line_diff_engine.rs:534-540 and :572-576 declare fixed `cases` vectors; no generator or shrinking is involved and the crate's Cargo.toml:7-8 carries no dev-dependency. rust-tester.SKILL.md Property-Based Testing names `proptest` and `quickcheck` for exactly this, and its Advanced Testing Tools table lists `proptest` for invariant validation and edge case discovery. WORK:report-d70x7f2ecw records the same observation under Observations and states the question belongs to post-work rather than to the consented plan. WORK:advice-zaj371p040 drafted them as `property tests` without naming a generator, so the delivered shape matches the approved pre-work strategy.
  - **The property tests run fixed cases, not generated ones** · decision `judgement-needed` · execution `agent-nontrivial`
    - _Both tests under the Properties heading iterate a hard-coded list, so universal invariants are checked on three to five inputs._
    - **Context**:
      Re-orienting: the two invariant tests closing the engine's suite.
      Catch-up:
      - Where: rust-dioxus/tests/line_diff_engine.rs:527-607.
      - Purpose: hold the rejoin and contiguous-numbering invariants over all inputs.
      - Situation: each iterates a hard-coded list of three to five text pairs.
      - Goal: the invariants exercised by inputs no author chose.
    - **Key points**:
      - No generator or dev-dependency is involved (Cargo.toml:7-8).
      - Both invariants hold over all inputs, which is what generators are for.
      - The worker recorded the same observation and handed the question to post-work.
      - The shipped shape matches the approved pre-work strategy at WORK:advice-zaj371p040.
    - **Resolution points**:
      - `generate-the-property-inputs`: add proptest as a dev-dependency and drive both invariants from a generated text pair, keeping the existing cases as named examples.
      - `rename-to-what-they-are`: leave the tests as they are and rename the heading and both tests as example tests; demoted because both invariants are genuinely universal, and the chosen examples are already covered by the reference-case table elsewhere in the suite.
    - **Substance**: Test coverage: these invariants are the engine's strongest correctness statements and are checked only on inputs chosen by the author of the code.
    - **Actionability**: Workable either way: a dev-dependency and two generators, or a rename.
    - **Specificity** `under-design`: WORK:advice-zaj371p040 (Test strategy for the engine) prescribed property tests without naming a generator, so the plan left the strength of the check unstated.

- **finding** `AUDIT:finding-8y4219ghpa` (F7) · type `coverage-gap` · severity `low` · priority `med` · status `in-review` · reviewer _Rust Tester_ · In the test suite as a whole (rust-dioxus/tests/line_diff_engine.rs), against split_lines at rust-dioxus/src/line_diff_engine.rs:106-139 and the error-free contract of ARCH-atczcqvdsz (Line diff hand-off)
  - Two input shapes the engine deliberately handles have no test: a bare `\r` terminator, which `split_lines` recognizes and which REQT-4zyjfjrhd3 (Line ending chips) names as a chip value, and an empty text on either side, which ARCH-atczcqvdsz promises yields a LineDiff. The suite was drawn from the reference cases and the requirement statements, so input shapes outside both went uncovered.
  - **evidence**: rust-dioxus/src/line_diff_engine.rs:113-120 treats a lone `\r` as a terminator, and :102-105 documents the intent. A search of rust-dioxus/tests/line_diff_engine.rs for a bare `\r` case and for `line_diff(""` returned no matches at the capture-time check; the terminator tests at :107-121 and :208-229 both use `\r\n` against `\n`. A scratch probe at the capture-time check confirmed the behavior is already correct: `line_diff("a\rb", "a\nb", ...)` reports the ending change as `("\r", "\n")`, `line_diff("", "")` yields no entries, and `line_diff("", "abc")` yields one added entry. .project/requirements/diff-viewer.reqts.md:77 names `\r` among the chip values.
  - **Bare carriage return and empty texts have no test** · decision `obvious-essential` · execution `agent-trivial`
    - _The engine handles a lone carriage return and an empty text on either side, and no test asserts either behavior._
    - **Context**:
      Re-orienting: input shapes outside the reference cases and the requirement statements.
      Catch-up:
      - Where: rust-dioxus/tests/line_diff_engine.rs as a whole.
      - Purpose: hold the engine to what its code and architecture promise.
      - Situation: split_lines recognizes a lone carriage return and the suite never uses one.
      - Goal: every deliberately handled input shape has a test.
    - **Key points**:
      - split_lines treats a lone carriage return as a terminator (line_diff_engine.rs:113-120).
      - REQT-4zyjfjrhd3 (Line ending chips) names the bare carriage return among the chip values.
      - ARCH-atczcqvdsz (Line diff hand-off) promises every pair of texts yields a LineDiff.
      - A probe confirmed all three behaviors are already correct, so these are tests, not fixes.
    - **Resolution points**:
      - `add-three-cases`: add one test for a bare carriage return against a newline, and extend the rejoin test's case list with an empty text on each side.
    - **Substance**: Test coverage: correct behavior with nothing holding it, so a later change to split_lines or to the trimming would go unnoticed.
    - **Actionability**: Workable: one test and two case-list entries.
    - **Specificity** `not-design-related`: Not design material: REQT-4zyjfjrhd3 (Line ending chips) and ARCH-atczcqvdsz (Line diff hand-off) already specify the behaviors; only the tests are absent.

- **finding** `AUDIT:finding-n6j5d8cb2w` (F8) · type `maintainability-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Rust Coder_ · In CompareMethod (the consumer's choice of how a modified line's two texts are compared) at rust-dioxus/src/line_diff_options.rs:6-16
  - `CompareMethod` is a public enum the project already expects to grow, and it carries no `#[non_exhaustive]`, so a consumer's exhaustive match breaks when a fourth method lands. `ChangeKind` and `TokenKind` are closed by their requirements and are not part of this.
  - **evidence**: rust-dioxus/src/line_diff_options.rs:6-16 declares the three variants with no `#[non_exhaustive]`, and rust-dioxus/src/lib.rs:13 exports it. WORK:advice-rpqenvbxcb records that README.md:151-165 lists seven jsdiff compare methods while the requirements define three, with the mapping of the other four carried by WORK-6ds59btb76 (Diff viewer Dioxus component), so growth is anticipated rather than speculative. rust-coder.SKILL.md Specific Patterns prescribes `#[non_exhaustive]` for types likely to change. REQT-hmsfnfe5wc (Line marking) and REQT-4nz35dscrn (Inline changes) enumerate the marks and token kinds exhaustively, which is why the other two enums are excluded here.
  - **CompareMethod is exhaustive and expected to grow** · decision `advisory` · execution `agent-trivial`
    - _Four more compare methods are anticipated and the enum carries no non_exhaustive, so a consumer's exhaustive match breaks when one lands._
    - **Context**:
      Re-orienting: how the compare method enum grows.
      Catch-up:
      - Where: rust-dioxus/src/line_diff_options.rs:6-16.
      - Purpose: the consumer's choice of how a modified line's texts are compared.
      - Situation: three of the reference's seven methods are offered so far.
      - Goal: adding a fourth does not break a consumer's match.
    - **Key points**:
      - WORK:advice-rpqenvbxcb records the other four methods as the component unit's scope.
      - Rusty prescribes non_exhaustive for types likely to change.
      - ChangeKind and TokenKind are closed by their requirements and stay exhaustive.
    - **Resolution points**:
      - `mark-compare-method-non-exhaustive`: add #[non_exhaustive] to CompareMethod alone, which costs nothing inside the crate and keeps a later variant from breaking downstream apps.
    - **Substance**: Maintainability: a breaking change for downstream apps is avoidable now at no cost, and not avoidable once apps depend on the enum.
    - **Actionability**: Workable: one attribute.
    - **Specificity** `under-design`: ARCH-n7wmjnmt65 (LineDiffOptions) names the three compare methods without saying whether the set is closed.

- **finding** `AUDIT:finding-8bxs2tvt76` (S1) · severity `low` · priority `low` · status `withdrawn` · reviewer _Architect_ · In line_diff (the engine's entry point) at rust-dioxus/src/line_diff_engine.rs:30-34, where each text's end is trimmed
  - A one-line text differing from another only in its terminator, `a\r\n` against `a\n`, reports no change at all, because trimming removes the sole terminator and REQT-rtwn1qresp (Line ending changes) needs both lines terminated.
  - **evidence**: A scratch probe at the capture-time check returned one Unchanged entry with no line ending change for `line_diff("a\r\n", "a\n", ...)`. .project/requirements/diff-viewer.reqts.md:69 (REQT-9tze98pt6g, Trailing whitespace) requires whitespace at each text's end be ignored, and :71 (REQT-rtwn1qresp, Line ending changes) conditions the mark on both lines ending with a terminator. The reference does the same: src/compute-lines.ts:149-151 calls `trimRight()` on each whole text before diffing.
  - **resolution**: Withdrawn: not a defect. The two requirements state this interaction explicitly and the engine follows both, matching the reference's own trimming. Changing it would need REQT-9tze98pt6g (Trailing whitespace) amended, which no stakeholder input asks for.

- **finding** `AUDIT:finding-60ezgk4dgk` (S2) · severity `low` · priority `low` · status `withdrawn` · reviewer _Rust Observability_ · In the crate manifest (its dependency list) at rust-dioxus/Cargo.toml:7-8, and across rust-dioxus/src/ where no instrumentation appears
  - The crate carries no `tracing` dependency and no instrumentation, although the requirements name tracing among the project's technologies and the work unit seats the Rust Observability skill as an implementer.
  - **evidence**: rust-dioxus/Cargo.toml:7-8 lists `similar` alone, and a search of rust-dioxus/src/ for `tracing` returned no matches at the capture-time check. .project/requirements/diff-viewer.reqts.md:16 names tracing as a related technology, not an essential one. ARCH-atczcqvdsz (Line diff hand-off) records `None on text input: every pair of texts yields a LineDiff`, so there is no failure path to report and no I/O boundary to instrument. WORK:report-d70x7f2ecw records the same reasoning under Decisions.
  - **resolution**: Withdrawn: not a gap. `line_diff` is a pure total function with no I/O and no failure case, so entry and exit spans would add noise without diagnostic value. Instrumentation earns its place in WORK-6ds59btb76 (Diff viewer Dioxus component), where rendering and consumer callbacks appear.

- **finding** `AUDIT:finding-bwyw91t8hb` (S3) · severity `low` · priority `low` · status `withdrawn` · reviewer _Work Planner_ · In the impl_status of the nine in-scope child requirements at .project/requirements/diff-viewer.reqts.md:61-87, and in the work unit's unchecked acceptance criteria and absent work_commits records
  - The in-scope requirements still read NEXT rather than IN PROGRESS, the acceptance criteria are unchecked, and no work_commits record exists, although the implementation is complete.
  - **evidence**: .project/requirements/diff-viewer.reqts.md:61-71 and :86 show NEXT on all nine in-scope children. work-planner.SKILL.md, Worker Acceptance, states the NEXT-to-IN-PROGRESS move is the worker's and that the Work Planner does not gate on it. postwork.SKILL.md Phase 4 assigns the commit recording and the advance to IMPLEMENTED/NEEDS VERIFICATION to the final stage, after this review. WORK:report-d70x7f2ecw states the criteria were left unchecked deliberately because verifying them is post-work's own step.
  - **resolution**: Withdrawn: the protocol's own sequence, not a gap. All three land in Phase 4 of this review, after the findings resolve.







## Reports


### 2026-09-15T19:33:00Z — Work Planner
<a id="work:report-df8dwhn43a"></a>
`WORK:report-df8dwhn43a`

**Commits**
- `9a0699b`: line change and line number requirements drafted DREAMED for this unit's scope.

**Decisions**
- The Rust Roundup UI group (Dioxus Coder, Dioxus UI Patterns, Appshell Expert, Forms Expert) is left off this unit because the engine has no UI; the mission names it for the crate as a whole, and the component work unit takes it.

**Observations**
- `.project/architecture/` does not exist yet, so the first architecture record for this crate (the candidate architecture work-ready asks for) needs the Architect's placement choice first; low severity.

**For the next worker**
- `.project/` is a plain git worktree on branch `project` with no `.weaver/` tooling, so the Work Organizer's weave gates have nothing to run; commit inside the worktree with `git -C .project`.
- The toon MCP server is registered at user scope in `~/.claude.json`, not in a project `.mcp.json`; toon tools work in this repo regardless.


### 2026-09-15T23:20:00Z — Rust Tester
<a id="work:report-ayg53vvte0"></a>
`WORK:report-ayg53vvte0`

**Commits**
- `4eb9d2a`: settles the reference-case criterion, the porting-hazard wording, and the changed-line positions advice.

**Decisions**
- Reference cases whose expectation is a reference defect are recorded as differing with that cause, rather than carried over or blamed on `similar`.
- The changed-line positions advice was dismissed rather than acted on: ARCH-exgfx6rwdt (LineDiff) and ARCH-jf3s5npp9s (LineDiffEngine) already state that positions index the entries holding a change.
- The drafted tests assert observable output. The character-comparison test asserts which character is marked rather than the whole token split, since splitting is `similar`'s business, not a requirement.

**Blockers and stubs**
- `rust-dioxus/tests/line_diff_engine.rs` does not compile until the crate exists; the first activity establishes it. The file names a provisional entry point (`line_diff`, `LineDiffOptions`, `LineDiff`) drawn from the architecture records, and its header says the worker owns the exact spelling.

**For the reviewer**
- The coverage map is the test names: each carries the REQT id it covers, so post-work can check every in-scope child requirement against one named test.
- Four reference cases expect a changed-row index past the last row. They are recorded as a reference defect in the reference-case test, with the source lines named beside them.

**For the next worker**
- The overcount in the reference comes from jsdiff merging consecutive added tokens into one change: the nested call for the paired added line and the outer removed line each increment the row counter (`src/compute-lines.ts:197-211`, `:251`).
- `similar`'s line tokenizer keeps `\n`, `\r\n`, and a bare `\r` inside each line token (`~/.cargo/registry/src/*/similar-3.2.0/src/text/abstraction.rs:164-190`), which is why line identity has to drop terminators while REQT-rtwn1qresp (Line ending changes) still needs them.
- `.project/` is a plain git worktree on branch `project` with no `.weaver/` tooling, so the weave gates have nothing to run; commit inside it with `git -C .project`.


### 2026-09-15T23:35:38Z — Coder
<a id="work:report-d70x7f2ecw"></a>
`WORK:report-d70x7f2ecw`

**Commits**
- `28289d6`: the crate, the engine, and the drafted tests brought up to passing.
- `6d53a74`: a test holding the engine to leaving out the reference's one-space padding.

**Decisions**
- This crate splits lines itself rather than using `similar`'s line tokenizer, so alignment compares line text while each terminator stays available for the line ending change.
- `DiffOp::Replace` does the modified-line pairing: its overlapping old and new runs become the pairs, and the leftover lines fall out as plain removals or additions.
- Entry text is an owned `String` rather than a borrow of the input texts, because a Dioxus component holds this output in state across renders, which needs `'static`.
- No `tracing` dependency and no instrumentation: the engine is a pure function with no I/O and no failure case, so instrumenting it would be entry and exit spam.
- Inline tokens are left unmerged: the pre-work word-comparison test fixes unmerged tokens, and same-kind runs are recoverable from tokens while tokens are not recoverable from runs.

**Observations**
- The pre-work test helpers returned a three-part tuple that clippy's `type_complexity` flagged; a `Row` alias now carries it. Low severity, resolved in the same commit.
- The two property tests run over fixed case lists rather than generated inputs, which is the shape the pre-work strategy drafted and the sign-off approved. Whether these invariants deserve generator-driven coverage is a live question for post-work, not a gap against the consented plan. Low severity.

**For the reviewer**
- Three tests carry the reference-case accounting, each named for its disposition: all nine cases in one table, the reference's changed-row overcount, and the character-token granularity difference.
- Every in-scope child requirement is covered by the test whose doc comment carries its REQT id, as the pre-work strategy laid out.
- The first acceptance criterion is checkable from `LineDiff` alone: each entry carries both sides with their numbers, the change kind, inline tokens, and any ending change, and `changed_positions` gives folding its distances.
- `cargo test` (19 passing), `cargo clippy --all-targets` (clean), and `cargo fmt --check` all pass in `rust-dioxus/`.
- The acceptance criteria are left unchecked deliberately; verifying them is post-work's own step.

**For the next worker**
- The crate root is `rust-dioxus/` itself: its `Cargo.toml` names the package `dioxus-diff-viewer`, and no workspace sits above it.
- Builds land in `/home/san/dev/.cargo-target` through the user's cargo config rather than `rust-dioxus/target`, so that is where artifacts are.
- `dioxus` is deliberately absent from the dependencies; the component work unit adds it, which keeps engine test compiles fast.
- The reference records an added-only row as an empty `left` object while the engine records an absent old side, which is why the reference-case table asserts `None` there.
- Unmerged inline tokens mean character comparison hands the component one token per character, where the reference handed it one per run. Merging adjacent same-kind tokens for rendering is the component's call and is a pure function of what the engine already returns.






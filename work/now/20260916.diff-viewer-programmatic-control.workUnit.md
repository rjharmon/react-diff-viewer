
# Programmatic control of the diff viewer

`WORK-98bxwcd28f` · created 2026-09-16 · slug `diff-viewer-programmatic-control`
state `reviewing` · maturity `DREAMED` · kind `preplanned` · source `architecture` · discipline `coding` · variety `expansion`
importance `med` · urgency `low` · priority `low` · epic `EPIC-77znwqj94w`

> **JSONL is authoritative.** This document is auto-generated from `WORK-98bxwcd28f`'s `.workUnit.jsonl`. Do not hand-edit; changes will be overwritten on the next write.



## Status Summary

Post-work Phase 3 is complete: AUDIT:session-dw1td85d9n's four in-review findings are all resolved, and its other seven were filed and resolved during independent analysis. Three post-work sign-offs still read findings-review and are the next gate; before them, the Code Whisperer's post-work gate 1 triggers the reqm traceability audit over this changeset, and the Architect's Post-Work Architectural Capture audit still needs to run. Phase 4 follows: record work_commits, reconcile the range with unowned-commits.sh, and advance REQT-f2affyt2h2, REQT-hsef5r7c4z and REQT-g86vdmyyp9 out of IN PROGRESS. WORK:report-hrek7tkh0f names which commits Phase 4 records and which it excludes. The unit's own plan text predates the vocabulary decisions steering added to its scope; CONTENT-336nr6vsn0 maps the retired terms.

## Problem / Context

- Apps may want to drive a mounted diff viewer from their own code: read where the changes are, read which lines are hidden, and open folds, not only reset them.
- The reference package offers nothing like this: an app passes props in, receives a line-number click, and calls one reset method.
- The component unit left fold state inside the viewer behind a reset-only handle; this unit replaces that handle with a live diff an app holds.

## Plan / Guidance

- Build to the in-scope requirements; they carry the contract for what a live diff answers and what opens and refolds.
- Change and fold positions come from data the engine and component already compute; the engine returns positions, not runs, so runs are derived.
- Keep the component unit's rule: an app never names a fold. Reading is safe; an app names lines, and the crate maps them onto its own folds.
- The engine rename rides with this unit, reaching the engine modules and their filenames, the crate root, the engine tests, the architecture record, the requirements' file list, and the glossary once the code carries the new names.

Terminology Note: ~~live diff~~ is retired; replacement: "diff", which stands against "diff analysis" for the engine's output. ~~LineRun~~ was renamed to LineNumberSpan, and ~~fold reset trigger~~ was retired with the handle it named. This unit's problem context, plan guidance and activity descriptions were written in the earlier vocabulary and are left as written; the crate, the requirements and the architecture records carry the current one.

## Required Skills

- [`skillz/coder/coder.SKILL.md`](../skillz/coder/coder.SKILL.md) · roles: `implementer`

- [`skillz/rust-coder/rust-coder.SKILL.md`](../skillz/rust-coder/rust-coder.SKILL.md) · roles: `implementer`

- [`skillz/rust-observability/rust-observability.SKILL.md`](../skillz/rust-observability/rust-observability.SKILL.md) · roles: `implementer`

- [`skillz/logging/logging.SKILL.md`](../skillz/logging/logging.SKILL.md) · roles: `implementer`

- [`skillz/dioxus-coder/dioxus-coder.SKILL.md`](../skillz/dioxus-coder/dioxus-coder.SKILL.md) · roles: `implementer`

- [`skillz/dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md`](../skillz/dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md) · roles: `implementer`

- [`skillz/appshell-expert/appshell-expert.SKILL.md`](../skillz/appshell-expert/appshell-expert.SKILL.md) · roles: `implementer`

- [`skillz/schemata-crafter/data-forms/forms-expert/forms-expert.SKILL.md`](../skillz/schemata-crafter/data-forms/forms-expert/forms-expert.SKILL.md) · roles: `implementer`

- [`skillz/rust-tester/rust-tester.SKILL.md`](../skillz/rust-tester/rust-tester.SKILL.md) · roles: `domain-tester`

- [`skillz/code-whisperer/code-whisperer.SKILL.md`](../skillz/code-whisperer/code-whisperer.SKILL.md) · roles: `prework-advice`, `postwork-audit`

- [`skillz/architect/architect.SKILL.md`](../skillz/architect/architect.SKILL.md) · roles: `reviewer`




## References

**ARCH**
- `ARCH-y9545npjzg`: fold reset trigger, retired _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-exgfx6rwdt`: engine output, renamed _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH:dcisn-xgpr1asvyn`: app holds the live diff _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH:dcisn-x8fa9g2hm8`: engine rename _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-8tce9rry3b`: the live diff, new _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-smcbvznq0z`: live diff options, new _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-87bw15t11s`: the hook pair, new _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-m4dkxzw6hh`: viewer, reshaped _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-atczcqvdsz`: hand-off, caller changes _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-n7wmjnmt65`: engine options, renamed _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-jf3s5npp9s`: engine component, renamed _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`


**REQT**
- `REQT-nyjk0c9fbf`: the app-held live diff _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-jyhvdaa75p`: reading changes and hidden lines _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-p3xyw73v2p`: opening and refolding _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-v748c7mjr6`: expanding folds, both routes _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-869jyzdes7`: fold state ownership _(role: scope)_ · `diff-viewer.reqts.jsonl`


**WORKUNIT**
- `WORK-6ds59btb76`: diff viewer component _(role: dependency)_ · `20260915.diff-viewer-component.workUnit.jsonl`


**OTHER**
- `EPIC-77znwqj94w`: port epic _(role: context)_ · `rust-dioxus-port.workEpic.jsonl`





## Focus Files

- `rust-dioxus/src/lib.rs`: The crate root and the names an app imports; both renames and the new live diff and its hooks land in its exports.

- `rust-dioxus/src/line_diff_engine.rs`: Holds the engine function being renamed, in a module whose filename changes with it.

- `rust-dioxus/src/line_diff_output.rs`: Holds the engine's output type being renamed, alongside the paired line entries and changed positions the reading activity derives change runs from.

- `rust-dioxus/src/line_diff_options.rs`: Holds the engine's options type, which renames with the engine and which the live diff's own options value composes.

- `rust-dioxus/src/diff_viewer.rs`: Where the viewer today calls the engine, computes the rendered-content identity, holds the fold state and plans rows; all of it moves to the live diff, and the props for what the live diff owns go.

- `rust-dioxus/src/fold_planning.rs`: The row plan the live diff comes to own, and the only place that decides which entries a fold hides; the hidden-lines answer reads from it.

- `rust-dioxus/src/fold_reset_trigger.rs`: The expanded folds and their basis, moving to the live diff, and the trigger retiring with them; line 75 records why expansions read correctly without a write during rendering.

- `rust-dioxus/src/line_id.rs`: How an app names the line it wants revealed, and the form the crate maps onto its own folds.

- `rust-dioxus/tests/line_diff_engine.rs`: The engine suite the rename touches most; roughly a hundred mentions of the old names, and the evidence that no case was dropped.

- `rust-dioxus/tests/diff_viewer/folding.rs`: The fold behavior already covered through a mounted app, including the reset trigger this unit retires; the new fold actions are exercised the same way.

- `rust-dioxus/tests/diff_viewer/mounted_app.rs`: How component tests mount an app, settle its rendering and read the rows back; the tests for reading and the fold actions build on it.

- `rust-dioxus/examples/theme_preview.rs`: Mounts the viewer through props the reshape removes, so it moves to the live diff and shows an app what the new shape reads like.

- `.project/requirements/diff-viewer.reqts.jsonl`: Carries the in-scope requirements and the file list naming the crate's modules, which the renames and the retired module change.

- `.project/architecture/dioxus-diff-viewer.arch.jsonl`: Holds the two decisions this unit enacts and the two records it renames and retires.

- `.glossary.jsonl`: The project glossary whose fold reset trigger entry retires with the code, and which gains the live diff's names once the crate carries them.

- `rust-dioxus/tests/diff_viewer/views.rs`: Views, change markers and chips, covered through a viewer built from the two texts; every case moves to the live diff.

- `rust-dioxus/tests/diff_viewer/content_rendering.rs`: The consumer renderers and rendered-content identity, driven through the compare and inline-change props the live diff takes over.

- `rust-dioxus/tests/diff_viewer/theming.rs`: The palettes, custom properties and layering, plus the existing two-viewer mounting that shows the shape one live diff behind two viewers needs.

- `rust-dioxus/tests/diff_viewer/line_selection.rs`: Highlighting and line-number clicks, which stay the viewer's own props, reached through a viewer built the retiring way.

- `rust-dioxus/tests/diff_viewer/styling_hooks.rs`: The dxdiff classes each element carries, asserted over a viewer whose construction changes even though its markup does not.

- `rust-dioxus/tests/diff_viewer/reference_cases.rs`: The cases carried over from the TypeScript package's own tests, each mounting a viewer from two texts.






## Activities


### Rename the engine to analyze_diff and DiffAnalysis
<a id="work:activity-hhfakg741g"></a>
`WORK:activity-hhfakg741g`

Carry ARCH:dcisn-x8fa9g2hm8's rename through every place in the crate the earlier names appear: the engine modules and their filenames, the crate root's exports, and the engine's test file. The engine's options type renames with them, so that a consumer importing the crate whole never meets two options types differing by the word the decision set out to disambiguate. No engine behavior changes here, so every engine test keeps its subject and its assertions under the new names. The architecture record, the requirements' file list, and the glossary wait for the last activity, once the code carries the names.




**Acceptance criteria**
- [ ] The engine test suite covers the same ground after the rename as before it, with no case dropped or weakened. · maturity `DREAMED`





### Build the app-held live diff
<a id="work:activity-9b867dj9sw"></a>
`WORK:activity-9b867dj9sw`

Add the live state ARCH:dcisn-xgpr1asvyn describes, reached through the hook pair the in-scope requirements name, with its options value taking the live diff's own name and defaulting every field. It owns the two texts, the engine call, the rendered-content identity, the folding configuration, the expanded folds with their basis, and the planned rows. The expanded-fold state and its basis move here out of the viewer's own module rather than being written a second time, keeping the discipline that module records at rust-dioxus/src/fold_reset_trigger.rs:75, that expansions read correctly without any write during rendering.






### Reshape the viewer around the live diff
<a id="work:activity-gx60bkawf4"></a>
`WORK:activity-gx60bkawf4`

The viewer takes the live diff and keeps only the presentation props the in-scope requirements list for it. It calls no engine, keeps no fold state, and takes no prop for anything the live diff owns, including the folding choices ARCH-phde62wxvn resolved onto the live diff's options. The fold reset trigger retires with the state it guarded. The crate's example and every component test reaching the viewer through the retired props move to the new shape in this activity rather than being left to fail.






### Report changes and hidden lines
<a id="work:activity-pktstta3q3"></a>
`WORK:activity-pktstta3q3`

The live diff answers both questions the in-scope requirements put to it, each in the two sides' plain line numbers. The answers derive from the changed positions the engine already reports and the fold plan the live diff already holds, so neither introduces a second reckoning of where a change or a fold is, and the lines counted as changed stay the ones folding measures from.






### Open and refold from app code
<a id="work:activity-bx7yxakmx2"></a>
`WORK:activity-bx7yxakmx2`

The live diff takes the three actions the in-scope requirements name. An app names a line by its line id and never a fold, so the crate maps a named line onto its own folds, and a named line that is not hidden leaves the shown lines alone. Resolving a named line to its fold reads the same relation between entry positions and each side's line numbers that the reading answers report through, rather than a second one of its own. Because every viewer over one live diff reads one row plan, the result of any of the three shows in all of them. Closing one expanded region from app code stays out, as ARCH-m4dkxzw6hh records.





**Advice**
- _Code Whisperer_ · state `addressed` · type `scope-refinement` · priority `high`: Three of the new answers need the same translation between entry positions and each side's line numbers, and the plan names it as one concern for only two of them. The fold plan and the changed positions are both keyed by position in `diff.entries`, while REQT-f2affyt2h2 and REQT-hsef5r7c4z ask for runs in old-side and new-side line numbers, and REQT-g86vdmyyp9 asks for the reverse, a line id resolved to the fold that hides it. Activity WORK:activity-pktstta3q3 forbids a second reckoning for the two reading answers; activity WORK:activity-bx7yxakmx2 says nothing of the kind, so the reverse translation can grow its own copy of the position-to-number relation. Two copies of that relation drifting apart is the failure: a hidden run reported at one numbering and a named line resolved at another. `WORK:advice-bdghwgkv53`
  - **evidence**: rust-dioxus/src/fold_planning.rs:23-30 defines Fold by `start` and `len` over entry positions; :46-73 plans rows entirely in positions. rust-dioxus/src/line_diff_output.rs:140-146 gives LineDiff `changed_positions`, also positions. rust-dioxus/src/line_diff_output.rs:33-42 carries each side's `number` on LineSide, so the numbers exist per entry and per side, including the absent side of an added or removed line. The two activity descriptions were read at capture time in the committed .workUnit.jsonl.
  - **One position-to-line-number relation, three answers using it** · decision `obvious-essential` · execution `agent-trivial`
    - _The plan forbids a second reckoning for the two reading answers and says nothing for expand-at-a-line._
    - **Context**:
      ***Re-orienting***: Reading and expanding both cross between entry positions and line numbers.
      ***Catch-up***:
      - The engine hands over entries in display order, plus the positions that changed.
      - Folding plans rows by position; apps ask and answer in each side's line numbers.
      - Three new answers cross that boundary, two inward and one outward.
      - A good outcome: one relation, used three ways.
    - **Key points**:
      - `fold_planning.rs` keys folds by entry position; `LineDiff` reports changed positions the same way.
      - REQT-f2affyt2h2 and REQT-hsef5r7c4z want runs in each side's line numbers, translating outward.
      - REQT-g86vdmyyp9 wants a named line id resolved to the fold hiding it, translating inward.
      - Activity WORK:activity-pktstta3q3 forbids a second reckoning for the outward pair; activity WORK:activity-bx7yxakmx2 sets no such boundary for the inward one.
      - The failure is two copies drifting: a run reported at one numbering, a named line resolved at another.
    - **Resolution points**:
      - `name-the-relation-once`: add to activity WORK:activity-bx7yxakmx2's description that resolving a named line to its fold reads the same position-and-line-number relation the reading answers report through, rather than its own. The expectation is which relation is authoritative, not how the worker builds it.
    - **Substance**: Contract correctness: two independent translations can disagree, and the disagreement shows as an app expanding a line the crate just reported hidden, or not expanding one it did. The criteria would not catch it, since each answer is individually right.
    - **Actionability**: One sentence added to an existing activity description closes it; the relation itself is the worker's to shape.
    - **Specificity** `under-design`: The design material is the two activity descriptions. One states the boundary, the other leaves it open, so the gap is an expectation missing from prescribed text rather than technique being over-specified.
  - **Response**: Activity WORK:activity-bx7yxakmx2's description now says that resolving a named line to its fold reads the same relation between entry positions and each side's line numbers the reading answers report through, rather than a second one of its own. The criteria are untouched: under the cheapest-sufficient ladder the activity is the right rung, and a criterion here would name a mechanism rather than an outcome.




### Catch the records up to the delivered code
<a id="work:activity-s1m3wbpec7"></a>
`WORK:activity-s1m3wbpec7`

Once the code carries the new names and arrangement, bring the project's records to what the crate now is: every architecture record this unit scopes, the requirements' file list matching the crate's modules, and the project glossary carrying entries for the live diff and its hooks. Two of those records change rather than simply losing text: the viewer's expanded-fold responsibility moves to the live diff's record, and the hand-off keeps its identity while its caller becomes the live diff. The glossary's fold reset trigger entry retires here, with the code that gave it its meaning.








## Acceptance Criteria

- [ ] The engine's alignment output and the app-held live state read as two different things everywhere a consumer meets them: the crate's exported names, the module filenames, and the project's records. · maturity `DREAMED`

- [ ] No viewer owns anything the live diff owns: no prop, no state, and no engine call reachable through a mounted viewer. · maturity `DREAMED`

- [ ] An app drives a mounted viewer through the live diff alone, naming lines and never folds, without reaching past what the crate exports. · maturity `DREAMED`

- [ ] Every behavior the in-scope requirements name is exercised by a test driving it the way an app would, through the hooks and the viewer rather than through the crate's internals. · maturity `DREAMED`

- [ ] Nothing left in the crate, its tests, its example, or the project's records still presents the viewer as the owner of fold state. · maturity `DREAMED`




## Sign-Offs

### Pre-Work

- **Architect**: state `approved` · 2026-09-19
  - Scope is bounded to one crate and the records describing it. Every architecture record the work changes is now named on a work_ref, and the three new elements have records of their own, so nothing the plan moves is missing from the architecture. Requirements coverage holds: the rename needs none, and every behavioral change in scope traces to an area 4.0 child or to one of the two amended area 2.2 children. Both findings raised here are addressed.

- **Code Whisperer**: state `approved` · 2026-09-19
  - One structural expectation was missing and is now named: which relation between entry positions and line numbers is authoritative, so the three new answers cannot each grow their own. The advisory the expansion variety's work-ready tier asks for is WORK:advice-bdghwgkv53. Examined and found sound as written: the no-write-during-render discipline, which tests encode and the plan carries forward with its citation; the module named for the retiring trigger, which the plan empties before retiring; and `Diff` as a name, which is the crate's own domain noun rather than a generic word a glossary would have to define. Architect sign-off was approved first, as this gate requires.

- **Rust Tester**: state `approved` · 2026-09-19
  - The testing approach fits the domain. Coverage is bound by criterion WORK:crit-zwcqtj9zdc, which asks that every in-scope behavior be driven the way an app would, and by WORK:crit-d5vagb7xpe, which guards the one case a green build would not catch: an engine test quietly dropped during the rename. The focus files now name every component test file the viewer reshape reaches, so the test work is sized right at the start. One advice was dismissed as craft a worker clears unaided; its one durable line, the two-viewer mounting already built, sits in the pass's coverage trail.



### Post-Work

- **Architect**: state `findings-review` · audit `AUDIT:session-dw1td85d9n`

- **Code Whisperer**: state `findings-review` · audit `AUDIT:session-dw1td85d9n`

- **Rust Tester**: state `findings-review` · audit `AUDIT:session-dw1td85d9n`





## Advice (work-unit scope)

- _Architect_ · state `addressed` · type `documentary` · priority `crit`: The live diff, its hook pair, and the options value they take have no structural architecture record. They exist only inside the `detail` narrative of ARCH:dcisn-xgpr1asvyn, which is a decision record rather than an element record. The expansion variety's work-ready tier requires the ARCH records for new architectural elements to be in place and resolved, so this unit cannot exit pre-work as it stands. ARCH-phde62wxvn's resolution, that the folding choices are fields of the options value, likewise lands in no element record. `WORK:advice-56drzh115e`
  - **evidence**: Read all 22 records in .project/architecture/dioxus-diff-viewer.arch.jsonl: 11 software_object records, none naming the live diff, the hooks, or their options. ARCH:dcisn-xgpr1asvyn's detail (`dioxus-diff-viewer.arch.jsonl`) describes what the live state owns and answers, which is element substance sitting in a decision. The tier requirement is REQT-tpsd94jwgp at coder/coder.codingDiscipline.reqts.md:120.
  - **The live diff and its hooks have no architecture record** · decision `obvious-essential` · execution `agent-nontrivial`
    - _New elements described only inside a decision's narrative; work-ready wants resolved element records for them._
    - **Context**:
      ***Re-orienting***: The arch file carries eleven element records; the new live state is in none.
      ***Catch-up***:
      - It holds one structural record per component, object and interaction of the port.
      - This unit adds a live diff an app holds, two hooks, and an options value.
      - Its variety gates work-ready on resolved records for new elements.
      - A good outcome: the new elements read beside the old.
    - **Key points**:
      - The live diff, `use_diff` and `use_diff_with`, and the options value exist only inside ARCH:dcisn-xgpr1asvyn's `detail` narrative.
      - A decision says why a choice was made; it is not where a reader looks for what the system is made of.
      - ARCH-phde62wxvn's resolution, that the folding choices are fields of the options value, also lands in no element record.
      - REQT-tpsd94jwgp gates this unit's exit on those records being in place and resolved rather than candidate.
    - **Resolution points**:
      - `write-the-element-records`: author a `software_object` for the live diff and one for its options value under ARCH-m4dkxzw6hh, plus the hook pair as how an app reaches them, at gesture altitude with `supports_requirements` pointing at area 4.0's children; add a `work_ref` per new record. Drafted under `#streaming-dreaming` at DREAMED, so the stakeholder steers from the draft rather than before it.
    - **Substance**: Contract correctness and requirements coverage both turn on this: area 4.0's children specify behavior for elements the architecture never names, so a later reader reconstructing the arrangement finds a viewer, an engine, and an unexplained hook. It also blocks the gate outright.
    - **Actionability**: A workable direction exists and its inputs are in hand: the decision's detail already states what the live state owns, what it answers, and what it takes.
    - **Specificity** `under-design`: The design material is the architecture record set for the port. It specifies every existing element and omits three new ones, so the omission is a gap in prescribed material rather than an excess of it.
  - **Response**: Three element records written at DREAMED in dioxus-diff-viewer.arch.jsonl, all under ARCH-m4dkxzw6hh as the stakeholder's default left standing: ARCH-8tce9rry3b (Diff), ARCH-smcbvznq0z (DiffOptions) and ARCH-87bw15t11s (UseDiffHooks). ARCH-phde62wxvn's resolution now lands in ARCH-smcbvznq0z's discussion. Each is scoped by a work_ref: WORK:ref-h7mxwn1m3b, WORK:ref-mhwzeafxwm and WORK:ref-kmcqhfzqjb.

- _Architect_ · state `addressed` · type `documentary` · priority `high`: Four existing architecture records change under this plan and none is on a work_ref. ARCH-m4dkxzw6hh (DiffViewer) lists `Expanded-fold state and its reset trigger` among its responsibilities and `Render split and inline views from a LineDiff` among its activities; both go. ARCH-atczcqvdsz (Line diff hand-off) names the viewer as the actor that asks the engine and carries that direction in its edges and payload; the live diff takes that over. ARCH-n7wmjnmt65 (LineDiffOptions) renames with the engine under this session's assent. ARCH-jf3s5npp9s (LineDiffEngine) is the component whose modules and filenames the rename reaches. The scope was drawn from the records the two decisions name by id, and those decisions name only ARCH-exgfx6rwdt. `WORK:advice-7mqn2rv5t1`
  - **evidence**: Read every component, software_object and interaction record in .project/architecture/dioxus-diff-viewer.arch.jsonl at capture time. ARCH-m4dkxzw6hh's responsibilities and activities arrays, and ARCH-atczcqvdsz's actors, payload and edges arrays, each name behavior this plan moves. The work unit's four ARCH work_refs are ARCH-y9545npjzg, ARCH-exgfx6rwdt and the two decisions; the four named here are absent from that set.
  - **Four architecture records change and none is in scope** · decision `obvious-essential` · execution `agent-nontrivial`
    - _The viewer, the hand-off, the engine's options and the engine component all move; the scope refs name none of them._
    - **Context**:
      ***Re-orienting***: Four arch records describe behavior this plan moves, and none is in scope.
      ***Catch-up***:
      - The port's arch file records the viewer, the engine, their objects and the call between them.
      - This unit moves the engine call and the fold state out of the viewer.
      - Scope refs were drawn from the ids the two decisions name.
      - A good outcome: every record the work changes is scoped.
    - **Key points**:
      - ARCH-m4dkxzw6hh (DiffViewer) carries `Expanded-fold state and its reset trigger` as a responsibility and `Render split and inline views from a LineDiff` as an activity; the work removes the first and renames the second's subject.
      - ARCH-atczcqvdsz (Line diff hand-off) names the viewer as the actor that asks the engine, in its actors, payload and edges alike; the live diff takes that over.
      - ARCH-n7wmjnmt65 (LineDiffOptions) renames with the engine under this session's assent.
      - ARCH-jf3s5npp9s (LineDiffEngine) is the component whose modules and filenames the rename reaches.
    - **Resolution points**:
      - `scope-and-evolve-the-four`: add a `work_ref` for each of the four with a two-to-three-word clue, so activity WORK:activity-s1m3wbpec7's records catch-up has them in view, and evolve each record's text with the code. ARCH-m4dkxzw6hh's fold-state responsibility moves to the live diff's new record rather than simply going, and ARCH-atczcqvdsz's actors and edges turn toward the live diff.
    - **Substance**: A scope reference set that misses four affected records leaves the post-work reviewer checking the wrong list, and leaves the architecture describing an arrangement the code no longer has. Maintainability rather than correctness, but it compounds: the next unit reads the stale records as current.
    - **Actionability**: Direct: the four records are identified and the plan already has a records catch-up activity for them to land in.
    - **Specificity** `under-design`: The design material is the work unit's scope representation, which prescribes which records the work touches. It under-names them, so a worker following it faithfully still leaves architecture stale.
  - **Response**: All four scoped: WORK:ref-63m40t6nm5 (ARCH-m4dkxzw6hh), WORK:ref-mfze15bbty (ARCH-atczcqvdsz), WORK:ref-0smta5tf2b (ARCH-n7wmjnmt65), WORK:ref-tcz69mprae (ARCH-jf3s5npp9s). Activity WORK:activity-s1m3wbpec7 now selects the records by rule rather than listing two ids, and names the two evolutions this dialogue settled: the viewer's expanded-fold responsibility moves to ARCH-8tce9rry3b, and ARCH-atczcqvdsz keeps its identity while its caller becomes the live diff. The records' own text changes with the code, in that activity.
  - depends on `WORK:advice-56drzh115e`: New records may absorb the viewer's fold-state lane

- _Rust Tester_ · state `dismissed` · type `recommendation` · priority `med`: The reading answers and the three fold actions return values rather than rows, and the component test harness reads rendered rows only. `MountedApp` mounts an app and exposes its rendered tree; nothing carries a value back out of a component to the test. Since a live diff is built by a hook, it exists only inside a mounted component, so every test for what a live diff answers needs a way to get that answer out. Criterion WORK:crit-zwcqtj9zdc requires exactly those tests to drive the crate the way an app would rather than through its internals, so the gap sits between the criterion and the harness that would satisfy it. `WORK:advice-qf0vfmktm4`
  - **evidence**: rust-dioxus/tests/diff_viewer/mounted_app.rs:17-60 shows MountedApp holding a VirtualDom and a RenderedTree, with `tree()` as the only reader; there is no channel for a value. rust-dioxus/tests/diff_viewer/folding.rs:202-210 shows the nearest prior art, an app component holding a handle and a button that acts on it, asserted through rows afterwards. A two-viewer mounting already exists from the theming work at commit ee5ea56, which is the shape REQT-dxbaat20ja needs.
  - **The new answers have no way out of a mounted component** · decision `advisory` · execution `not-needed`
    - _A live diff lives inside a hook; the harness reads rendered rows and carries no value back to the test._
    - **Context**:
      ***Re-orienting***: Component tests read rendered rows; the new answers are returned values.
      ***Catch-up***:
      - `MountedApp` mounts an app, settles it, and exposes the rendered tree.
      - A live diff is built by a hook, so it lives inside a mounted component.
      - Its reading answers and fold actions return values rather than changing rows.
      - A good outcome: a test reads an answer, not the internals.
    - **Key points**:
      - `mounted_app.rs:17-60` holds a VirtualDom and a RenderedTree, with `tree()` as the only reader.
      - Change runs and hidden runs are data an app reads, and asserting them through rendered rows would test the rows instead.
      - The nearest prior art is `folding.rs:202-210`: an app component holding a handle, a button that acts on it, rows asserted after.
      - REQT-dxbaat20ja needs two viewers over one live diff, and the theming work already mounted two viewers at commit `ee5ea56`.
      - Criterion WORK:crit-zwcqtj9zdc asks for exactly these tests, driven as an app would.
    - **Resolution points**:
      - `carry-the-hazard-to-the-worker`: leave this open as the worker's context rather than settling the harness now. It names three things the worker would otherwise rediscover: that a hook-built value needs a component to live in, that rows are the only channel the harness has today, and the two pieces of prior art closest to what the new tests need.
    - **Substance**: Test coverage: the criterion binds tests for every in-scope behavior, and the behaviors that return values have no established way to be observed. A worker who does not see this early tends to assert through rows, which couples the test to rendering and loses resistance to refactoring.
    - **Actionability**: Actionable as context rather than as an edit: how the value leaves the component is the worker's craft, and pre-work entrusts it.
    - **Specificity** `calibrated`: The design material is the test approach, which the plan leaves to the worker by intent. Naming the hazard and the prior art adds no prescription, so the material stays calibrated.
  - **Response**: Dismissed as craft a practical worker clears unaided. Three factors settled it: WORK:file-3g4hd4ep2n already names mounted_app.rs with a relevance line saying the reading and fold-action tests build on it, so the plan carried the harness fact before this advice restated it; that a hook runs inside a component is Dioxus knowledge the seated lenses bring, not a hazard of this work; and a worker opening the named file meets `tree()` as its only reader on sight, which is inductive discovery rather than a gap. The one line that did not clear that bar, the two-viewer mounting already built at commit ee5ea56, moved to WORK:advice-f08kwnj9z8's coverage trail, where it costs nothing to find and claims no TODO.

- _Rust Tester_ · state `addressed` · type `documentary` · priority `low`: The focus files understate how far the viewer reshape reaches into the component test suite. Seven of the ten files under rust-dioxus/tests/diff_viewer/ construct a viewer through props this unit removes, and the work unit names only folding.rs and mounted_app.rs among them. A worker reading the focus files would size the test work at one file and meet six more. `WORK:advice-tqbxrp5a7p`
  - **evidence**: A capture-time grep for the retiring prop names over rust-dioxus/tests/diff_viewer/*.rs matched content_rendering.rs, folding.rs, line_selection.rs, reference_cases.rs, styling_hooks.rs, theming.rs and views.rs; rendered_tree.rs, mounted_app.rs and main.rs did not match. The unit's work_file records name rust-dioxus/tests/diff_viewer/folding.rs and rust-dioxus/tests/diff_viewer/mounted_app.rs only.
  - **The focus files understate the component test reach** · decision `obvious-essential` · execution `agent-trivial`
    - _Seven of ten component test files build a viewer through the removed props; the unit names one of them._
    - **Context**:
      ***Re-orienting***: The focus files name one component test file; seven of ten are affected.
      ***Catch-up***:
      - `work_file` records tell a worker which files to open before starting.
      - The viewer reshape removes eight props that component tests pass today.
      - Seven of the ten files under tests/diff_viewer/ construct a viewer that way.
      - A good outcome: the file list matches the reach.
    - **Key points**:
      - Affected at the capture-time check: content_rendering.rs, folding.rs, line_selection.rs, reference_cases.rs, styling_hooks.rs, theming.rs, views.rs.
      - The unit's focus files name folding.rs and mounted_app.rs, and mounted_app.rs is one of the three that do not build a viewer.
      - A worker sizing the test work from the focus files meets six more files than the list showed.
    - **Resolution points**:
      - `name-the-affected-files`: add a `work_file` record for each affected component test file not already named, with a relevance line saying what that file covers and why the reshape reaches it. The two already named keep their entries.
    - **Substance**: Maintainability of the plan rather than of the code: nothing is lost, but the reshape is sized wrong at the start, and mis-sized work is where the pressure to cut tests comes from.
    - **Actionability**: Direct: the affected files are named and the records are additive.
    - **Specificity** `under-design`: The design material is the focus file list, which exists to be exhaustive about what the worker opens. Naming fewer files than the work touches is a gap in it.
  - **Response**: Six focus-file records added, one per affected component test file not already named: WORK:file-rd48342t9m (views.rs), WORK:file-t7cmkd9n1h (content_rendering.rs), WORK:file-v81jc6xs9c (theming.rs), WORK:file-30ge1mxghq (line_selection.rs), WORK:file-1h44rej5j7 (styling_hooks.rs) and WORK:file-ydp6wbya56 (reference_cases.rs). Each says what its file covers and why the reshape reaches it. A single directory-level record was weighed and set aside: work_file is file-granular by schema, and a per-file relevance line is what a directory entry could not carry.

- _Work Planner_ · state `addressed` · type `procedural` · priority `low`: Pre-work session record: what the three reviewers developed, resolved against the sources, or set aside. No activity description was changed by activity development; each was examined and left as written, and the two edits this session made to activity text came out of finding resolution instead.

**Resolved against the sources**
- `Diff` as the live state's name reads as the crate's concrete domain noun rather than an abstract or metaphor label, so the Code Whisperer's rename-before-defining rule does not fire and activity WORK:activity-s1m3wbpec7's glossary entry is not a meaning imputed to a generic word. ARCH:dcisn-x8fa9g2hm8's detail (`dioxus-diff-viewer.arch.jsonl`) shows the name was steered to at draft maturity beside `DiffAnalysis` and `DiffViewer`, which read as one family.
- The engine rename needs no requirement of its own: a rename changes no behavior, and the new options value is already required by REQT-7tk9vxv9wd (Building a live diff), which names every field it carries. Checked against the full area 4.0 text in `.project/requirements/diff-viewer.reqts.md:139-158`.
- The no-write-during-render discipline survives the move. `rust-dioxus/src/fold_reset_trigger.rs:70-86` compares a basis on read rather than writing during rendering, and `rust-dioxus/tests/diff_viewer/folding.rs:181-201` encodes the property as tests, so it is a contract under the Contract Verification Gate rather than a comment. Activity WORK:activity-9b867dj9sw names it and cites its line.
- The module named for the retiring trigger does not outlive it: activity WORK:activity-9b867dj9sw moves the expanded folds out of it and activity WORK:activity-gx60bkawf4 retires the trigger, leaving nothing behind in `rust-dioxus/src/fold_reset_trigger.rs`.
- Every `work_ref.file` value on this unit is a bare filename resolving to exactly one file, checked against the repository at capture time: `dioxus-diff-viewer.arch.jsonl`, `diff-viewer.reqts.jsonl`, `20260915.diff-viewer-component.workUnit.jsonl`, `rust-dioxus-port.workEpic.jsonl`. Correct under the pointer rubric's first verdict.
- The scope boundary states what is out: ARCH-m4dkxzw6hh carries the per-region close and the reset boolean as recorded exclusions, and activity WORK:activity-bx7yxakmx2 names the first of them.

**Prior art worth knowing**
- Two viewers over one live diff, which REQT-dxbaat20ja needs, already has its mounting shape: the theming work built a two-viewer test at commit `ee5ea56`. Carried here rather than as an open item, since it saves a search through an 815-line file and asks nothing of the worker.

**Set aside as unanchored**
- Whether the engine's function and output stay publicly exported once an app reaches them through the live diff. Nothing requires either way; it is the worker's call.
- How the fold basis reckons a reset once the trigger's counter is gone. Craft, entrusted.
- Whether the live diff's module will pass the size target `#prevent-megacode-files` sets. The worker carries that principle and splits as it writes.
- How a value reaches a test out of a mounted component. Raised as advice WORK:advice-qf0vfmktm4 and dismissed: a worker opening `rust-dioxus/tests/diff_viewer/mounted_app.rs`, which the focus files already name, meets the limit on sight. `WORK:advice-f08kwnj9z8`
  - **evidence**: Each bullet above carries the citation for its own check, read at capture time. The activity descriptions were read from the committed .workUnit.jsonl; no narrower reading of any of the six was found that would satisfy its words and miss its intent, so none was developed.
  - **Pre-work coverage trail for this work unit** · decision `advisory` · execution `not-needed`
    - _What the three reviewers resolved against the sources and what they set aside, so neither is invisible._
    - **Context**:
      ***Re-orienting***: The pre-work pass's own coverage trail for this work unit.
      ***Catch-up***:
      - Pre-work examines each activity and resolves what it can against the sources.
      - Candidates that settle get one cited line here rather than their own record.
      - Candidates with no anchor in the material get one line too.
      - A good outcome: nothing the pass considered is invisible.
    - **Key points**:
      - Six candidates resolved against sources read at capture time, each with its citation.
      - Three set aside as unanchored: two craft calls and one the worker's own principle covers.
      - No activity description was developed; all six were examined and left as written.
    - **Resolution points**:
      - `keep-the-trail`: the record is the deliverable; it needs no change beyond sealing.
    - **Substance**: No impact on coverage or correctness; it is the visibility instrument for candidates that did not become findings.
    - **Actionability**: Complete as written.
    - **Specificity** `not-design-related`: Not design material; a record of what the pass examined.
  - **Response**: The record is the deliverable and needs no change beyond this seal. It gained the two-viewer prior art and the dismissal of WORK:advice-qf0vfmktm4 as the review ran.




## Audit Trail

### post-work

- **session** `AUDIT:session-dw1td85d9n`: WORK-98bxwcd28f (Programmatic control of the diff viewer): the delivered crate at fbe9f74, its tests, its example, and the project records at 2c838d2 · audit `post-work` · 2026-09-19 · status `in-progress` · theme `Deliverable verification against the six acceptance criteria and the in-scope requirements, then architectural capture, structural quality, and test quality over the changeset` · expertise `Work Planner`, `Architect`, `Code Whisperer`, `Rust Tester`, `Rust Coder`, `Dioxus Coder`, `Dioxus UI Patterns`
  - Independent analysis complete; findings filed before dialogue.

**Evaluations run, with what the evidence showed**
- Engine ground-coverage (WORK:crit-d5vagb7xpe) verified independently rather than trusted: `tests/diff_analysis_engine.rs` re-derived from `git show ade636d:rust-dioxus/tests/line_diff_engine.rs` through the rename substitutions plus rustfmt differs from the delivered file on exactly two prose lines (`:1` module doc, `:366` ARCH label). No case dropped or weakened.
- Component test counts per file at 3fd4a83 against fbe9f74: every file holds the same count or more, folding.rs 12 to 20 and the new reading_a_diff.rs at 8, total 58 to 74. The deleted `src/fold_reset_trigger.rs` carried no inline tests.
- Build state at fbe9f74: 28 engine tests, 74 component tests, `cargo clippy --all-targets` clean, `cargo fmt --check` clean.
- Provisional-identifier sweep (REQT-rr1e0zsbcj): `_u_[0-9a-hjkmnp-tv-z]{3}` over `rust-dioxus/{src,tests,examples}` and the unit's own records returned no matches.
- Pointer rubric over the unit's four `work_ref.file` values: each bare filename resolves to exactly one file in the repository.
- Glossary sweep over the changeset: the prohibited and avoid forms return only legitimate uses, `standing` as the ordinary verb (styling_hooks.rs:40, fold_planning.rs:1, diff.rs:150) and `edge` in the boundary sense (theming.rs:317-320); no work-process id (`WORK:`, `WORK-`) appears in any source comment.
- Rusty's post-work lenses 2 to 5: no `#[allow(...)]`, no `unsafe`, no `async` anywhere in the crate; the new and renamed filenames each name a domain concept with no grab-bag or insider acronym.
- Beacon, Logan, Appshell Expert and Forms Expert were seated and found no surface, as in the worker's own reports: the crate emits no log event and opens no span, and `Cargo.toml` names neither appshell nor `rust-json-schema-forms`.
- Worker report claims cross-checked and true, with one understatement: WORK:report-r1mjsy8vam says the `live diff|live state` search returns "only the two older decisions"; it returns three (ARCH:dcisn-wma0h4mndz, ARCH:dcisn-x8fa9g2hm8, ARCH:dcisn-xgpr1asvyn). Carried inside AUDIT:finding-jvj8fbh7pc rather than as a finding of its own.

**Set aside as unanchored**
- Whether `expand_all_folds()` and `reset_folds()` should skip the signal write when nothing would change; both act on an app's call rather than in a render path, and the cost is one re-render.
- Whether `hidden_line_runs()` deserves to be a memo rather than recomputing on each call; the requirement wants it current as of the asking either way.

**Second pass: the two gate audits (F12 to F19)**

The Code Whisperer's post-work gate 1 triggered the reqm traceability audit over this changeset, and the Architect ran Post-Work Architectural Capture over the related-architecture-set. That set is one file, `.project/architecture/dioxus-diff-viewer.arch.jsonl`: it holds every ARCH record the unit declares, the crate is the only module the implementation touched, and expansion through surrogate links, `mod_id` parentage and FK references adds no further file, since the crate has no co-located arch and no sub-arch exists.

- Traceability reverse check over the in-scope requirements: every one of REQT-7tk9vxv9wd, REQT-m776z5vdhe, REQT-dxbaat20ja, REQT-f2affyt2h2, REQT-hsef5r7c4z, REQT-g86vdmyyp9, REQT-h8rxvhpj9g, REQT-ps5zx85jvc, REQT-v748c7mjr6 and REQT-869jyzdes7 carries at least one annotation in the delivered crate.
- Traceability forward check over the thirteen changed source files: no annotation names a requirement that no longer exists, and every id annotated under `rust-dioxus/src/` resolves to a record in the requirements file.
- Architectural sync over the three surfaces: the DiffViewer record's activities and responsibilities no longer claim fold state, ARCH-atczcqvdsz's participants match the engine call at diff.rs:239-246, and ARCH-8tce9rry3b lists the two answers and three fold actions the crate delivers.
- The file's one open_question, on whether the folding choices belong to the diff or the viewer, is already resolved onto the diff, and the delivered `DiffOptions` carries both folding fields, so the resolution holds against the implementation.

- **finding** `AUDIT:finding-jvj8fbh7pc` (F1) · type `documentary` · severity `medium` · priority `high` · status `fixed` · reviewer _Architect_ · ARCH:dcisn-wma0h4mndz and ARCH:dcisn-xgpr1asvyn (decision records, in .project/architecture/dioxus-diff-viewer.arch.jsonl), each in the replacement span of its `detail` field
  - Two decision records state where the delivered code lives and name a file that does not exist. Each cites `rust-dioxus/src/live_diff.rs`, and the module was renamed to `rust-dioxus/src/diff.rs` at commit 82679c3, inside this same unit. Both citations sit in the replacement text after a strikeout, which is the span that asserts what the code carries now rather than the pre-decision snapshot the strikeout form deliberately preserves. A reader following either citation finds nothing. Criterion WORK:crit-skf7jygyyz asks that the engine's output and the app-held state read as two different things in the project's records as well as the crate, and these two records still point at the retired spelling.
  - **evidence**: Read both records through `toon read` on .project/architecture/dioxus-diff-viewer.arch.jsonl at the post-delivery state. ARCH:dcisn-wma0h4mndz's `detail` ends "...the viewer builds split and inline views from the plan (`rust-dioxus/src/live_diff.rs`, `rust-dioxus/src/diff_viewer.rs`)"; ARCH:dcisn-xgpr1asvyn's `detail` ends "The code carries this arrangement: `rust-dioxus/src/live_diff.rs` holds the live diff and the hook pair". `ls rust-dioxus/src/` lists diff.rs and no live_diff.rs, and `git log -S` places the rename at 82679c3. A `toon search` for `live diff|live state` over .project/architecture and .project/requirements returns three decisions, not the two WORK:report-r1mjsy8vam names; the third is ARCH:dcisn-x8fa9g2hm8, whose own text carries no dead path.
  - **Two decisions cite a module path the rename retired** · decision `obvious-essential` · execution `agent-trivial`
    - _Both decisions say the code lives in `live_diff.rs`; the module is `diff.rs` since this unit's own commit._
    - **Context**:
      ***Re-orienting***: Two decisions say where the delivered code lives, and the module moved.
      ***Catch-up***:
      - The arch file carries five decisions behind the port's shape.
      - Two were repaired with strikeout once the code landed.
      - Each replacement span names the module holding the diff.
      - A good outcome: the citation opens the file it names.
    - **Key points**:
      - `ARCH:dcisn-xgpr1asvyn` ends "The code carries this arrangement: `rust-dioxus/src/live_diff.rs` holds the live diff and the hook pair".
      - `ARCH:dcisn-wma0h4mndz` ends "...from the plan (`rust-dioxus/src/live_diff.rs`, `rust-dioxus/src/diff_viewer.rs`)".
      - The module became `rust-dioxus/src/diff.rs` at 82679c3, inside this unit.
      - Both dead paths sit after the strikeout, in the span asserting what the code carries now, not in the background snapshot the form preserves on purpose.
      - `WORK:crit-skf7jygyyz` reaches the project's records, so this is the criterion's own gap.
    - **Resolution points**:
      - `strike-the-retired-path`: strike each dead path and follow it with the delivered one, leaving every surrounding word alone. In `ARCH:dcisn-xgpr1asvyn`: "The code carries this arrangement: ~~`rust-dioxus/src/live_diff.rs`~~ `rust-dioxus/src/diff.rs` holds the live diff and the hook pair, and ..." In `ARCH:dcisn-wma0h4mndz`: "...from the plan (~~`rust-dioxus/src/live_diff.rs`~~ `rust-dioxus/src/diff.rs`, `rust-dioxus/src/diff_viewer.rs`)." A module rename is an obsolete term, which `#strikeout-correction` keeps visible in a decision record so a reader carrying either vocabulary can follow it; the same form the worker already used for `~~LineDiff~~ DiffAnalysis` in these records. The retired word "live" around the path stays unstruck, as steering settled.
    - **Substance**: Maintainability: a reader following either citation to understand the arrangement finds no file, and the record that tells them where to look is the one the next unit reads first.
    - **Actionability**: Direct: both spans are quoted with their replacement, and the form matches what these two records already carry.
    - **Specificity** `under-design`: The design material is the architecture capture of the delivered arrangement, which exists to say where the code is. A path naming no file is a gap in it.
  - **resolution**: Both decisions now strike the retired `rust-dioxus/src/live_diff.rs` and follow it with `rust-dioxus/src/diff.rs`, leaving every surrounding word alone, including the retired word live. The background snapshot in ARCH:dcisn-xgpr1asvyn, which names fold_reset_trigger.rs on purpose, is untouched.

- **finding** `AUDIT:finding-gz4xv6c302` (F2) · type `maintainability-concern` · severity `medium` · priority `high` · status `fixed` · reviewer _Code Whisperer_ · `HiddenLines` in rust-dioxus/src/consumer_callbacks.rs:53-60 against `Diff::hidden_line_runs` in ~~rust-dioxus/src/diff.rs:97-108~~ rust-dioxus/src/diff.rs:59-75, with `side_run` in rust-dioxus/src/entry_line_numbers.rs:47
  - One spelling carries two semantics at module-visible scope. `HiddenLines` is the fold-row renderer's payload: one fold row's hidden-line count with the first hidden line's two numbers. `hidden_line_runs()` is an app-facing answer: every fold's hidden run, each as the line numbers it spans. Both are exported from the crate root and both read as "hidden lines", which is what the module-visibility exclusivity rule forbids. The worker recorded the pair twice as a post-work reader's call (WORK:report-ebyt7npy4m and WORK:report-bgaz8sjtnj) rather than settling it. A second, file-local instance travels with it: `side_run` names a line-number range a "run" where the crate's own vocabulary reserves "run" for the entries and "span" for the numbers (ARCH-hrwskjav44).
  - **evidence**: Read rust-dioxus/src/consumer_callbacks.rs:48-60 and ~~rust-dioxus/src/diff.rs:92-108~~ rust-dioxus/src/diff.rs:45-75; both names reach an app through rust-dioxus/src/lib.rs:22 and :23. Checked the neighbouring pair first and found it sound: `changed_line_runs()` returning `LineNumberSpan` is not one semantic under two spellings, because ARCH-hrwskjav44 states the distinction, "the line numbers one run of entries spans, rather than the run itself", and REQT-f2affyt2h2 uses both words that way in one sentence. `side_run` at rust-dioxus/src/entry_line_numbers.rs:47 is `fn`-private, so the module-visibility rule does not reach it and only the vocabulary consistency does.

Corrected at resolution, from sources re-read at fbe9f74. This record was filed with two claims it did not hold. It cited `hidden_line_runs` at diff.rs:97-108, where the delivered file carries it at :64-75 with its doc from :59; the wrong span was noticed on opening the file to draft the rename. It also said each name is its own requirement's wording, so that a rename moves a record too; reading REQT-1tdrfvay4q, REQT-hsef5r7c4z and ARCH-jakkqkh6e1 shows none of the three names an identifier. The requirement asks for "the hidden-line count, and the old and new line numbers of the first hidden line" and the architecture record says the same in its own words, so the rename reaches code alone: consumer_callbacks.rs, lib.rs, diff_viewer.rs, row_rendering.rs and tests/diff_viewer/folding.rs, confirmed by `grep -rn 'HiddenLines'` over src, tests and examples.
  - **Hidden lines named twice at module-visible scope** · decision `judgement-needed` · execution `agent-nontrivial`
    - _`HiddenLines` and `hidden_line_runs()` are both exported and both read as hidden lines, meaning two different things._
    - **Context**:
      ***Re-orienting***: Two exported names both read as hidden lines, meaning different things.
      ***Catch-up***:
      - The crate root exports the fold-row renderer's payload and the diff's answers.
      - One is a single fold row's count with its first line's numbers.
      - The other is every fold's hidden run, in line numbers.
      - A good outcome: one spelling, one meaning.
    - **Key points**:
      - `HiddenLines` (consumer_callbacks.rs:53-60) is what a fold-row renderer receives for one row, and it holds no lines: a count, plus the first hidden line's old and new numbers.
      - `hidden_line_runs()` (~~diff.rs:97-108~~ diff.rs:64-75) is what an app asks the diff about every fold at once, and it does report line numbers.
      - Both reach an app through `lib.rs:22` and `:23`, so the module-visibility exclusivity rule binds both.
      - ~~Each name is its own requirement's wording: REQT-1tdrfvay4q for the payload, REQT-hsef5r7c4z for the answer, so a rename moves a record too.~~ Neither REQT-1tdrfvay4q nor ARCH-jakkqkh6e1 names the identifier; both describe the count and the first line's two numbers in their own words, so the rename reaches code alone.
      - The worker named the pair in two reports and left the call here rather than settling it.
      - `side_run` (entry_line_numbers.rs:47) travels with it: a line-number range called a run, where the crate reserves run for entries and span for numbers.
    - **Resolution points**:
      - `name-the-payload-for-its-row`: rename the exported struct to `FoldRowLines`, keeping its three fields, and rename the private helper to `side_span`. `HiddenLines` names one row's hidden lines while the answer names every fold's, so the payload holds the weaker claim to the spelling. Reaches the crate root export, `consumer_callbacks.rs`, `row_rendering.rs`'s call site, `diff_viewer.rs`'s prop type, the `fold_row_renderer` doc, `folding.rs`'s renderer test~~, ARCH-jakkqkh6e1's definition line, and REQT-1tdrfvay4q's wording~~ and no record, since neither the requirement nor the architecture record names the identifier.
      - `keep-both-and-record-the-split`: leave the names and add a glossary TERM for each, stating the granularity that separates them. Demoted: the glossary defines terms, and both of these are the same term at two granularities, so an entry would impute a distinction the spelling does not carry.
    - **Substance**: Maintainability, and the vocabulary an app learns first: a consumer reading `HiddenLines` in a renderer signature and `hidden_line_runs()` on the diff has no way to know they are different granularities of different things until they read both. Contract correctness is untouched; nothing computes wrongly.
    - **Actionability**: A workable direction exists and its reach is enumerated above~~, but it crosses a requirement's own wording, so the trade is the stakeholder's to make~~; it reaches code alone. The trade is still the stakeholder's, because the crate's public vocabulary is what an app meets first.
    - **Specificity** `calibrated`: The design material is the crate's public vocabulary, which the plan left to the worker except where a requirement fixed a word. ~~Two requirements each fixed the same word for a different thing, so the collision is in the prescribed material rather than in the worker's craft.~~ Neither requirement fixed an identifier, so the collision is in the worker's own naming, within material the plan left open.
  - **resolution**: The exported `HiddenLines` is now `FoldRowLines`, keeping its three fields, and the private `side_run` is `side_span`. The rename reached consumer_callbacks.rs, lib.rs, diff_viewer.rs, row_rendering.rs including its local binding, entry_line_numbers.rs and tests/diff_viewer/folding.rs, and no record, since neither REQT-1tdrfvay4q nor ARCH-jakkqkh6e1 names the identifier. The crate is green at 28 engine and 74 component tests, clippy and rustfmt clean, the same counts as before the rename.
Resolution dialogue.

The collision argument rests on a fact neither name states: `HiddenLines` held no lines. Its three fields are a count and the first hidden line's old and new numbers, while `hidden_line_runs()` does report line numbers, as `Vec<LineNumberSpan>`. An app meeting both met the plainer-sounding name on the value carrying a number, and the qualified name on the value carrying lines.

`FoldRowLines` is built from covered vocabulary. The project glossary defines `fold row` as the single row standing in for a run of folded unchanged lines, so the replacement needed no new term.

`keep-both-and-record-the-split` stayed demoted on the Code Whisperer's rename-before-defining rule: a glossary entry for each spelling would impute a distinction the spellings do not carry.

The Key Question asked whether one app would meet both names. The stakeholder assented to the rename, which closed it.

Two claims this record was filed with failed a re-read at fbe9f74 and are struck in its own fields: the cited span for `hidden_line_runs`, and the assertion that a rename moves REQT-1tdrfvay4q and ARCH-jakkqkh6e1. Neither names the identifier.
- **finding** `AUDIT:finding-1c4bch8xby` (F3) · type `traceability-gap` · severity `low` · priority `med` · status `fixed` · reviewer _Rust Tester_ · rust-dioxus/tests/diff_viewer/folding.rs:399-412, where the helper `two_line_folds` sits between the two-viewer fixture and the test it serves
  - The requirement-traceability comment for the two-viewer test landed on a helper instead of the test. Lines 399-401 carry "REQT-dxbaat20ja (One plan per diff) with REQT-869jyzdes7 (Where fold state lives): a fold opened through one viewer opens in the other..." and then line 402 continues "How many viewers still hide the fixture's first two lines", so one doc block describes a test and a counting helper at once and attaches to `fn two_line_folds`. The test it was written for, `a_fold_opened_in_one_viewer_over_one_diff_opens_in_the_other` at :411-412, carries no doc comment at all. Every other test in this file names its requirement directly above `#[test]`, so the two requirements this test is the cover for read as covering a helper.
  - **evidence**: Read rust-dioxus/tests/diff_viewer/folding.rs:396-414 at the delivered state; the four doc lines run 399-402 with `fn two_line_folds` at :403 and the bare `#[test]` at :411. `git log -S "How many viewers still hide" -- rust-dioxus/tests/` places the helper's arrival at ee6a018, this unit's fold-actions commit, so the fusion was introduced here rather than inherited. The sibling test `a_fold_an_app_opens_by_naming_a_line_opens_in_every_viewer` at :447-450 shows the file's own form, its three doc lines sitting directly above `#[test]`.
  - **A test's requirement comment landed on the helper beside it** · decision `obvious-essential` · execution `agent-trivial`
    - _The two-viewer test's REQT trail sits on `two_line_folds`; the test itself carries no comment._
    - **Context**:
      ***Re-orienting***: The two-viewer test's requirement comment sits on a helper.
      ***Catch-up***:
      - Every test in the folding file names its requirements just above `#[test]`.
      - A counting helper arrived beside the two-viewer test this unit added.
      - One doc block now describes both and attaches to the helper.
      - A good outcome: the comment sits on the test it covers.
    - **Key points**:
      - `folding.rs:399-401` names REQT-dxbaat20ja and REQT-869jyzdes7 and describes a fold opening in both viewers.
      - `:402` continues "How many viewers still hide the fixture's first two lines", the helper's own line.
      - `fn two_line_folds` at `:403` takes the whole block; `#[test]` at `:411` takes nothing.
      - `git log -S` puts the helper's arrival at ee6a018, so the fusion is this unit's, not inherited.
      - Two in-scope requirements therefore read as covered by a counting helper.
    - **Resolution points**:
      - `split-the-block-back-apart`: leave `/// How many viewers still hide the fixture's first two lines.` on `two_line_folds`, and move the three REQT lines down to sit directly above `#[test]` at `:411`, matching `a_fold_an_app_opens_by_naming_a_line_opens_in_every_viewer` at `:447-450`.
    - **Substance**: Test coverage as a reader finds it: the test runs and passes either way, but anyone tracing REQT-dxbaat20ja or REQT-869jyzdes7 to their cover lands on a counting helper and has to read on to find the test.
    - **Actionability**: Direct: three lines move down eight lines, nothing else changes.
    - **Specificity** `not-design-related`: The design material is the file's own traceability convention, which every other test in it follows. This is one slip against it rather than a gap in what the plan prescribed.
  - **resolution**: The three REQT lines now sit directly above `#[test]` on `a_fold_opened_in_one_viewer_over_one_diff_opens_in_the_other`, and `two_line_folds` keeps its own line about counting viewers. The file's every-test form is restored; the suite is green at 74 component tests.

- **finding** `AUDIT:finding-8prqcgrsg6` (F4) · type `documentary` · severity `low` · priority `med` · status `fixed` · reviewer _Code Whisperer_ · `ExpandedFolds` in rust-dioxus/src/expanded_folds.rs:24-59: the `basis` field's documentation against what `reset` leaves behind
  - A field's stated invariant does not survive the operation next to it. The `basis` field at :26-27 is documented as "What the expansions below were made under; absent while none are held", and `reset` at :57-59 clears `starts` while leaving `basis` at `Some(..)`, so after any reset the struct holds a basis with no expansions under it. Nothing reads the pair that way today, which is why every test still passes: `is_expanded` at :36-38 needs a matching basis and a present start, and an empty `starts` already answers false. The claim is prose with no encoding behind it, in a 60-line module whose whole job is that pair of fields.
  - **evidence**: Read rust-dioxus/src/expanded_folds.rs:24-59 in full. `reset` touches only `self.starts.clear()`; `expand` at :45-51 is the only writer of `basis`. Checked the consequence through the one reader, `is_expanded` at :36-38, and through `Diff::reset_folds` at ~~rust-dioxus/src/diff.rs:114-117~~ rust-dioxus/src/diff.rs:81-84: the stale basis changes no answer, so the defect is the documentation's, not the behavior's. The module arrived whole in this unit's commit 22ca09b, so the wording is this changeset's rather than carried in.

Corrected at resolution from the file re-read at fbe9f74: this record cited `Diff::reset_folds` at diff.rs:114-117, where the delivered file carries it at :81-84. The same wrong offset appeared in AUDIT:finding-gz4xv6c302 and is struck there too.
  - **A field's stated invariant does not survive reset** · decision `obvious-essential` · execution `agent-trivial`
    - _`basis` is documented as absent while no expansions are held; `reset` clears the starts and leaves it set._
    - **Context**:
      ***Re-orienting***: A field's documented invariant does not survive its neighbour.
      ***Catch-up***:
      - The expanded folds hold a set of starts plus the basis they were made under.
      - The field doc says the basis is absent while no starts are held.
      - Resetting clears the starts and leaves the basis set.
      - A good outcome: the doc says what the state actually is.
    - **Key points**:
      - `expanded_folds.rs:26-27` documents `basis` as "absent while none are held".
      - `reset` at `:57-59` touches only `self.starts.clear()`.
      - `expand` at `:45-51` is the only writer of `basis`, so after a reset it stays at the last expansion's value.
      - No behavior changes: `is_expanded` at `:36-38` needs a present start, and an empty set already answers false.
      - The module arrived whole at 22ca09b, so the wording is this changeset's.
    - **Resolution points**:
      - `say-what-the-field-holds`: rewrite the field doc as "What the expansions in `starts` were made under; absent until the first expansion, and left standing by `reset`." The behavior is right as written; only the claim about it is wrong.
    - **Substance**: Maintainability in a small module: a later reader trusting the stated invariant could add a `basis.is_none()` test as a stand-in for "nothing expanded" and get a wrong answer after any reset.
    - **Actionability**: Direct: one doc line, with the replacement wording above.
    - **Specificity** `not-design-related`: The design material is the module's own documentation of its two-field state, which the plan entrusted to the worker. The claim overstates what the code keeps.
  - **resolution**: The field doc now reads "What the expansions in `starts` were made under; absent until the first expansion, and left standing by `reset`." `reset` is unchanged. The Contract Verification Gate settled the alternative of clearing the basis in `reset`: no test encodes the pair, `is_expanded` is the only reader and needs a present start, and `Diff::reset_folds` is the only caller, so the stated invariant was a convention rather than a contract. Crate green at 28 engine and 74 component tests, clippy and rustfmt clean.

- **finding** `AUDIT:finding-g87b6g5fdk` (F5) · severity `medium` · status `withdrawn` · reviewer _Code Whisperer_ · `FoldBasis` in rust-dioxus/src/expanded_folds.rs:15-21 against the fold boundaries `plan_rows` derives in rust-dioxus/src/fold_planning.rs:43-81
  - A fold is identified by an entry position, and expansions survive any input change the basis does not cover. The basis holds the content identity and the folding configuration alone, so a change of compare method, of inline-change marking, or of line offset leaves expansions standing. If any of those could move which entries count as changed, the fold boundaries would move under a standing set of start positions, and a start held from before would open a different run of lines than the reader asked for.
  - **evidence**: Read the engine's recording path: `EntryBuilder::push` at rust-dioxus/src/diff_analysis_engine.rs:251-256 records a changed position from `entry.change` and `entry.line_ending_change` only, and the four `push_*` constructors at :181-240 set `change` from `similar`'s alignment alone. `options.compare` reaches only `inline_changes` and `whitespace_change` (:219-229) and `options.line_offset` only each side's number (:160-178). Fold boundaries therefore move only when the texts move, which is exactly what `FoldBasis.content_identity` keys on together with `folding`. Not an issue.
  - **resolution**: Withdrawn on the engine's own recording path: `changed_positions` depends on the texts alone, never on the compare method, inline-change marking, or line offset, so the basis covers every input that can move a fold boundary.

- **finding** `AUDIT:finding-txhft8jrc6` (F6) · severity `medium` · status `withdrawn` · reviewer _Dioxus Coder_ · `use_diff_inputs` in rust-dioxus/src/diff.rs:284-302, called from `use_diff_with` at :235
  - The hook writes a signal from a component's render body, which is the framework's best-known hot-loop defect: a write during render marks subscribers dirty, the component re-runs, and the write happens again. `held.set(..)` at :294-300 runs on every render where the caller's texts or options differ from the ones held.
  - **evidence**: Compared against the framework's own mechanism for the same job: `use_reactive` at dioxus-hooks-0.7.9/src/use_reactive.rs:103-116 (the crate resolves dioxus-hooks 0.7.10 per Cargo.lock:462-464) is `use_signal`, a `peek()` comparison, and a `set` only when the dependency differs. `use_diff_inputs` is that shape written out: the `changed` test at :290-293 reads through `held.peek()`, so the calling component never subscribes to the signal it writes, and an unchanged render compares the texts instead of copying them. The write reaches only the memos at :239-265. Not an issue.
  - **resolution**: Withdrawn: the write-during-render is `use_reactive`'s own mechanism, and the `peek()` comparison keeps the writing component off the signal's subscriber list, so no loop is reachable.

- **finding** `AUDIT:finding-ah3k334sgg` (F7) · severity `medium` · status `withdrawn` · reviewer _Rust Coder_ · `Diff::hidden_line_runs` in rust-dioxus/src/diff.rs:97-108, which holds an analysis read guard across a row-plan read
  - The answer takes a read guard on the analysis memo at :98 and then reads the row-plan memo at :99-100, whose own recomputation reads the analysis again (rust-dioxus/src/diff.rs:260-265). A memo that recomputes writes its cached value back, so a re-entrant recomputation under a held guard would be a runtime borrow panic rather than a compile error, and no test drives the case where both memos are dirty at once.
  - **evidence**: Read `Memo::try_read_unchecked` and `Memo::recompute` at dioxus-signals-0.7.9/src/memo.rs:166-195 and :120-139. A read on a dirty memo drops its own borrow before recomputing, and `recompute` writes only when the new value differs. The analysis read at :98 clears the analysis memo's dirty flag first, so by the time the row plan recomputes, its inner read of the analysis is a plain second immutable borrow, which generational-box permits. The same ordering holds in the viewer body (rust-dioxus/src/diff_viewer.rs:98-103) and in `fold_start_hiding` (:170-181), where the analysis guard drops at the end of its own statement. Not an issue.
  - **resolution**: Withdrawn: reading the analysis first cleans that memo, so the row plan's recomputation re-enters it as a plain immutable borrow rather than a write under a held guard. Every site in the crate reads in that order.

- **finding** `AUDIT:finding-3z6wb7fttb` (F8) · severity `low` · status `withdrawn` · reviewer _Rust Tester_ · `DiffOptions` in rust-dioxus/src/diff_options.rs:16-27 against the component tests that build one through `use_diff_with`
  - Two of the five fields an app sets on a diff, `mark_inline_changes` and `line_offset`, are set by no component test. The viewer reshape moved all five from viewer props onto the options value, so the path each field takes to the engine is new, and a field nothing exercises through that path could be dropped on the way without any test noticing.
  - **evidence**: Searched the five field names over rust-dioxus/tests/diff_viewer/ and rust-dioxus/examples/: `compare`, `fold_unchanged_lines` and `surrounding_line_count` are set; the other two are not. Checked whether the reshape lost coverage by searching the same two names over the seven component test files at `git show 3fd4a83`: neither appeared there either, so nothing was lost. The behaviors are covered in the engine suite, where `DiffAnalysisOptions` carries them directly (rust-dioxus/tests/diff_analysis_engine.rs, including `each_side_counts_its_own_lines_from_one_more_than_the_offset` at :488). The remaining risk is `DiffOptions::analysis_options` at rust-dioxus/src/diff_options.rs:52-58 mis-wiring a field, which the three distinct field types make uncompilable rather than silent. Not an issue.
  - **resolution**: Withdrawn: no coverage was lost by the reshape, the engine suite covers both behaviors, and the one new mapping the reshape adds cannot transpose the fields without failing to compile.

- **finding** `AUDIT:finding-7vxgj05905` (F9) · severity `low` · status `omit` · reviewer _Rust Coder_ · `DiffOptions` in rust-dioxus/src/diff_options.rs:15-27, the value every app constructs to build a diff
  - The options value is a public struct with five public fields and no `#[non_exhaustive]`, so a sixth field is a breaking change for any caller that names all five rather than writing `..DiffOptions::default()`. The type is the crate's newest public API and the one most likely to grow, since it is where a further engine or folding choice would land.
  - **evidence**: Read rust-dioxus/src/diff_options.rs:15-48. `Default` covers every field (:29-48), which is what REQT-7tk9vxv9wd asks for, and every construction in the crate, its tests and its example uses the struct-update form: the four sites at rust-dioxus/tests/diff_viewer/folding.rs, reading_a_diff.rs, views.rs and styling_hooks.rs, plus examples/theme_preview.rs:46-52. Nothing in scope asks for evolution guarantees, and the crate is at 0.1.0 (Cargo.toml).
  - **resolution**: Omitted: the tradeoff is real but nothing in this unit's scope raises it, the defaulted construction the requirement mandates is what every call site already uses, and a pre-1.0 crate carries no evolution promise to protect. Worth revisiting when the crate first publishes.

- **finding** `AUDIT:finding-aehn19g65s` (F10) · severity `low` · status `withdrawn` · reviewer _Architect_ · `analyze_diff` and `DiffAnalysis` in the crate root, rust-dioxus/src/lib.rs:24 and :26-29
  - The engine's function and output stay publicly exported although an app now reaches the analysis through the diff it holds. Two exported names for one computation invites a consumer to call the engine directly and plan its own rows, which is the arrangement ARCH:dcisn-xgpr1asvyn moved away from, and no architecture record states the crate's intended public boundary either way.
  - **evidence**: Read rust-dioxus/src/lib.rs:22-33 and confirmed both names are exported, alongside `Diff` and the hook pair. Pre-work put this exact question and set it aside as the worker's own call, one line in WORK:advice-f08kwnj9z8's "Set aside as unanchored" group; the worker then recorded the choice twice with its reason, in WORK:report-50arbnmjqy and WORK:report-ebyt7npy4m. No in-scope requirement speaks to visibility: area 4.0's children (REQT-7tk9vxv9wd, REQT-m776z5vdhe, REQT-dxbaat20ja and the reading and folding groups) state what a diff does, never what the crate hides.
  - **resolution**: Withdrawn: pre-work entrusted the call to the worker and the worker made it with its reason on the record. Re-opening it here would relitigate a closed fork rather than report a gap.

- **finding** `AUDIT:finding-qwx485arfe` (F11) · severity `low` · status `withdrawn` · reviewer _Architect_ · `Diff` (ARCH-8tce9rry3b), a software_object recorded under the DiffViewer component (ARCH-m4dkxzw6hh) in .project/architecture/dioxus-diff-viewer.arch.jsonl
  - The diff an app holds is recorded as an object of the viewer's component, though the app owns it and the viewer only renders it. The same file puts `DiffOptions` and `UseDiffHooks` there too, so the component that owns none of the three carries all of them, and a reader looking for what an app holds finds it filed under what an app mounts.
  - **evidence**: Read the three records and their `component` field in .project/architecture/dioxus-diff-viewer.arch.jsonl; all name ARCH-m4dkxzw6hh. Checked how the placement was reached: pre-work put it as a key question, "whether the live diff belongs under the viewer's component or wants one of its own", and the default under ARCH-m4dkxzw6hh was left standing by the stakeholder, recorded in WORK:report-pk5qj70fb9's Clarifications. The same report records the second half, that ARCH-atczcqvdsz keeps its identity with the diff as its calling participant.
  - **resolution**: Withdrawn: the placement was put to the stakeholder at pre-work as a key question and settled by the default being left standing. Not this pass's to reopen.

- **finding** `AUDIT:finding-04e6rqxr1t` (F12) · severity `medium` · priority `high` · status `pending` · reviewer _REQM_ · The Files section of .project/requirements/diff-viewer.reqts.md, the implementation-file list a traceability pass traverses, rendered from the `file` records in .project/requirements/diff-viewer.reqts.jsonl
  - The requirements' file list omits a file carrying eight requirements' traceability annotations. `rust-dioxus/src/dxdiff.css` holds the annotations for REQT-sc8expw3q8 (Theme selection), REQT-3928hx46s3 (Styling hooks), REQT-78dg0g6a2h (Theme palettes), REQT-8y5fp2aarz (Chip and arrow color), REQT-1accrb4jhp (Named colors), REQT-ey9f1s27r1 (Yielding rules), REQT-aaxrz33x1n (Line numbers under the pointer) and REQT-ef5a9d2paw (Row under the pointer), and no `file` record names it. A traceability pass traverses the files that list names, so a file absent from it is invisible to every such pass and the requirements it carries read as unannotated. This unit's activity WORK:activity-s1m3wbpec7 (Catch the records up to the delivered code) rewrote the list to match the crate, which is where the omission became reachable.
  - **evidence**: toon read of .project/requirements/diff-viewer.reqts.jsonl filtered to type file returns fourteen records, every one a .rs path under rust-dioxus/src/. Scanning rust-dioxus/src/dxdiff.css for requirement ids returns the eight above at the capture-time check, and the file is 104 lines of stylesheet the viewer embeds through include_str at rust-dioxus/src/diff_viewer.rs:21. `git show 208d319 -- requirements/diff-viewer.reqts.md` shows this unit rewriting the list, adding diff.rs, diff_options.rs, entry_line_numbers.rs and expanded_folds.rs while dxdiff.css stayed out.

- **finding** `AUDIT:finding-pb59qwa0sp` (F13) · severity `medium` · priority `high` · status `pending` · reviewer _Architect_ · The name, context, detail and involved notes of ARCH:dcisn-xgpr1asvyn (The app holds the live diff), ARCH:dcisn-wma0h4mndz (Engine separate from component) and ARCH:dcisn-x8fa9g2hm8 (Engine renamed so the viewer can own the word diff) in .project/architecture/dioxus-diff-viewer.arch.jsonl
  - Three decision records still call the app-held state the live diff, the word this unit retired from the crate and the glossary. ARCH:dcisn-xgpr1asvyn carries it in its own name, twice in its detail and twice in its involved notes, including inside the replacement span after the strikeout, which is the text asserting what the code carries now rather than the pre-decision snapshot a strikeout preserves. ARCH:dcisn-wma0h4mndz carries it in the replacement half of its detail and in its involved note. ARCH:dcisn-x8fa9g2hm8 carries the live state in its context, its background impact and its detail. The crate exports `Diff` and `use_diff` and names no module or type live, and the glossary records diff analysis for the engine's output with no term for the app-held state. A reader searching the records for the crate's vocabulary finds a name the crate does not have.
  - **evidence**: toon search for live diff or live state over .project/architecture returns exactly these three decisions at the capture-time check, and all three were read in full from .project/architecture/dioxus-diff-viewer.arch.jsonl. Commit fbe9f74 states the glossary position: No term for the diff an app holds: diff is the crate's own domain noun. AUDIT:finding-jvj8fbh7pc's resolution records that its own repair left every surrounding word alone, including the retired word live, so the term was never dispositioned, only left outside that finding's span. The obsolete-terms clause of PRIN-g4w4dzntxf (Strikeout Correction) has a decision strike the obsolete term and follow it with the current one wherever it appears.

- **finding** `AUDIT:finding-fxbyh8pk6x` (F14) · severity `medium` · priority `high` · status `pending` · reviewer _Architect_ · The Software Objects section of .project/architecture/dioxus-diff-viewer.arch.md, beside ARCH-hrwskjav44 (LineNumberSpan), for the relation implemented at rust-dioxus/src/entry_line_numbers.rs:1-81
  - The architecture names the shape the reading answers come back in but not the relation that produces them. `rust-dioxus/src/entry_line_numbers.rs` is a module whose whole job is to be the one relation between entry positions and each side's line numbers: `line_number_span_of` and `changed_runs` translate outward for both reading answers, and `position_of_line` translates inward for expanding at a named line. ARCH-hrwskjav44 (LineNumberSpan) records the answer's shape and ARCH-8tce9rry3b (Diff) records the state and lists its answers, but no record names the relation or says it is single. The pre-work Code Whisperer sign-off closed on exactly that expectation, that one relation is authoritative so the three answers cannot each grow their own, and WORK:advice-bdghwgkv53 carries it. The expectation now lives in a module doc comment and in this work unit alone, so a reader of the architecture sees three answers with no stated reason they agree.
  - **evidence**: Read rust-dioxus/src/entry_line_numbers.rs:1-81, whose module doc at :1-7 states the single-relation intent, and rust-dioxus/src/diff.rs:47-49, where the diff imports all three functions from that module and nothing else translates. toon search for entry_line_numbers, entry position or relation over .project/architecture returns no match at the capture-time check. The pre-work Code Whisperer sign-off and WORK:advice-bdghwgkv53 were both read in the work unit and both name the authoritative-relation expectation.

- **finding** `AUDIT:finding-z6z69zxrw9` (F15) · severity `low` · priority `med` · status `pending` · reviewer _Architect_ · The Components section of .project/architecture/dioxus-diff-viewer.arch.md, where ARCH-wdrt39xvh4 (similar) is the only external component recorded, against the dependencies in rust-dioxus/Cargo.toml
  - The crate depends on `blake3` to compute the identity consumer-rendered content stays mounted by, and no architecture record names it. `similar` has a component record, ARCH-wdrt39xvh4, and takes part in ARCH-atczcqvdsz (Diff analysis hand-off); `blake3` has neither, though ARCH-8tce9rry3b (Diff) lists the content identity among what the diff owns and REQT-zen8fyae28 (Rendered content identity) governs what it must do. The dependency predates this unit, which moved the hashing out of the viewer's body and into the diff, so the gap is inherited rather than introduced.
  - **evidence**: rust-dioxus/Cargo.toml names three dependencies, blake3, dioxus and similar. rust-dioxus/src/diff.rs:304-311 computes the identity with `blake3::Hasher` and rust-dioxus/src/diff.rs:72 holds it as the diff's `content_identity` memo. Listing every record name in .project/architecture/dioxus-diff-viewer.arch.jsonl returns one external component, similar, at the capture-time check. `git log -S blake3 -- rust-dioxus/Cargo.toml` places the dependency at 306ad02, in the diff-viewer-component unit, before this unit's first commit f47c1b3.

- **finding** `AUDIT:finding-9k6kfpaksa` (F16) · severity `low` · status `withdrawn` · reviewer _REQM_ · On the `diff` prop of the DiffViewer component (the viewer's props list) at rust-dioxus/src/diff_viewer.rs:69-72
  - REQT-m776z5vdhe (Rendering a diff) has two clauses and the annotation names only the first. The comment at rust-dioxus/src/diff_viewer.rs:71 reads REQT-m776z5vdhe (Rendering a diff): the viewer's only data prop, and sits on the `diff` prop; the requirement's second clause, naming the props the viewer keeps as its own, is implemented by the sibling props that follow and carries no annotation of its own.
  - **evidence**: Read rust-dioxus/src/diff_viewer.rs:68-95. The annotated `diff` prop opens the argument list and the eight props the requirement's second clause names, view, theme, show_line_numbers, fold_row_renderer, highlighted_lines, on_line_number_click, line_content_renderer and the two titles, all follow within twenty-four lines, each with its own doc line.
  - **resolution**: Withdrawn: both clauses are implemented by one argument list and the annotation stands at its head, within sight of every prop the second clause names. A second annotation would restate the requirement rather than locate anything a reader of that list cannot already see.

- **finding** `AUDIT:finding-398hknhypd` (F17) · severity `low` · status `withdrawn` · reviewer _REQM_ · Requirement 3.2.6 Palette override (REQT-kcm5ba45sp) in .project/requirements/diff-viewer.reqts.md, against the theming annotations in rust-dioxus/src/dxdiff.css
  - REQT-kcm5ba45sp (Palette override) carries no traceability annotation anywhere in the implementation, while both of its leaf children do. The reverse check over the whole requirements set turns up this one non-group requirement with no annotation.
  - **evidence**: Comparing every requirement id in .project/requirements/diff-viewer.reqts.md against every id annotated under rust-dioxus/src/ leaves sixteen unannotated ids at the capture-time check; fifteen are area or group anchors and REQT-kcm5ba45sp is the only child among them. Its two leaves, REQT-1accrb4jhp (Named colors) and REQT-ey9f1s27r1 (Yielding rules), are both annotated in rust-dioxus/src/dxdiff.css, and ARCH:dcisn-pgydgmajhx lists REQT-kcm5ba45sp among what it supports.
  - **resolution**: Withdrawn on two grounds: the parent's leaves realize its intent and carry its annotations between them, which is what the fourth outline level is for; and the theming code it would annotate lies outside this unit's changeset, where the post-work traceability gate does not reach.

- **finding** `AUDIT:finding-3pb25bv1jn` (F18) · severity `low` · status `withdrawn` · reviewer _REQM_ · In side_span (one side's first and last line numbers over a run of entries) at rust-dioxus/src/entry_line_numbers.rs:45
  - One annotation in the changeset inverts the reference format. REQM writes a code reference as the id followed by its label in parentheses; the doc line at rust-dioxus/src/entry_line_numbers.rs:45 puts both inside one parenthesis instead, reading (REQT-smd01rma2q, Independent numbering), so the changeset carries two spellings of one convention.
  - **evidence**: Scanning rust-dioxus/src for requirement ids not followed by an open parenthesis returns four lines at the capture-time check: entry_line_numbers.rs:45, diff_analysis_engine.rs:249 and :250, and dxdiff.css:104. Only the first is this changeset's: `git log -S` places the engine pair at 7cfe398, in the line-diff-engine unit, and the stylesheet line predates this unit with the file itself. Every other requirement reference in the crate uses the id-then-label form.
  - **resolution**: Withdrawn: the inverted form carries the same id and the same label, which is what the reference format exists to guarantee, and it reads as the prose aside it is, inside a sentence about how each side numbers its lines. The three instances outside the changeset show the form is the crate's occasional prose variant rather than a slip introduced here.

- **finding** `AUDIT:finding-agp9k3q2ad` (F19) · severity `low` · status `withdrawn` · reviewer _Code Whisperer_ · The project glossary at .glossary.jsonl, against the exported LineNumberSpan and the two reading answers at rust-dioxus/src/lib.rs:32 and rust-dioxus/src/diff.rs:85 and :97
  - The crate draws a distinction between run and span that the project glossary does not record. `changed_line_runs()` and `hidden_line_runs()` return `LineNumberSpan`, all three module-visible, and the distinction behind the pairing, run for the entries and span for the numbers those entries cover, lives in ARCH-hrwskjav44 and in one sentence of REQT-f2affyt2h2. The Code Whisperer's naming stewardship asks that terms visible beyond their file be covered by a glossary in scope, and neither word has an entry.
  - **evidence**: toon read of .glossary.jsonl returns eleven terms at the capture-time check, none of them run, span or line number span. AUDIT:finding-gz4xv6c302's resolution examined this same pairing and found it sound on ARCH-hrwskjav44 and REQT-f2affyt2h2. Commit fbe9f74, which is this unit's glossary pass, states the position in its own message: No term for the reading answers' shape: its one non-obvious property is stated in REQT-f2affyt2h2 and ARCH-hrwskjav44.
  - **resolution**: Withdrawn: the question was put and answered inside this unit's glossary pass at fbe9f74, with the grounds recorded in that commit. Both words are ordinary English used in their ordinary senses, which the universality rule leaves uncovered, and the one non-obvious thing about the pairing has a home in the architecture record and the requirement.







## Reports


### 2026-09-18T17:02:00 · Work Planner
<a id="work:report-grvh0n8dbq"></a>
`WORK:report-grvh0n8dbq`

Planning opened on this unit and ~~the stakeholder deferred it the same session. Nothing was drafted beyond the phase move.~~ the stakeholder deferred the architecture decision rather than the work, so planning continues on the functional intentions.

**Correction**
- This entry as first written claimed the unit itself was deferred, and carried a Blockers group parking it in backlog pending unnamed future work. Both were a misreading of "we have other future work that will change this, let's defer for now", which answered the architecture-timing question put to the stakeholder rather than the question of whether to do the work; the stakeholder said so directly in the next turn. Commits `41d5c3e` and `da3fea7` acted on the misreading and `577b564` undid the parking.
- A later citation repair: the no-write-during-render discipline cited below was first written as `fold_reset_trigger.rs:78-81` and is line 75.
- A later substance repair, struck below: this entry claimed all three requirement areas are reader-facing. REQT-x8e364jbqm (How it fits the app) carries the `functional` text "Apps style the viewer, light or dark, and render line content their own way", so apps were already an audience there. The error was mine and it framed the fourth area as the only app-facing one until the record was re-read.

**Commits**
- `10c38cf` opened planning and moved the unit's pair out of backlog.
- `577b564` returned the pair to planning.

**Decisions**
- Architecture capture for the widened handle is deferred at the stakeholder's direction, on other future work that will change it. The work proceeds with the functional intentions as its hard constraint, and the architecture is captured after the empirical experience rather than predicted before it.

**For the next worker**
- ~~Two questions were reached and neither was answered. First: how much of the reading-and-steering mechanism to settle before building it.~~ Two questions were reached; the first, how much of the reading-and-steering mechanism to settle before building it, is answered under Decisions above. The constraint behind that question still holds and still shapes the build: for the handle to publish viewer state, the viewer must write signals the handle owns, and `rust-dioxus/src/fold_reset_trigger.rs:75` records the discipline the current code keeps, that expansions read correctly without any write during rendering. At least three designs resolve that, and neither the architecture nor the requirements picks one.
- Second: an app driving a mounted viewer fits none of the three requirement areas, ~~which are all reader-facing~~ two of which are reader-facing while the third already addresses apps about appearance alone (`.project/requirements/diff-viewer.reqts.md:40-51`). A new functional area looks right, and REQM requires the stakeholder's consent before one is created.
- The dependency is satisfied and ready to build on: WORK-6ds59btb76 (diff viewer component) sits in `done/`, and `FoldResetTrigger` at `rust-dioxus/src/fold_reset_trigger.rs:16-50` carries the reset-only signal this unit widens.
- The repository has no project-level `.mcp.json`; toon reaches this project through global configuration, so a fresh session should not read its absence as missing tooling.


### 2026-09-18T18:20:00 · Architect
<a id="work:report-9xgcm5c0v7"></a>
`WORK:report-9xgcm5c0v7`

The design conversation the stakeholder asked for reached a settled arrangement. Nothing was built and no requirements were written.

**Commits**
- `10c38cf` opened planning and moved the unit's pair out of backlog.
- `577b564` returned the pair to planning.
- `dc2ba60` corrected the record of what had been deferred.

**Decisions**
- Two accepted architecture decisions came out of the dialogue, both at draft maturity because the stakeholder steered them line by line: ARCH:dcisn-x8fa9g2hm8 (engine renamed to `analyze_diff` and `DiffAnalysis`) and ARCH:dcisn-xgpr1asvyn (the app holds the live diff, the viewer renders it). Each detail field carries the alternatives weighed and set aside, so they need not be argued again.
- The engine rename belongs inside this work unit rather than a container of its own. The collision it fixes exists only because this unit adds `use_diff`, so it reads as part of landing that API coherently; a unit whose whole justification is another unit's need is the case `#proliferation-avoidance` names.
- The architecture-timing deferral recorded earlier is superseded for everything these two decisions cover, and still holds for whatever they leave open.

**For the next worker**
- Requirements come before activities, and the stakeholder has said the settled approach may be implied in the requirement text, so they need not stay free of mechanism.
- Expect the rename to be wide and mechanical: the engine modules and their filenames, the crate root, roughly forty-five call sites in `rust-dioxus/tests/line_diff_engine.rs`, ARCH-exgfx6rwdt, the requirements' file list at `diff-viewer.reqts.md:135-137`, and the glossary, which gains entries for the new names only when the code carries them.
- One reading that shaped every decision here, worth re-deriving rather than trusting: the viewer needs the texts for exactly two things, the engine memo and the identity hash (`rust-dioxus/src/diff_viewer.rs:119-135`), which is why a live diff owning both leaves the viewer with no use for them.
- Citation repair: this unit's earlier report and the session dialogue both cited the no-write-during-render discipline as `fold_reset_trigger.rs:78-81`; it is line 75. The earlier report has been corrected in place.


### 2026-09-18T18:45:00 · Work Planner
<a id="work:report-5pc5baey65"></a>
`WORK:report-5pc5baey65`

The three open items blocking requirements are closed, each on stakeholder assent. No requirement children are written yet.

**Commits**
- `d88711b` settles where the folding choices sit.
- `1a1fd9e` opens the fourth requirement area.
- `616f9c8` records per-region re-folding as excluded.

**Clarifications**
- ARCH-phde62wxvn resolved onto the live diff: ARCH:dcisn-xgpr1asvyn already gives it the planned rows, which cannot be derived without whether folding is on and the surrounding-line count, and the expansion basis already turns on the same count.
- The app-as-actor behavior earned its own area rather than joining `How it fits the app`; area 4.0 `Driving it from code` (REQT-62p6rwsepb) now exists with its functional-area record.
- Per-region re-folding stays out, recorded as an exclusion on DiffViewer (ARCH-m4dkxzw6hh): an app reaches the same end state by reading which lines are hidden, resetting, and re-expanding what it keeps.

**Decisions**
- The new area was appended at outline 4.0 so no existing outline shifted and no renumbering consent was needed.
- Records written this session rest at DREAMED; under the maturity vocabulary, assent to a proposal is provenance-neutral and never advances a record.

**Observations**
- REQT-x8e364jbqm (How it fits the app) reads BACKLOG while every child beneath it reads IMPLEMENTED/NEEDS VERIFICATION. Low severity, predates this unit, left untouched.
- Area 4.0 carries no children, so the Functional Areas collation currently promises a contract nothing specifies.

**For the next worker**
- The toon write's markdown regeneration is asynchronous: the `.md` lands a few seconds after the write call returns, so a read taken immediately shows the previous text and reads as a stale projection. Wait, then re-read, before concluding anything needs catching up.
- REQT-869jyzdes7 (Fold reset) and REQT-v748c7mjr6 (Expanding folds) describe the fold reset trigger that ARCH:dcisn-xgpr1asvyn retires. The new area's children cannot be written without amending those two, or two contracts will describe one behavior.
- The Rust Roundup's Forms Expert lens was not read in full for this increment; `rust-dioxus/Cargo.toml` depends only on blake3, dioxus and similar, so no forms work was exercised.


### 2026-09-19T02:40:00 · REQM
<a id="work:report-90nxbnagkz"></a>
`WORK:report-90nxbnagkz`

The requirement text this unit needed is written: area 4.0 in full, and the two folding requirements it made stale. No code changed.

**Commits**
- `51606e2` opens area 4.0 with the app-held live diff and the viewer's place beside it.
- `c1ea49f` says what a live diff answers about changes and hidden lines.
- `a10d0dc` says what opens a fold and what returns them all to folded.
- `0e915ca` takes the retired trigger out of area 2.2's fold contract.
- The commit carrying this entry catches the unit's own framing and references up to all of it.

**Clarifications**
- The stakeholder ruled the word `steering` out of this vocabulary when the third group was proposed under it. That group is REQT-p3xyw73v2p (Opening and refolding), and the word was cleared in the same pass from REQT-dxbaat20ja (One plan per live diff) and from area REQT-62p6rwsepb's purpose.
- Reading answers in each side's plain line numbers; opening a fold names one line by its line id. Both shapes were put as key questions and closed by the stakeholder's assent to the proposals.
- The three fold actions report nothing about what they changed. An app that wants to know reads REQT-hsef5r7c4z (Which lines are hidden) instead, which is why the reference package's reset boolean stays dropped.

**Decisions**
- REQT-869jyzdes7 (Where fold state lives) kept its id and its place in the folding group rather than moving into area 4.0: a reader of area 2 still needs to know where the state lives, while the reset action itself is area 4.0's to state. Its maturity dropped to DREAMED, because the text a stakeholder once steered is gone and the replacement is the agent's.
- The three new groups are org-groups. Area 4.0 keeps the single functional-area claim, so the Functional Areas collation gains no new bullets.

**Observations**
- The project glossary's `fold reset trigger` entry still describes the crate-owned handle accurately, because `rust-dioxus/src/fold_reset_trigger.rs` still exists. It retires when the code does, together with the entries the rename decision defers to that same moment.
- The requirements' Files list still names `fold_reset_trigger.rs` and the `line_diff_*` modules. ARCH:dcisn-x8fa9g2hm8 covers that list in the rename's reach, so it changes with the code rather than now.

**For the reviewer**
- Everything written today is unimplemented, including the amended halves of the two area 2.2 children. Those two still read IMPLEMENTED/NEEDS VERIFICATION because requirement status prep belongs to pre-work exit; do not read that status as covering their new sentences.
- The in-scope requirement set is readable from the unit's `work_ref` scope records; nothing in the plan text restates it.

**For the next worker**
- The engine rename has no requirement of its own and needs none: it is a naming change, carried by ARCH:dcisn-x8fa9g2hm8 and by this unit's plan guidance. Do not go looking for a requirement that authorizes it.
- A toon patch needs the record's `__rev`, and that token is computed by the tool rather than stored in the JSONL, so grepping the file for it finds nothing and a `fields` projection is rejected for it. Budget one plain record read before each patch.
- The Rust Roundup lenses were not loaded this increment. The skills index rules them optional when an increment only reads Rust, which is all this one did; an increment that touches the crate loads them in full, with the UI group all-or-none.


### 2026-09-19T16:30:00 · Work Planner
<a id="work:report-qh2xqwdk3q"></a>
`WORK:report-qh2xqwdk3q`

The plan is complete: six activities, six criteria, and the skill and file records the activities settle. No code changed.

**Commits**
- The commit carrying this entry writes the whole plan and the status summary that follows from it.

**Clarifications**
- ARCH:dcisn-x8fa9g2hm8 names `line_diff` and `LineDiff` and says the rename reaches the engine modules and their filenames, but says nothing about `LineDiffOptions`, which is exported beside them at `rust-dioxus/src/lib.rs:23`. Put to the stakeholder as the activities' one open fork and closed by assent: the engine's options type renames too, so the live diff's own options value and the engine's do not differ by the word the rename set out to disambiguate. The activity text carries the resolution, so no alternative stands in the plan.

**Decisions**
- Testing is not an activity of its own. Each activity carries the tests for what it delivers, and the coverage expectation sits in the criteria instead, so a test activity cannot be the one that slips.
- The records catch-up is last rather than distributed, because the architecture record, the requirements' file list, and the glossary all describe names the code does not carry until the earlier activities land.
- The Rust Roundup is seated in full, UI group included, per the skills index all-or-none rule. Appshell Expert and Forms Expert are seated although `rust-dioxus/Cargo.toml` names neither appshell nor `rust-json-schema-forms`; a worker whose increment exercises neither says so in its report rather than dropping them from the plan.

**Observations**
- The live diff and its hook pair have no structural architecture record, only the detail fields of the two decisions. The expansion variety's work-ready tier requires resolved ARCH records for new elements, so pre-work closes this; it is named here so it cannot be missed. Medium severity.
- `work_unit.state` reads `backlog` while the file sits in `.project/work/planning/`. The schema's state machine promotes to `ready` only at pre-work exit, so there is no state between the two; left alone.

**For the reviewer**
- The six criteria deliberately restate no in-scope requirement: under `#04-reqts-are-critical` those requirements are already the scope. Read the criteria as what the requirements do not carry, chiefly that nothing of the old arrangement survives anywhere a consumer or a record can still see it.
- Criterion `WORK:crit-d5vagb7xpe` (engine suite covers the same ground) is the one whose verification is non-obvious: the rename touches roughly a hundred mentions in `rust-dioxus/tests/line_diff_engine.rs`, and a dropped case would not fail the build.

**For the next worker**
- A `work_activity` record has no `maturity` field, though `work_crit` does; a write that includes one is rejected for additional properties rather than for the field by name.
- The component test suite is larger than the requirements' file list suggests: `rust-dioxus/tests/diff_viewer/` holds ten files and roughly 2490 lines, mounting apps through `mounted_app.rs`. The reshape reaches more of it than `folding.rs` alone.
- The coding discipline's readiness module is at `coder/coder.codingDiscipline.reqts.md` in the skills library, and this unit's variety is expansion, area 3.0 there.


### 2026-09-19T18:25:00 · Work Planner
<a id="work:report-pk5qj70fb9"></a>
`WORK:report-pk5qj70fb9`

Pre-work is closed. Three reviewers seated, five findings raised and resolved, three sign-offs approved, and the in-scope requirement statuses moved to NEXT. The unit is work-ready and no code changed.

**Commits**
- `1f6872f` files the independent analysis from all three reviewers before any dialogue.
- `7008aa4` triages each finding to in-review with its presentation and venue.
- `3e7e673` gives the live diff, its options and the hook pair element records of their own.
- `3511bb9` scopes the four existing architecture records the work changes.
- `402b38e` names one position-and-line-number relation for the three answers that cross it.
- `2e99833` dismisses the test-harness advice and keeps its one durable line.
- `4323447` matches the focus files to the component test reach.
- The commit carrying this entry approves the sign-offs and moves the requirement statuses.

**Clarifications**
- Whether the live diff belongs under the viewer's component or wants one of its own: put as a key question, and the default under ARCH-m4dkxzw6hh was left standing.
- Whether the hand-off interaction survives the change: collapsed to a default against ARCH:dcisn-xgpr1asvyn's detail and `rust-dioxus/src/diff_viewer.rs:119-127`. One call remains, made by a different actor, so ARCH-atczcqvdsz keeps its identity and swaps its caller.
- Assent to build according to the in-scope requirements was given in dialogue and, per the pre-work protocol, is recorded nowhere else; no requirement's maturity moved because of it.

**Decisions**
- WORK:advice-qf0vfmktm4 was dismissed rather than carried, after the stakeholder asked whether a practical worker would not simply figure it out. Three factors settled it, and the first was an error of mine: WORK:file-3g4hd4ep2n already carried the harness fact in its relevance line, so the advice restated the plan back to itself. The lesson generalizes past this unit: before filing a recommendation, check whether a focus file's relevance line already says it.
- The four existing architecture records are scoped now but keep their current text; they change with the code in activity WORK:activity-s1m3wbpec7 rather than ahead of it. New element records went in ahead of the code, matching how the two decisions were recorded.
- Requirement status prep moved fourteen records to NEXT: area 4.0 with every child, and the two amended area 2.2 children whose IMPLEMENTED/NEEDS VERIFICATION no longer matched their text.

**For the reviewer**
- The post-work sign-offs are seated and pending for the same three reviewers.
- Criterion WORK:crit-d5vagb7xpe is the one whose verification has to be deliberate: a dropped engine test case during the rename leaves the build green.
- The pass's own coverage trail is WORK:advice-f08kwnj9z8; what pre-work examined and set aside is there rather than in this report.

**For the next worker**
- A toon patch to a `.project` record needs the row's current `__rev`, and guessing one is rejected outright; the rejection reports the on-disk value, so an unprojected read of the row first is the cheaper path.
- `presentation.context` on a finding record is capped at 400 characters and the cap is easy to overshoot by a few; measure the text before the write rather than after the rejection.
- The repository has no project-level `.mcp.json`; toon reaches this project through global configuration.
- Seven of ten files under `rust-dioxus/tests/diff_viewer/` build a viewer through props this unit removes, and each now has its own focus-file record saying why.


### 2026-09-19T19:40:00 · Coder · Rename the engine to analyze_diff and DiffAnalysis
<a id="work:report-50arbnmjqy"></a>
`WORK:report-50arbnmjqy`

The engine rename is delivered and the crate is green: `analyze_diff`, `DiffAnalysis` and `DiffAnalysisOptions` replace the earlier names across the modules, their filenames, the crate root and the engine test file. No behavior changed.

**Commits**
- `2ca6473` accepts the unit and marks its fourteen requirements under way.
- `4d4d7e4` moves the unit's pair into `work/now/`.
- `ade636d` reformats two spots that predate this unit.
- `f47c1b3` renames the engine and everything that names it.

**Decisions**
- The engine's options type became `DiffAnalysisOptions`, pairing with `DiffAnalysis` as `DiffOptions` pairs with `Diff`. Whether it renamed at all was closed by stakeholder assent at pre-work; the spelling was the worker's under pre-work's entrustment rule.
- The engine's three modules kept their engine/options/output split and took `diff_analysis_*` filenames, rather than collapsing into one module named for the renamed function.
- `analyze_diff` and `DiffAnalysis` stay publicly exported. Pre-work left that to the worker, and a rename is the wrong moment to change visibility; the reshape is when hiding them could earn its keep.
- The formatting drift landed in its own commit ahead of the rename, so the rename commit reads as a substitution rather than a substitution mixed with reflow.

**Observations**
- Two files were already unformatted at the rename's starting commit, `rust-dioxus/tests/diff_viewer/theming.rs` and `rust-dioxus/src/row_rendering.rs`, both from the theming unit. Low severity; fixed in `ade636d`.
- Beacon and Logan were seated and found no surface: the crate emits no log event and opens no span, so there was no emit site to judge. Appshell Expert and Forms Expert likewise, since `rust-dioxus/Cargo.toml` names neither appshell nor `rust-json-schema-forms`.

**For the reviewer**
- Criterion `WORK:crit-d5vagb7xpe` is checkable mechanically rather than by reading 896 lines: re-derive each file from its pre-rename version through `sed -e 's/LineDiffOptions/DiffAnalysisOptions/g' -e 's/LineDiffEngine/DiffAnalysisEngine/g' -e 's/LineDiff\b/DiffAnalysis/g' -e 's/line_diff_engine/diff_analysis_engine/g' -e 's/line_diff_options/diff_analysis_options/g' -e 's/line_diff_output/diff_analysis_output/g' -e 's/\bline_diff\b/analyze_diff/g'`, run rustfmt over the result, and diff it against the delivered file.
- That check leaves exactly four deviations, each deliberate: the module-doc line of the engine test file and line 4 of `diff_viewer.rs`, both prose reading "diff analysis" where they read "line diff"; and rustfmt's reordering of `mod` and `use` lines in `lib.rs`, which the standalone re-derivation cannot reproduce because it cannot resolve the modules. A word-level diff of `lib.rs` confirms no exported symbol was lost.
- Test counts are unchanged at 28 engine and 58 component tests, and clippy over all targets is clean. The counts alone would not have caught a weakened case, which is why the re-derivation above is the evidence.

**For the next worker**
- `ReadSignal` component props are updated **in place** rather than replaced: the generated `memoize` calls `point_to` and `mark_dirty` on the old signal (`dioxus-core-macro/src/props/mod.rs:629-676`), which is what keeps a `use_memo` subscribed across renders when a parent hands down a changing text. A plain hook gets none of that machinery, so `use_diff` MUST NOT simply take a `ReadSignal<String>` and expect a caller's changing text to reach its memo.
- The framework's mechanism for a hook taking plain values is `use_reactive` (`dioxus-hooks/src/use_reactive.rs:105-118`): it holds the dependency in a `use_signal` and writes it during render only when the value actually changed, which is why that write is not the mutate-during-render loop. Its `Dependency` bound needs `PartialEq + Clone + 'static`, so `&str` cannot be a dependency and the texts have to arrive as an owned type.
- `SuperFrom` for `ReadSignal<O>` wraps its input in a **fresh** `Signal` on every call (`dioxus-signals/src/props.rs:7-15`), so a literal passed to a `ReadSignal` prop allocates a new signal per render. That is already true of today's viewer props and is worth knowing before choosing what the hooks take.
- The viewer reshape reaches 51 viewer constructions across the seven component test files. Most are a one-line app component with the two texts and a single presentation prop, but six spots mount **two** viewers in one app component, so two hook calls have to sit at that component's top: `views.rs:50-51`, `:74-75`, `:136-142` and `:160-166`, and `theming.rs:703-705` and `:737-739`. The last of those is the shape `REQT-dxbaat20ja` (One plan per live diff) needs.
- A whole-crate `cargo fmt` sweeps files the current change never touched, so check `git status` before staging rather than after.


### 2026-09-19T20:20:00 · Coder · Catch the records up to the delivered code
<a id="work:report-jjpddapefz"></a>
`WORK:report-jjpddapefz`

The architecture records and the requirements' file list now describe the delivered crate. The project glossary is what remains of this activity.

**Commits**
- The commit carrying this entry catches the two record sets up to the crate's names and arrangement.

**Decisions**
- `ARCH-y9545npjzg` (FoldResetTrigger) was retired to `abandoned` maturity rather than removed. `ARCH:dcisn-xgpr1asvyn` names it in an `involved[].ref` and again in its `detail`, and the Architect's referential-completeness rule wants every FK-typed id resolving inside the file, so removal would have dangled two pointers.
- `LineRun` gained an element record, `ARCH-hrwskjav44`, on the same rule that earned the live diff and its hooks theirs at pre-work: it is exported and it is the shape both reading answers come back in.
- The viewer kept the rendered-content lane rather than handing it to the live diff with the identity value. The row keys are still the viewer's (`row_rendering.rs:35-50`); only the outer identity moved, so the lane reads "Keeping consumer-rendered content mounted with its line" and `REQT-zen8fyae28` stays on the component the requirement addresses.
- `REQT-m776z5vdhe` (Rendering a live diff) was added to the viewer's `supports_requirements`. It constrains the viewer's props, so the viewer is the element that satisfies it.
- The three decisions were repaired with strikeout rather than rewritten. `#strikeout-correction` puts decision records in scope for the form precisely because they narrate a moment, and each strike is the smallest false span with its replacement cited.

**Clarifications**
- Whether a `software_object` may be a participant in `ARCH-atczcqvdsz` (Line diff hand-off), given the field's description reads "components/actors". The stakeholder assented to `ARCH-8tce9rry3b` (Diff) as the calling participant, over the larger move of promoting the live diff to a component.

**Observations**
- `rust-dioxus/src/dxdiff.css` has no `file` record in the requirements, and had none before this unit; the theming unit added the file without one. Low severity, and outside this unit's scope, so it was left for a reader to weigh.
- The two renaming decisions' `background.situation` fields cite `rust-dioxus/src/diff_viewer.rs:119-152` and `fold_reset_trigger.rs:75`, neither of which holds that content now. Left standing: a decision's background is the pre-decision snapshot by design, and striking it would be strikeout-as-apology.

**For the reviewer**
- The catch-up is checkable mechanically rather than by reading the whole arch file: `toon search` the `arch` ext for `LineDiff|line_diff|line diff|fold reset|FoldReset` over `.project/architecture`. Four records match, and every match is deliberate: the two renaming decisions and `ARCH:dcisn-wma0h4mndz` carry struck obsolete terms on purpose, and `ARCH-y9545npjzg` is the retired record keeping its historical name.
- The requirements' file list matches `rust-dioxus/src/` exactly but for `dxdiff.css`, noted above. Three records changed path with the engine rename, one was removed with its module, and four were added.
- The collaboration diagram regenerated with `Diff` in place of `DiffViewer`; `Diff` carries `datastore` as its `diagramElementType`, which is in the theme, so the node renders colored rather than grey.

**For the next worker**
- A toon `dispatch` cannot carry an array value: its indented form takes one scalar per record id. A record whose arrays change needs a full `content` replacement, which means reading it for its `__rev` first.
- `software_object` records have no `activities` or `responsibilities` fields and the schema denies unknown ones, so a responsibility lane leaving a component for an object has nowhere structured to land. It goes into the object's `definition` or `discussion`, which is where `ARCH-8tce9rry3b` was already carrying the fold state from pre-work.



### 2026-09-19T20:55:00 · Coder · Catch the records up to the delivered code
<a id="work:report-r1mjsy8vam"></a>
`WORK:report-r1mjsy8vam`

The records catch-up is complete and the unit's six activities are all delivered. The crate is green at 28 engine and 74 component tests, clippy clean over all targets, `cargo fmt --check` clean. What the activity did not anticipate: steering retired the word "live" from the project's vocabulary and renamed one exported type, so the catch-up reached the crate as well as the records.

**Commits**
- `208d319` architecture and requirements records brought to the crate's names and arrangement.
- `82679c3` the word "live" retired from the crate, module renamed to `diff.rs`.
- `ee76bd4` the same word retired from twelve requirements and ten architecture records.
- `795b52d` `LineRun` renamed to `LineNumberSpan`.
- `fbe9f74` glossary brought to the crate's vocabulary.
- The commit carrying this entry closes the activity and submits the unit for review.

**Clarifications**
- The stakeholder read the proposed glossary draft and found `live diff` and `line run` wanting against `#01-meaningful-labels`. That is what turned a glossary edit into a vocabulary decision, resolved across four octopus topics: drop "live" and let `diff` stand against `diff analysis`; rename `LineRun` to `LineNumberSpan`; leave the two older decisions' narrative unstruck; delete the fold reset trigger entry with no replacement.

**Decisions**
- The glossary gained one term, not four. `diff analysis` earns a record because a reader would not guess the engine's output carries the changed positions. `diff` does not: it is the crate's own domain noun, and the Code Whisperer's universality rule exempts it. `LineNumberSpan` does not: its one non-obvious property, the absent side, is stated in REQT-f2affyt2h2 (Where the changes are) and ARCH-hrwskjav44 (LineNumberSpan).
- `use_diff` and `use_diff_with` got no entry either. Glossary entries define terms rather than identifiers, and both component terms already pass.
- The two reading answers kept the names `changed_line_runs` and `hidden_line_runs` while their return type renamed. A caller reads them as the runs, each given as the span it covers, which is REQT-f2affyt2h2's own sentence.
- No `DONT`-deprecated record for the retired `fold reset trigger`. `ARCH-y9545npjzg` keeps the name at `abandoned` maturity with a pointer to where the responsibility went, so the old phrase still resolves.

**Observations**
- `rust-dioxus/src/dxdiff.css` has no `file` record in the requirements, and had none before this unit. Low severity, outside scope, unacted.
- `side_run` in `entry_line_numbers.rs` still says "run" for a value that is one side's line-number range. It is file-local, so the module-visibility rule does not reach it; a post-work reader may judge it worth aligning.

**For the reviewer**
- The vocabulary sweep is checkable mechanically: `grep -rn '\blive\b' rust-dioxus/ --include=*.rs` returns nothing, and `toon search` the `*` ext for `live diff|live state` over `.project` returns only the two older decisions, whose narrative was deliberately left as written.
- The three renames this unit made are each verifiable the same way: `LineDiff`/`line_diff`, `live`, and `LineRun`/`line_run_of` have no occurrences left outside the deliberate ones above.
- Test counts held at 28 and 74 across all three renames; the only test edit was one function name, `a_fold_opened_in_one_viewer_over_a_live_diff_opens_in_the_other` becoming `..._over_one_diff_...`.
- An estimate in this session was wrong and is worth knowing when checking scope: "four architecture records" was quoted for the sweep and thirteen carried the phrase. The cause was counting with `grep` on the `.md` projection instead of `toon search` over the `.jsonl` across both spellings, `live diff` and `live state`.

**For the next worker**
- A blanket `sed` for a term will miss occurrences broken across a line in wrapped doc comments. One such case sat at `diff_viewer.rs:3-4`; `grep -rn '\blive\b'` on the single word is what found it, and the same trick generalizes to any multi-word term sweep.
- Renaming a module means reordering two lists in `lib.rs` by hand. `rustfmt` sorts `use` statements but not `mod` declarations, so a renamed module lands wherever the old name sat.
- `toon read` rejects `fields: ["$.__rev"]` outright, so a batch write needs an unprojected read of every target record to collect its `__rev` first. There is no cheap id-and-rev view.



### 2026-09-19T21:20:00 · Work Planner
<a id="work:report-m132v9kkr3"></a>
`WORK:report-m132v9kkr3`

Post-work resolution opened on the four findings left in review. F1 is sealed; the other three are still open, and F2 is where the pass stops for steering.

**Commits**
- The commit carrying this entry strikes the retired module path in the two decisions that name it.

**Decisions**
- F1 carried no genuine fork, so it resolved on its stated default rather than holding a turn: the strikeout-versus-rewrite choice was settled at 1cd0b4c and the word live was settled earlier still.

**Observations**
- Two findings cite line numbers in `rust-dioxus/src/diff.rs` that do not match the delivered file: AUDIT:finding-gz4xv6c302 (F2) places `hidden_line_runs` at :97-108 and AUDIT:finding-8prqcgrsg6 (F4) places `reset_folds` at :114-117, where the file at fbe9f74 holds them at :64-75 and :81-84. The anchors around them, in `consumer_callbacks.rs`, `entry_line_numbers.rs`, `expanded_folds.rs` and `lib.rs`, all check out, so the substance of both findings stands; only the two `diff.rs` citations are wrong.

**For the reviewer**
- F2's stated reach is wider than the code supports. It says the rename crosses REQT-1tdrfvay4q's wording and ARCH-jakkqkh6e1's definition line, and neither names the identifier `HiddenLines`: the requirement says "the hidden-line count, and the old and new line numbers of the first hidden line" and the architecture record says the same in its own words. The rename is code-local, reaching `consumer_callbacks.rs`, `lib.rs`, `diff_viewer.rs`, `row_rendering.rs` and `tests/diff_viewer/folding.rs` and no record at all.
- The project glossary already carries `fold row` as a TERM, which is what makes the proposed `FoldRowLines` covered vocabulary rather than a new coinage.

**For the next worker**
- `__rev` cannot be projected through `fields`, so a seal needs one full read of the record it patches; a projected read to find the record and a second unprojected one to get the rev is two calls, not one.
- Both wrong `diff.rs` citations and F2's overstated reach are corrections owed to the finding records themselves, under `#strikeout-correction`, and land with each finding's own turn rather than here.


### 2026-09-19T21:30:00 · Coder · Build the app-held live diff
<a id="work:report-ebyt7npy4m"></a>
`WORK:report-ebyt7npy4m`

The app-held live diff is built and the viewer is reshaped around it, so activity WORK:activity-gx60bkawf4 lands in the same commit. The crate is green: 28 engine tests, 59 component tests, clippy clean over all targets.

**Commits**
- The commit carrying this entry moves every piece of diff state out of the viewer and onto the live diff an app holds.

**Decisions**
- `reset_folds()` landed here rather than waiting for WORK:activity-bx7yxakmx2. The retiring trigger's two tests had nothing left to exercise the moment it went, and the alternative was carrying failing tests across a landing. The other two fold actions and both reading answers stay with their own activities.
- The fold basis became the content identity paired with the folding configuration, retiring both the reset counter and the `Rc<Cell>` generation the viewer bumped on every re-evaluation. One behavior changes with it: a text signal written with an equal value used to drop every expansion, and now keeps them, which is what REQT-v748c7mjr6 asks for.
- The hooks take `impl AsRef<str>` and copy a text only when it differs from the one already held, rather than taking owned texts and copying both on every render.
- Three modules rather than one: `diff_options.rs`, `expanded_folds.rs` and `live_diff.rs`. The engine's own three-module split is the precedent, and each lands well under the size target.
- `analyze_diff` and `DiffAnalysis` stay publicly exported. An app reaches the analysis through the live diff now, but nothing in scope asked for them to be hidden.

**Observations**
- `HiddenLines`, the fold-row renderer's payload, and the `hidden_lines()` answer WORK:activity-pktstta3q3 adds are two things about hidden lines in one namespace. Low severity, and each name is the one its requirement uses; worth a look when that activity lands rather than a rename now.

**For the reviewer**
- REQT-dxbaat20ja (One plan per live diff) is covered by `a_fold_opened_in_one_viewer_over_a_live_diff_opens_in_the_other` in `rust-dioxus/tests/diff_viewer/folding.rs`, which clicks one viewer's fold row and asserts the other viewer's fold is gone too.
- REQT-m776z5vdhe (Rendering a live diff) is carried by the compiler as much as by a test: the viewer's props are exactly the nine the requirement allows it, and a call site passing any of the removed eight does not compile.
- The engine suite is untouched by this landing; all 28 of its tests still name the same cases they named at `f47c1b3`.

**For the next worker**
- A viewer re-renders on a live diff's change through its subscriptions, never its props. `Diff` is a bundle of `Memo` handles and one `Signal`, so its `PartialEq` never changes and prop memoization always skips the viewer; reading `analysis()` and `planned_rows()` in the viewer's body is what marks its scope dirty. An apparently-constant prop here is correct, not a bug to fix with a version field.
- `use_signal`'s initializer is `FnOnce` with no `'static` bound, which is what lets the `use_reactive` shape in `use_diff_inputs` borrow the caller's `&str` instead of taking it owned. `use_memo`'s closure does need `'static`, which is why only `Copy` handles are captured there.
- `MountedApp::expand_first_fold` clicks the first fold row in document order, which belongs to the first mounted viewer, so a two-viewer test proves the shared plan by reading the second viewer's rows afterwards.



### 2026-09-19T21:55:00 · Work Planner
<a id="work:report-hrek7tkh0f"></a>
`WORK:report-hrek7tkh0f`

Post-work Phase 3 is complete: all four in-review findings are resolved and the audit session holds no open finding. The three post-work sign-offs are the next gate, and Phase 4 follows them.

**Commits**
- `e02722b` (project): strike the retired module path in two decisions.
- `4d47135` (code): rename the exported HiddenLines to FoldRowLines, side_run to side_span.
- `b0d6d46` (code): move the two-viewer test's requirement comment onto its test.
- `bacf209` (code): correct the basis field's doc about what reset keeps.
- `894d9c5` (project): seal the four findings and record the naming finding's dialogue.

**Decisions**
- F1, F3 and F4 carried no genuine fork and resolved on their stated defaults; F2 was the one stakeholder call, and the rename was assented to.
- F4's alternative, clearing `basis` inside `reset` so the documented claim became true, was settled by the Contract Verification Gate rather than by preference: no test encodes the pair, `is_expanded` is the only reader, `Diff::reset_folds` the only caller.
- Only F2 gained a `content` dialogue record. F1, F3 and F4 carry their whole grounds in `resolution`, so annotating them would duplicate.
- The four single-record seal commits were folded into `894d9c5` under Commit Grouping; the `project` branch has no upstream, so nothing shared was rewritten.

**Observations**
- Three findings were filed with `rust-dioxus/src/diff.rs` line numbers that do not match the delivered file, all offset the same way. Each is struck in its own record. A fourth-hand check of any remaining `diff.rs` citation in this unit's records is cheap and worth doing before the Architect signs off.

**For the reviewer**
- Remaining before Done, in order: the Code Whisperer's post-work gate 1, which triggers the reqm traceability audit scoped to this changeset; the Architect's second post-work audit, Post-Work Architectural Capture; the three sign-offs; then Phase 4.
- Phase 4's recordable code-worktree commits are the six delivery commits from `f47c1b3` through `fbe9f74` plus `4d47135`, `b0d6d46` and `bacf209`. On the project worktree only `e02722b` qualifies, since it developed architecture content; every other project commit in this pass is confined to this unit's own records and is excluded by name.
- Three in-scope requirements still read IN PROGRESS: REQT-f2affyt2h2 (Where the changes are), REQT-hsef5r7c4z (Which lines are hidden), REQT-g86vdmyyp9 (Expanding at a line). They advance at Phase 4, not before.

**For the next worker**
- `__rev` is not projectable through `fields`, so patching a record costs one unprojected read of it; a projected read to find it and a second to get the rev is two calls.
- The content-group catalog for this file is `skillz/auditor/auditResults.contentGroups.jsonl`, reached through `workUnit.schemata.json`'s `templates.workUnit.contentGroups`. Notes is `CONTENT:group-2h8e63br0e`; Evidence is for attachments, not narrative.
- The crate is green at 28 engine and 74 component tests, clippy and rustfmt clean, after all three code changes.


### 2026-09-19T23:10:00 · Coder · Report changes and hidden lines
<a id="work:report-bgaz8sjtnj"></a>
`WORK:report-bgaz8sjtnj`

The two reading answers are delivered and the crate is green: 28 engine tests, 67 component tests, clippy clean over all targets. `changed_line_runs()` and `hidden_line_runs()` both read one relation between entry positions and each side's line numbers.

**Commits**
- The commit carrying this entry adds both answers and the one relation behind them.

**Decisions**
- The relation became a module of its own, `rust-dioxus/src/entry_line_numbers.rs`. The reverse lookup that activity WORK:activity-bx7yxakmx2 (Open and refold from app code) needs lands there beside `line_run_of`, which is what makes the one relation WORK:advice-bdghwgkv53 (one position-to-line-number relation) asks for structural rather than a habit two answers happen to keep.
- `LineRun` carries each side as `Option<RangeInclusive<usize>>` rather than a span type of its own. The absent side is real for a run of added or removed lines, and an inclusive range is what a Rust reader already knows; the price is that `LineRun` is `Clone` and not `Copy`.
- The answers took the longer names `changed_line_runs()` and `hidden_line_runs()`. `HiddenLines`, the fold-row renderer's payload, already holds the shorter spelling in this namespace and names a different thing: one fold row's hidden-line count with its first line's two numbers.
- Changed runs group the engine's own `changed_positions` through `slice::chunk_by`, so no second notion of what counts as changed exists. That is what binds REQT-f2affyt2h2 (Where the changes are) to the measure folding already uses.

**Observations**
- `HiddenLines` and `hidden_line_runs()` still sit in one namespace. Low severity, and both were left standing: the granularity differs and each name is the one its own requirement uses. A post-work reader who stumbles over the pair has the call.

**Seated lenses with no surface**
- Beacon and Logan found no emit site again: the crate opens no span and logs no event, so there was nothing to judge. Appshell Expert and Forms Expert likewise, since `rust-dioxus/Cargo.toml` still names neither appshell nor `rust-json-schema-forms`. All four stay seated in the plan rather than dropped from it.

**For the reviewer**
- The failure mode WORK:advice-bdghwgkv53 names, two copies of the relation drifting apart, is checkable mechanically rather than by reading: `grep -rn '\.number' rust-dioxus/src/` outside `entry_line_numbers.rs` matches only `row_rendering.rs`, where every read builds a gutter's line id, a row key, or the fold-row renderer's payload. None of those answers an app, so no second copy exists.
- The non-obvious coverage is `a_pair_differing_only_in_its_line_terminators_is_reported_as_a_change`. It checks REQT-f2affyt2h2's second sentence, that the lines counted as changed are the ones folding measures from, on the one case where the two could diverge: a pair whose lines read the same and whose terminators differ.
- REQT-hsef5r7c4z (Which lines are hidden) is covered as of the asking by `a_fold_a_reader_has_expanded_is_reported_hidden_no_longer`, which expands a fold through the viewer and reads the answer again.

**For the next worker**
- A value leaves a mounted component by being rendered. The `Runs` component in `rust-dioxus/tests/diff_viewer/reading_a_diff.rs` takes the answer as a prop and the test reads it back with `MountedApp::texts_with_class`; both fold actions activity WORK:activity-bx7yxakmx2 adds can be observed the same way.
- A fold only ever hides entries lying far from every change, so both sides are always present on a hidden run. The absent-side case arises only among changed runs, which is where the tests exercise it.
- Activity WORK:activity-s1m3wbpec7 (Catch the records up to the delivered code) has `LineRun` to cover in the glossary as well as the live diff and its hooks.
- A toon patch has no append operator: patching a field replaces it whole, so a long `body` has to be rewritten entire. Read the record's current text first.


### 2026-09-19T23:55:00 · Coder · Open and refold from app code
<a id="work:report-h3wz30khpj"></a>
`WORK:report-h3wz30khpj`

The two remaining fold actions are delivered and the crate is green: 28 engine tests, 74 component tests, clippy clean over all targets. An app expands at a line it names or expands everything, and neither action names a fold.

**Commits**
- The commit carrying this entry adds both fold actions and the reverse half of the position-and-line-number relation.

**Decisions**
- The position-keyed door became `expand_fold_at_position` as the line-keyed `expand_fold_at_line` arrived beside it. Each name states what it is keyed by; leaving the older one as `expand_fold` would have put one semantic under two spellings at module-visible scope, which is the same drift in name form that WORK:advice-bdghwgkv53 (one position-to-line-number relation) guards against in code.
- `ExpandedFolds::expand` took a run of starts rather than gaining a second entry point for expanding everything. The basis-reset rule stays in one place, and one action writes the signal once whether it opens one fold or every fold.
- `position_of_line` resolves a line id by scanning the entries. A binary search looks available because each side's numbers ascend, but an entry absent on the named side breaks the monotone predicate `partition_point` needs, and the action runs on an app's call rather than in a render path.
- `Fold::hidden_positions()` replaced the range `hidden_line_runs` built inline, so the fold-covers-this-position test and the hidden-run answer read one span rather than two spellings of it.

**Observations**
- `HiddenLines` and `hidden_line_runs()` still sit in one namespace, as the previous entry recorded. Untouched here; low severity, and the pair is a post-work reader's call.

**For the reviewer**
- The mechanical one-relation check the previous entry named still holds: `grep -rn '\.number' rust-dioxus/src/` outside `entry_line_numbers.rs` matches only `row_rendering.rs`, where every read builds a gutter's line id, a row key, or the fold-row renderer's payload. The new reverse lookup went into `entry_line_numbers.rs` beside `line_run_of`, so no second reckoning appeared.
- REQT-g86vdmyyp9's second sentence, that a named line which is not hidden leaves the shown lines alone, is covered twice because it fails two ways: `naming_a_line_that_is_already_shown_leaves_the_shown_lines_alone` for a line the diff holds and shows, and `naming_a_line_the_diff_does_not_hold_leaves_the_shown_lines_alone` for a line it holds nowhere. The second is the one a resolution falling back to the first fold would break.
- REQT-dxbaat20ja (One plan per live diff) gained a second cover, `a_fold_an_app_opens_by_naming_a_line_opens_in_every_viewer`. The requirement's own wording is a fold opened through the live diff, while the existing cover opens one through a viewer's fold row.

**For the next worker**
- The row plan is read and its guard released before `expanded_folds` is written. Both new actions resolve their starts in a helper that returns owned values, and the write happens after that helper returns; reading the plan inside the write's scope would hold a guard on a memo over the very signal being written.
- `MountedApp::click_button` matches a button by its exact text, and a fold row's own control reads "Expand N lines ...", so an app-code button in a fold test needs a label no fold row can produce.
- Activity WORK:activity-s1m3wbpec7 (Catch the records up to the delivered code) has three fold actions to describe where the architecture records still show a reset-only handle, alongside `LineRun` and the live diff's own names.






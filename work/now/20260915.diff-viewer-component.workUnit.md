
# Diff viewer Dioxus component

`WORK-6ds59btb76` · created 2026-09-14 · slug `diff-viewer-component`
state `reviewing` · maturity `DREAMED` · kind `preplanned` · source `ad-hoc` · discipline `coding` · variety `greenfield`
importance `high` · urgency `med` · priority `high` · epic `EPIC-77znwqj94w`

> **JSONL is authoritative.** This document is auto-generated from `WORK-6ds59btb76`'s `.workUnit.jsonl`. Do not hand-edit; changes will be overwritten on the next write.



## Status Summary

In post-work review, resolving findings one at a time. All twelve open findings under AUDIT:session-99pe3an9sj are triaged to in-review with presentations; sign-offs are at findings-review. Resolution order: F14 (public behavior without requirements), F1 (architecture capture), F2 (glossary coverage), F3 (content renderer payload), then the low-priority findings. Next: present F14 with the orientation; after the last seal, the final stage records commits and advances requirement statuses.

## Problem / Context

- Dioxus apps need a diff viewer they can mount: split or inline views, unchanged lines folded around changes, line numbers, highlighted and clickable lines, titles, and consumer rendering of line content and fold rows.
- The reference is `src/index.tsx`; the component renders the line diff engine's output rather than diffing on its own.

## Plan / Guidance

- Every in-scope requirement binds acceptance alongside the criteria.
- Default values generally follow react-diff-viewer, per stakeholder steering; public names follow the requirement terms, as the engine's do, never the reference's prop names.
- Before work-ready, pre-work settles with the Architect the Dioxus forms of fold reset, the content renderer, and the fold-row renderer, captured as candidate architecture that implementation holds to.
- Palettes, themes, and style overrides belong to the theming work unit; this unit renders markup that theming styles.
- Trimmed line comparison and its whitespace change marking land in the engine as part of this unit.
- Risk signals carried from the epic: implementation-complexity and architectural-uncertainty; the stakeholder chose one work unit over a paired split.



## Required Skills

- [`skillz/coder/coder.SKILL.md`](../skillz/coder/coder.SKILL.md) — roles: `implementer`

- [`skillz/rust-coder/rust-coder.SKILL.md`](../skillz/rust-coder/rust-coder.SKILL.md) — roles: `implementer`

- [`skillz/rust-observability/rust-observability.SKILL.md`](../skillz/rust-observability/rust-observability.SKILL.md) — roles: `implementer`

- [`skillz/logging/logging-rs.SKILL.md`](../skillz/logging/logging-rs.SKILL.md) — roles: `implementer`

- [`skillz/dioxus-coder/dioxus-coder.SKILL.md`](../skillz/dioxus-coder/dioxus-coder.SKILL.md) — roles: `implementer`

- [`skillz/dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md`](../skillz/dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md) — roles: `implementer`

- [`skillz/appshell-expert/appshell-expert.SKILL.md`](../skillz/appshell-expert/appshell-expert.SKILL.md) — roles: `implementer`

- [`skillz/schemata-crafter/data-forms/forms-expert/forms-expert.SKILL.md`](../skillz/schemata-crafter/data-forms/forms-expert/forms-expert.SKILL.md) — roles: `implementer`

- [`skillz/rust-tester/rust-tester.SKILL.md`](../skillz/rust-tester/rust-tester.SKILL.md) — roles: `domain-tester`, `prework-advice`, `postwork-audit`

- [`skillz/code-whisperer/code-whisperer.SKILL.md`](../skillz/code-whisperer/code-whisperer.SKILL.md) — roles: `prework-advice`, `postwork-audit`

- [`skillz/architect/architect.SKILL.md`](../skillz/architect/architect.SKILL.md) — roles: `reviewer`, `prework-advice`, `postwork-audit`




## References

**ARCH**
- `ARCH-m4dkxzw6hh` — diff viewer _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-exgfx6rwdt` — engine output _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-atczcqvdsz` — line diff hand-off _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-y9545npjzg` — fold reset trigger _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-rs0yp4vc5q` — line content renderer _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-jakkqkh6e1` — fold row renderer _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-sx4az00gan` — line number click handler _(role: scope)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-kjjykjtt5r` — paired line entry _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-n7wmjnmt65` — line diff options _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`


**REQT**
- `REQT-2k7j51afde` — views _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-m9r3k5b1ge` — hidden line numbers _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-qerexp825r` — folding _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-p2gkf77kjj` — line selection _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-3ekk7hre3k` — content rendering _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-3928hx46s3` — styling hooks _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-rvcg21axa8` — compare methods _(role: context)_ · `diff-viewer.reqts.jsonl`

- `REQT-wqffp3d26e` — line changes _(role: context)_ · `diff-viewer.reqts.jsonl`

- `REQT-smd01rma2q` — independent numbering _(role: context)_ · `diff-viewer.reqts.jsonl`

- `REQT-z9r0pc53jg` — trimmed line comparison _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-hq1fzjaxg8` — whitespace change marking _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-27a1gxq15f` — whitespace chips _(role: scope)_ · `diff-viewer.reqts.jsonl`


**WORKUNIT**
- `WORK-p7e70a5mwf` — line diff engine _(role: dependency)_ · `20260915.line-diff-engine.workUnit.jsonl`

- `WORK-98bxwcd28f` — programmatic control, spun out _(role: context)_ · `20260916.diff-viewer-programmatic-control.workUnit.jsonl`


**OTHER**
- `EPIC-77znwqj94w` — port epic _(role: context)_ · `rust-dioxus-port.workEpic.jsonl`

- `MISSION.md` — port mission _(role: context)_ · `MISSION.md`





## Focus Files

- `rust-dioxus/` — The crate the component joins, beside the line diff engine.

- `src/index.tsx` — Reference component behavior being ported.

- `test/react-diff-viewer-test.tsx` — Reference component tests.

- `README.md` — Props and instance methods needing a Rust equivalent or a recorded omission.






## Activities


### Split and inline views
<a id="work:activity-fqnecph977"></a>
`WORK:activity-fqnecph977`

Render split and inline views from the line diff engine's line information.






### Folding
<a id="work:activity-e4p0fbwfyn"></a>
`WORK:activity-e4p0fbwfyn`

Fold unchanged lines around changes, with fold rows, expansion, and reset.





**Advice**
- _Architect_ · state `addressed` · type `design` · priority `high` — REQT-869jyzdes7 (Fold reset) has no Dioxus form. The reference resets folds through an imperative instance method reached by ref, which Dioxus components do not have; nothing in the plan or the architecture says how a consuming app reaches the fold state, and the component cannot be written until it does. `WORK:advice-w2s1am4dsx`
  - **evidence**: src/index.tsx:127-135 defines resetCodeBlocks() as a public class method, commented "Will be exposed to the parent component via refs", returning true when expandedBlocks was non-empty and false otherwise; src/index.tsx:73 and :119 hold expandedBlocks in component state; README.md:86 lists it as the package's only instance method. .project/requirements/diff-viewer.reqts.md REQT-869jyzdes7 states only that the viewer MUST let the consumer return every expanded fold to folded. ARCH-m4dkxzw6hh (DiffViewer) in .project/architecture/dioxus-diff-viewer.arch.md carries two activities and one responsibility and says nothing about state ownership. Dioxus has no component-instance handle: dioxus-coder.SKILL.md names use_context_provider and props as the boundary carriers, and flags a mutable Signal<T> prop as breaking one-way data flow.
  - **Fold reset has no Dioxus form** · decision `judgement-needed` · execution `agent-trivial`
    - _The reference resets folds through a ref method Dioxus components do not have, and nothing says who owns the fold state._
    - **Context**:
      ***Re-orienting***: fold reset in the component's Folding activity.
      ***Catch-up***:
      - Where: REQT-869jyzdes7 (Fold reset), one of four folding requirements.
      - Purpose: collapse every fold a reader expanded.
      - Situation: the reference uses an instance method reached by ref (src/index.tsx:127-135).
      - Goal: a Dioxus-shaped way for an app to reach that state.
    - **Key points**:
      - Dioxus components expose no instance handle; a parent reaches a child through props or context.
      - The reference keeps expandedBlocks in the component's own state, which is why reset needs an escape hatch.
      - Fold ids are minted by the component from the diff and the surrounding-line count, so a consumer cannot keep the set valid.
      - The requirement puts the obligation on the viewer: the consumer asks, the viewer resets.
      - The reference's reset also reports whether anything was expanded; nothing in the requirements asks for that.
    - **Resolution points**:
      - `crate-owned-fold-handle` (preferred): the crate exports a hook the app calls in its own component; the app passes the resulting opaque `Copy + PartialEq` handle to the viewer as a prop and calls a reset method on it. Fold state is a crate type mutated only through crate methods, and the app never sees a fold id. In-house precedent: json-schema-forms has the consumer hold a FormHandle through props while the crate keeps the frozen original, dirty tracking, and error registry.
      - `reset-token-prop`: the component keeps the state privately and watches a read-only counter the app bumps. Demoted: it encodes an action as a value change and gives the app no way to ask whether anything is expanded, which the reference's boolean return does provide.
      - `app-holds-expanded-folds`: withdrawn. Exporting the expanded set makes every consumer the keeper of ids the component mints, which is a reliability burden only the crate is equipped to carry.
    - **Substance**: Contract correctness and maintainability: the component's state ownership is decided once and every folding requirement, and the component's whole prop surface, sits downstream of it.
    - **Actionability**: Workable: the resolution is a sentence of plan guidance plus a candidate architecture record, before any code exists.
    - **Specificity** `under-design`: The plan names fold reset as something pre-work settles and leaves the mechanism open; the requirement puts the obligation on the viewer, and no record states how a consumer reaches it.
  - **Response**: The viewer keeps the expanded-fold state inside the crate, and a consumer resets it through an optional fold control the crate creates. REQT-869jyzdes7 (Fold reset) now states all three parts: the viewer holds the state, a consumer holding the fold control resets every expanded fold, and the control is optional so a consumer that never resets does not hold one. ARCH-y9545npjzg (FoldControl) is the candidate architecture for it, and ARCH-m4dkxzw6hh (DiffViewer) now names expanded-fold state and its control as a responsibility. The withdrawn direction and the reasoning error behind it are in CONTENT-8w5tydrbq7.
The Architect's first proposal handed the expanded-fold set to the app as a read-only prop. The stakeholder rejected it: state that needs reliable handling does not belong outside the crate. The fold ids are minted by the component from the diff and the surrounding-line count, so a consumer cannot keep the set valid, and the requirement puts the obligation on the viewer.

The Architect's supporting reasoning was also wrong in a narrower way, and the correction is worth keeping: the Dioxus caution against a mutable `Signal<T>` prop is about handing an app a raw signal over the component's own data, not about any consumer-reachable state. A crate-owned opaque handle is a different thing, and json-schema-forms is the in-house precedent for it.

The stakeholder then settled the remaining fork: a consumer that never resets must not have to construct a control. The control is therefore optional, and a viewer given none keeps its folds to itself.

After the seal, the stakeholder asked whether the handle is only for the reset. It is, so the term became fold reset trigger and ARCH-y9545npjzg was renamed FoldResetTrigger; "code" was dropped from the stakeholder's proposed code-folding-reset-trigger to match the requirements' folding vocabulary. The seal's response above names the record by its earlier label; the id is unchanged.

The stakeholder then asked whether the same hook could expose change positions, fold ranges, and the fold store for programmatic control, and whether the reference does anything similar. It is viable and the reference does not: its surface is props in, a line-number click out, and one reset method. The stakeholder chose to keep this unit's handle reset-only and spin the capability out as WORK-98bxwcd28f (Programmatic control of the diff viewer), which depends on this unit.



### Line numbers and selection
<a id="work:activity-4tsysj7qfw"></a>
`WORK:activity-4tsysj7qfw`

Show line numbers, highlighted lines, and line-number clicks.






### Titles and consumer rendering
<a id="work:activity-5ahsmdx1ws"></a>
`WORK:activity-5ahsmdx1ws`

Show column titles, and render line content and fold rows through consumer renderers.





**Advice**
- _Architect_ · state `addressed` · type `design` · priority `high` — Three consumer-supplied callables reach into the component's rendering, and none has its payload named in the records: the content renderer, the fold-row renderer, and the line-number click handler. The requirements say what each receives in reference terms; what a Dioxus consumer hands in, and what each gets back on a modified line or a fold row, is unstated. `WORK:advice-b9meb8srdt`
  - **evidence**: src/index.tsx:47 declares renderContent as (source: string) => JSX.Element, called per whole line at :232 and per inline token at :230 through renderWordDiff (:172-190), so the reference calls it once per token on a modified line. src/index.tsx:49-53 declares codeFoldMessageRenderer(totalFoldedLines, leftStartLineNumber, rightStartLineNumber), called at :420-424. src/index.tsx:55-58 declares onLineNumberClick(lineId, event), wired through onLineNumberClickProxy at :167-172 and attached at :242 and :257. The matching requirements REQT-vxtax4x0vs (Custom line content), REQT-1tdrfvay4q (Fold rows) and REQT-bqm9w6v4ms (Line number clicks) name the data but no Rust-side shape. ARCH-m4dkxzw6hh (DiffViewer) carries no software_object for any of the three, and the arch file's Software Objects section holds only the engine's three records.
  - **Three consumer callables have no named payload** · decision `judgement-needed` · execution `agent-trivial`
    - _The content renderer, fold-row renderer, and line-number click handler are named in reference terms only; no record says what each receives._
    - **Context**:
      ***Re-orienting***: the component's three consumer-supplied callables.
      ***Catch-up***:
      - Where: REQT-vxtax4x0vs, REQT-1tdrfvay4q, REQT-bqm9w6v4ms.
      - Purpose: let an app shape line text, fold-row text, and clicks.
      - Situation: the reference passes JS closures returning JSX (src/index.tsx:47-58).
      - Goal: each payload stated where worker and app both read it.
    - **Key points**:
      - The content renderer is called per whole line and again per inline-change token on a modified line, while the viewer keeps the change wrapping around each token; a highlighter sees fragments on modified lines, which REQT-vxtax4x0vs already settles.
      - The fold-row renderer gets the hidden-line count and the first hidden line's two numbers, already settled in REQT-1tdrfvay4q; those are document coordinates, so no fold id reaches the app.
      - The reference also passes the mouse event to the click handler; it is what lets an app build range selection, which the README presents as a supported use of highlighted lines.
      - None of the three has an architecture record, so the worker would invent all three payloads.
    - **Resolution points**:
      - `payloads-as-candidate-architecture` (preferred): three software_object records on the DiffViewer, named from requirement terms: LineContentRenderer (a whole line's text or one inline-change token's text, to rendered content), FoldRowRenderer (hidden-line count and the first hidden line's old and new numbers, to rendered content), LineNumberClickHandler (the clicked line's id plus which modifier keys were held, returning nothing); REQT-bqm9w6v4ms amended to add the modifier keys.
      - `click-carries-id-only`: the handler receives the id alone and the event is recorded as an omission. Demoted: it drops the signal an app needs to build range selection.
      - `click-carries-framework-event`: the handler receives the Dioxus mouse event itself. Demoted: it couples every consuming app to the framework's event type, where plain modifier data carries what apps use.
    - **Substance**: Contract correctness: these three payloads are the component's public boundary with its apps, and a wrong guess is a breaking change after release.
    - **Actionability**: Workable: three records at the altitude the arch already uses for the engine's three objects.
    - **Specificity** `under-design`: The plan names the content renderer and the fold-row renderer as pre-work's to settle; the requirements state each callable's data and no record states its shape.
  - **Response**: The three callables are candidate architecture on the DiffViewer: ARCH-rs0yp4vc5q (LineContentRenderer), ARCH-jakkqkh6e1 (FoldRowRenderer), and ARCH-sx4az00gan (LineNumberClickHandler), each naming what it receives and returns. REQT-bqm9w6v4ms (Line number clicks) now has the handler receive the held modifier keys alongside the line id, as plain data. Nothing is omitted from the three payloads. Dialogue in CONTENT-sk6b4m55dg.
The Architect first proposed recording the mouse event as an omission. Re-examined before presenting, the event turned out to be what lets an app build range selection, which the reference's README presents as a supported use of highlighted lines, so the proposal became the line id plus held modifier keys as plain data. The Rust Tester noted plain data keeps the handler testable without constructing a framework event; the Code Whisperer tied the three names to the naming rule still under review. The stakeholder accepted the proposal.



### Requirement tests
<a id="work:activity-49kbss9d77"></a>
`WORK:activity-49kbss9d77`

Cover every in-scope requirement with tests.





**Advice**
- _Rust Tester_ · state `in-review` · type `recommendation` · priority `high` — No test strategy is recorded, and greenfield work-ready requires one. The engine's strategy does not carry over: its entry point is a pure function, while every requirement in this unit is about rendered output or a reaction to an event. Proposed strategy:
- Rendered-output tests over a mounted component, one focused test per in-scope child requirement, named as a plain-English behavior carrying the REQT id.
- A shared fixture that mounts the component with given texts and props and yields the rendered rows, so each test reads as arrange-act-assert over rows rather than over framework calls.
- Event-driven tests for fold expansion, fold reset, and line-number clicks: activate the row, pump the loop, read the rows again. VirtualDom::process_events and render_immediate are the driving pair, with rebuild_in_place for the first paint.
- Assertions on row structure and marking, not on markup strings: which rows are present, each row's side, number, change marker, and the token kinds within a modified row.
- Styling hooks are the one place markup attributes are the contract: assert each element kind's `dxdiff` class and its change or highlight state class, not the element structure around them.
- A table of reference cases from test/react-diff-viewer-test.tsx, each named by its reference case; a case that differs asserts this component's own output and states its cause beside it.

Sketch:
```
rows = render("a\nb", "a\nc", Props::default())
assert rows == [unchanged(L1, R1, "a"), modified(L2, R2, "b" -> "c")]   // REQT-ys3yr5g185
rows = render(long_text, long_text_with_one_change, Props::default())
assert rows contains exactly one fold row reading "Expand 12 lines ..."   // REQT-1tdrfvay4q
click(fold_row); pump();
assert no fold row remains and the hidden lines are present             // REQT-v748c7mjr6
``` `WORK:advice-kxmvd4g6zg`
  - **evidence**: coder/coder.codingDiscipline.reqts.md REQT-2mge0tjtxq lists an articulated test strategy as greenfield work-ready condition (d). The work unit carried no work_advice before this pass. dioxus-core-0.7.9/src/virtual_dom.rs:544 (rebuild_in_place), :494 (process_events), :599 (render_immediate) and src/mutations.rs:384 (NoOpMutations) are present in the local registry and are the documented manual-driving pathway; dioxus-coder.SKILL.md's testing table names the same pair and records that Dioxus 0.7 has no first-class hook test framework. dioxus-ssr is not vendored in the local cargo registry at the capture-time check, so whether render_element serves the rendered-output tests is unverified here and is the worker's first thing to settle. test/react-diff-viewer-test.tsx is named as a focus file and holds the reference component cases. rust-tester.SKILL.md ranks output-based testing first and requires that a framework's own skill own the in-framework harness shape.
  - **Test strategy for the component** · decision `advisory` · execution `not-needed`
    - _No test strategy is recorded; this proposes rendered-output tests, an event-driven path for folds and clicks, and the reference-case table._
    - **Context**:
      ***Re-orienting***: the engine's test strategy does not carry over.
      ***Catch-up***:
      - Where: the Requirement tests activity of this unit.
      - Purpose: show every in-scope requirement holds.
      - Situation: greenfield work-ready needs an articulated strategy, and the engine's was for a pure function.
      - Goal: a strategy the worker follows and post-work can check.
    - **Key points**:
      - Every requirement here is about rendered rows or a reaction to an event, so the tests mount a component rather than call a function.
      - Fold expansion, fold reset, and line-number clicks need the event loop driven, not just a first paint.
      - Assertions go on row structure rather than markup strings, so the tests survive restyling.
      - Dioxus 0.7 has no first-class harness; the manual VirtualDom pathway is the documented one, and the rendered-output crate is the worker's first thing to confirm.
    - **Resolution points**:
      - `adopt-strategy`: keep this advice open as the worker's test plan.
    - **Substance**: Test coverage: gives post-work review a checkable coverage map, one named test per in-scope child requirement.
    - **Actionability**: Workable, with one open item the worker settles first: which crate renders a mounted component for assertions.
    - **Specificity** `under-design`: The test approach was unstated; the strategy names the shape and leaves the harness details to the worker.
Re-read after REQT-3928hx46s3 (Styling hooks) joined the scope, the strategy's no-markup-strings rule left the class vocabulary untested; the Rust Tester added a bullet asserting each element kind's `dxdiff` class and state class. The Code Whisperer noted class assertions survive restyling, matching the requirement's own purpose. The stakeholder adopted the strategy; the advice stays open as the worker's test plan. At the re-read, `dioxus-ssr` was still absent from the local cargo registry and the crate had no dioxus dependency, so confirming the rendered-output harness remains the worker's first step.





## Acceptance Criteria

- [ ] A Dioxus app can show a diff by giving the component two strings and nothing else. · maturity `DREAMED`

- [ ] Every README prop and instance method outside theming (`styles`, `useDarkTheme`) has a Rust equivalent on the component, or an `exclusions` entry on ARCH-m4dkxzw6hh (DiffViewer) naming what is omitted and why. · maturity `DREAMED`

- [ ] The theming work unit can apply palettes and overrides without restructuring the component's markup. · maturity `DREAMED`

- [ ] The TypeScript package at the repo root is unchanged. · maturity `DREAMED`




## Sign-Offs

### Pre-Work

- **Architect** — state `approved-with-notes` · 2026-09-16
  - Scope is bounded to the DiffViewer; it and its four candidate records are referenced as scope, and implementation holds to them. Requirements cover every in-scope behavior. Record each omitted README prop or method as an exclusions entry on the DiffViewer.

- **Code Whisperer** — state `approved-with-notes` · 2026-09-16
  - Public names follow the requirement terms, as the engine's do; styling classes carry the dxdiff prefix. Naming decisions worth curating go to the project glossary.

- **Rust Tester** — state `approved-with-notes` · 2026-09-16
  - The test strategy in WORK:advice-kxmvd4g6zg is the worker's test plan and stays open; confirm the rendered-output harness first.



### Post-Work

- **Architect** — state `findings-review` · audit `AUDIT:session-99pe3an9sj`

- **Code Whisperer** — state `findings-review` · audit `AUDIT:session-99pe3an9sj`

- **Rust Tester** — state `findings-review` · audit `AUDIT:session-99pe3an9sj`





## Advice (work-unit scope)

- _Architect_ · state `addressed` · type `design` · priority `high` — The third acceptance criterion promises the theming work unit can apply palettes and overrides without restructuring this component's markup, but nothing in this unit says what the markup carries for theming to attach to. The criterion is checkable only after the theming design exists, and that design is the very thing WORK-b86cyjcn7m is waiting on, so a markup choice made here can be discovered wrong only in the next unit. `WORK:advice-3rcdz9e2bd`
  - **evidence**: src/styles.ts:4-26 names 23 style keys the reference attaches per element (diffContainer, gutter, marker, content, diffAdded, diffRemoved, highlightedLine, highlightedGutter, wordAdded, wordRemoved, codeFold, codeFoldGutter, titleBlock, splitView and others), and :28-53 names 24 theme variables behind them. .project/work/rust-dioxus-port.workEpic.md records WORK-b86cyjcn7m as backlog with plan "Settle the Rust style-override design during planning, before this chunk is work-ready" and risk signal architectural-uncertainty. The work unit's third work_crit (WORK:crit-4b44c6vysr) states the no-restructuring promise; no work_ref, activity, or requirement in this unit names a styling hook.
  - **The markup's theming hook is unnamed** · decision `judgement-needed` · execution `agent-trivial`
    - _A criterion promises theming can style the viewer without restructuring it, while the styling design it must match is still unsettled._
    - **Context**:
      ***Re-orienting***: the markup's theming hook, across the whole component.
      ***Catch-up***:
      - Where: the unit's third acceptance criterion (WORK:crit-4b44c6vysr).
      - Purpose: let the theming unit style the viewer without reshaping it.
      - Situation: the Rust override design is still unsettled in WORK-b86cyjcn7m.
      - Goal: a hook this unit can be checked against now.
    - **Key points**:
      - The criterion is a promise about a later unit's experience, so this unit's post-work cannot check it.
      - The theming unit is held in backlog precisely until its override design is settled, so the promise has nothing to match against.
      - The reference attaches 23 named style keys per element and 24 theme variables behind them, so the shape of what theming needs is already known.
      - A markup choice made here without the hook is discovered wrong only after this unit closes.
    - **Resolution points**:
      - `styling-hooks-as-requirement` (preferred): a new child 3.2.3 Styling hooks under REQT-3cb9k1mg2d (Themes), referenced from this unit with role scope, requiring a stable, prefixed class name per element kind (title, row, gutter, change marker, content cell, inline-change token, line ending chip, fold row) plus classes for change and highlight state. The worker writes the class vocabulary and post-work captures it before theming reads it; how themes supply colors stays with the theming unit.
      - `hooks-in-plan-guidance`: state the same in plan guidance only. Demoted: the hooks become an internal habit rather than a contract, so nothing stops a later change from renaming what apps already style against.
    - **Substance**: Requirements coverage and maintainability: an unbacked criterion is unfalsifiable at post-work, and rework lands on the blocked theming unit.
    - **Actionability**: Workable: one plan-guidance sentence, or an added requirement if the hook should bind acceptance.
    - **Specificity** `under-design`: The criterion specifies an outcome whose enabling property nothing in the plan names; the styling mechanism itself stays out of scope.
  - **Response**: REQT-3928hx46s3 (Styling hooks), a new child under REQT-3cb9k1mg2d (Themes), requires a stable class per element kind plus change and highlight state classes, all prefixed with `dxdiff`, and this unit references it with role scope, so the third criterion is checkable at this unit's post-work. The rest of the class vocabulary is the worker's to write and post-work's to capture; how themes supply colors stays with WORK-b86cyjcn7m. Dialogue in CONTENT-sq6gscwygq.
The Architect proposed making the styling hooks a requirement rather than plan guidance, so the class vocabulary is a contract apps can rely on and the third criterion becomes checkable at this unit's post-work. The Code Whisperer added that a crate prefix keeps the vocabulary from colliding with app classes, following appshell and json-schema-forms. The stakeholder accepted.

The first write of the requirement dropped the prefix; the Architect caught it before commit, and the stakeholder set the prefix to `dxdiff` rather than leaving it to the worker.
- _Work Planner_ · state `addressed` · type `scope-refinement` · priority `med` — The second acceptance criterion accepts "a recorded omission with its reason" for any README prop without a Rust equivalent, and names no home for that record. A reviewer checking the criterion has nowhere to look, and a worker recording one has to invent a place. `WORK:advice-6zdq9z6afm`
  - **evidence**: The work unit's second work_crit (WORK:crit-3n3bdspx22) reads "Every README prop and instance method outside theming (styles, useDarkTheme) has a Rust equivalent on the component or a recorded omission with its reason." README.md:62-83 lists seventeen props and :86 the one instance method. architect.SKILL.md's Explicit Exclusions section requires scope-clarifying material to become structured exclusions entries on the relevant component rather than prose; ARCH-jf3s5npp9s (LineDiffEngine) already carries one such entry, visible in .project/architecture/dioxus-diff-viewer.arch.md, while ARCH-m4dkxzw6hh (DiffViewer) carries none.
  - **A recorded omission has no home** · decision `obvious-essential` · execution `agent-trivial`
    - _The prop-parity criterion accepts a recorded omission with its reason and names no place for a reviewer to look or a worker to write._
    - **Context**:
      ***Re-orienting***: where a recorded omission lives.
      ***Catch-up***:
      - Where: the unit's second acceptance criterion (WORK:crit-3n3bdspx22).
      - Purpose: account for every README prop the port does not carry.
      - Situation: the criterion accepts a recorded omission and names no file.
      - Goal: one place a reviewer can check and a worker can write.
    - **Key points**:
      - Seventeen props and one instance method are in view, and several will not carry over as written.
      - Without a named home, each omission lands wherever the worker happens to be writing.
      - The architecture already carries this kind of material: the engine's record holds its exclusion for views, folding, and styling.
    - **Resolution points**:
      - `omissions-as-arch-exclusions`: the criterion names the DiffViewer's `exclusions` entries as the home, matching how the engine's boundary is already recorded, and each entry names the prop and why it does not carry over.
    - **Substance**: Requirements coverage: the criterion's second branch is the one that covers whatever the port drops, and today it is uncheckable.
    - **Actionability**: Workable: one clause added to the criterion.
    - **Specificity** `under-design`: The criterion specifies an outcome and leaves its evidence location unstated; naming the location adds the missing expectation without narrowing the outcome.
  - **Response**: The second acceptance criterion (WORK:crit-3n3bdspx22) now names the home: an omitted README prop or instance method is recorded as an `exclusions` entry on ARCH-m4dkxzw6hh (DiffViewer), naming what is omitted and why. Dialogue in CONTENT-1x8hvt5vxa.
The Work Planner proposed naming the DiffViewer's `exclusions` entries as the home, matching the engine record's existing entry and the Architect skill's rule that scope-clarifying material becomes structured exclusions. At least one omission is already known: the reference's `resetCodeBlocks()` reports whether anything was expanded, and the fold reset trigger returns nothing. The Architect added that exclusions render beside the engine's in the generated architecture and that greenfield work-verified requires resolved ARCH records, so post-work reads them anyway. The stakeholder accepted the proposal without adding a consumer-facing record of omissions in the crate's docs.
- _Architect_ · state `addressed` · type `documentary` · priority `low` — The reference set predates the engine's completed architecture. ARCH-m4dkxzw6hh (DiffViewer) is referenced as context although this unit elaborates it from a placeholder into the real component; the two records the component reads its input through, PairedLineEntry and LineDiffOptions, are not referenced at all; and the three engine requirements whose options this component forwards to its consumers are absent. `WORK:advice-aqzepxx69n`
  - **evidence**: The unit's eleven work_ref records name ARCH-m4dkxzw6hh with role context, plus ARCH-exgfx6rwdt and ARCH-atczcqvdsz. ARCH-m4dkxzw6hh reads "placeholder until the component work elaborates it" in .project/architecture/dioxus-diff-viewer.arch.md, and work-planner.SKILL.md defines role scope as part of what this unit changes against context as related with no ordering claim. ARCH-kjjykjtt5r (PairedLineEntry) carries the per-row shape the component renders and ARCH-n7wmjnmt65 (LineDiffOptions) the choices it forwards; neither is referenced. README.md:62-83 lists compareMethod, disableWordDiff and linesOffset as props, whose behavior lives in REQT-rvcg21axa8 (Compare methods), REQT-wqffp3d26e (Line changes) and REQT-smd01rma2q (Independent numbering), none referenced here.
  - **The reference set predates the engine's architecture** · decision `obvious-essential` · execution `agent-trivial`
    - _DiffViewer is referenced as context although this unit elaborates it, and the records carrying the component's input are not referenced at all._
    - **Context**:
      ***Re-orienting***: this unit's work_ref records.
      ***Catch-up***:
      - Where: the eleven references on WORK-6ds59btb76.
      - Purpose: tell a worker and a reviewer what to load.
      - Situation: written before the engine's post-work finished its architecture.
      - Goal: the set names what this unit changes and what it reads.
    - **Key points**:
      - ARCH-m4dkxzw6hh (DiffViewer) is what this unit turns from a placeholder into the real component, which is the scope role.
      - ARCH-kjjykjtt5r (PairedLineEntry) is the per-row shape every view renders, and ARCH-n7wmjnmt65 (LineDiffOptions) the choices the component forwards; neither is referenced.
      - Three README props are engine options whose requirements are unreferenced here.
    - **Resolution points**:
      - `complete-the-reference-set`: change ARCH-m4dkxzw6hh to role scope, and add context references for ARCH-kjjykjtt5r, ARCH-n7wmjnmt65, REQT-rvcg21axa8, REQT-wqffp3d26e, and REQT-smd01rma2q.
    - **Substance**: Requirements coverage: the loading discipline reads what the references point at, so an incomplete set means a worker who never sees the shape it renders.
    - **Actionability**: Workable: one role change and five added records.
    - **Specificity** `not-design-related`: Not a design question; the plan's content is unaffected and only its reference records are incomplete.
  - **Response**: ARCH-m4dkxzw6hh (DiffViewer) is now referenced with role scope. Its four candidate records join it as scope: ARCH-y9545npjzg (FoldResetTrigger), ARCH-rs0yp4vc5q (LineContentRenderer), ARCH-jakkqkh6e1 (FoldRowRenderer), and ARCH-sx4az00gan (LineNumberClickHandler). ARCH-kjjykjtt5r (PairedLineEntry), ARCH-n7wmjnmt65 (LineDiffOptions), REQT-rvcg21axa8 (Compare methods), REQT-wqffp3d26e (Line changes), and REQT-smd01rma2q (Independent numbering) are referenced as context. Dialogue in CONTENT-ze1jan4cpg.
Re-read before presenting, the reference set also lacked the four DiffViewer software objects this pre-work added after the advice was filed, from the fold reset and consumer-callables resolutions; the Architect added them to the proposal with role scope, since this unit builds them. The Rust Tester noted the engine requirements stay context: their tests belong to the engine unit, and this unit's obligation is exposing their options as props, which the second acceptance criterion already carries. The stakeholder accepted.
- _Code Whisperer_ · state `addressed` · type `scope-refinement` · priority `high` — The crate's public names follow the requirement terms, settled on the engine unit and recorded only in that unit's plan guidance. This unit's plan guidance does not carry the rule, so a worker reading only this unit takes the reference's names for a prop surface of seventeen items, and the crate ends up with two naming schemes across its two modules. `WORK:advice-qdepp4erwb`
  - **evidence**: WORK-p7e70a5mwf's plan_guidance in .project/work/done/20260915.line-diff-engine.workUnit.md names the crate dioxus-diff-viewer and draws public names from the requirement terms rather than the reference's DiffType.DEFAULT, diffLines, or disableWordDiff; its Code Whisperer pre-work sign-off records "Public names follow the requirement terms." The engine's delivered names follow it: rust-dioxus/src/line_diff_options.rs uses compare, mark_inline_changes and line_offset where the reference has compareMethod, disableWordDiff and linesOffset. WORK-6ds59btb76's plan_guidance carries defaults-follow-the-reference and the theming boundary, and no naming rule. The reference's own names at README.md:62-83 include splitView, showDiffOnly, extraLinesSurroundingDiff and codeFoldMessageRenderer, against requirement terms split view, folding, surrounding-line count and fold row.
  - **The naming rule stayed on the engine unit** · decision `obvious-essential` · execution `agent-trivial`
    - _Public names follow the requirement terms, but that rule lives only in the engine unit's plan guidance, not in this one's._
    - **Context**:
      ***Re-orienting***: the component's public prop names.
      ***Catch-up***:
      - Where: this unit's plan guidance, against WORK-p7e70a5mwf's.
      - Purpose: keep one vocabulary across the crate's two modules.
      - Situation: the engine's names follow the requirements; this unit's guidance is silent.
      - Goal: a worker reading only this unit names props the same way.
    - **Key points**:
      - The engine's delivered names read compare, mark_inline_changes, line_offset where the reference reads compareMethod, disableWordDiff, linesOffset.
      - This unit's guidance says defaults follow the reference, which a worker can easily read as names following it too.
      - Seventeen props are in view, so the divergence would be the larger half of the crate's public vocabulary.
      - No glossary covers this project, so the plan text is the only place the rule can live.
    - **Resolution points**:
      - `carry-the-naming-rule`: this unit's plan guidance states that public names follow the requirement terms, as the engine unit's does, and that defaults-follow-the-reference is about values rather than names.
    - **Substance**: Maintainability: public names are costly to change once apps depend on them, and a split vocabulary inside one crate is visible to every reader.
    - **Actionability**: Workable: one plan-guidance sentence.
    - **Specificity** `under-design`: The plan specifies where defaults come from and leaves the naming source unstated; the rule already exists and is simply not carried here.
  - **Response**: This unit's plan guidance now states that default values follow react-diff-viewer while public names follow the requirement terms, as the engine's do, never the reference's prop names. Dialogue in CONTENT-jcb0cvjjek.
The Code Whisperer proposed carrying the engine unit's naming rule into this unit's plan guidance, splitting defaults (values follow the reference) from names (follow the requirement terms). The Architect noted the crate record already names public vocabulary drawn from requirement terms as a responsibility. Keeping the reference's prop names for React porters collapsed against that responsibility and the engine's delivered names. The stakeholder accepted, and asked for a project glossary and principles registry so naming details like these have a place to be curated.
- _Work Planner_ · state `addressed` · type `design` · priority `med` — Greenfield work-ready needs a person's assent to building according to the in-scope requirements (REQT-2mge0tjtxq, condition (a)), and no record shows it for this unit's five groups. The assent is gathered in dialogue and moves nothing on any requirement's maturity; this record carries it. `WORK:advice-7de9eswjzm`
  - **evidence**: .project/requirements/diff-viewer.reqts.md carries the five in-scope groups REQT-2k7j51afde (Views, four children), REQT-qerexp825r (Folding, four children), REQT-p2gkf77kjj (Line selection, two children), REQT-3ekk7hre3k (Content rendering, two children) and the single child REQT-m9r3k5b1ge (Hidden line numbers), all at maturity DREAMED and impl_status BACKLOG. coder/coder.codingDiscipline.reqts.md REQT-2mge0tjtxq lists a person's assent as work-ready condition (a). work-planner.SKILL.md's Requirements Clarity step 5 states the assent is dialogue with no record ceremony and never advances maturity. The unit's status_summary names pre-work as still owed.
  - **In-scope requirements await assent to build** · decision `judgement-needed` · execution `not-needed`
    - _Greenfield work-ready needs a person's assent to the five in-scope requirement groups, and no record shows it._
    - **Context**:
      ***Re-orienting***: the requirement consent gate for the component.
      ***Catch-up***:
      - Where: the five in-scope groups of WORK-6ds59btb76.
      - Purpose: settle what the worker must deliver.
      - Situation: every group and child sits at DREAMED and BACKLOG.
      - Goal: consented scope before work starts.
    - **Key points**:
      - Greenfield work-ready condition (a) is a person's assent to building per the in-scope requirements.
      - The assent is dialogue: it moves nothing on any requirement's maturity, and only steering of the text itself would.
      - Three other findings may amend requirement text first, so the assent comes after they resolve.
    - **Resolution points**:
      - `assent-after-amendments`: once the fold reset, renderer contracts, and theming hook findings resolve, present the in-scope requirements and gather the assent; this record carries it.
    - **Substance**: Requirements coverage: the gate that makes the in-scope requirements binding on the worker is unmet.
    - **Actionability**: Workable once the three amendment-bearing findings resolve.
    - **Specificity** `calibrated`: The requirement text is specific; only its consent state is open.
  - **Response**: The stakeholder assented to building according to the in-scope requirements (Views, Hidden line numbers, Folding, Line selection, Content rendering, Styling hooks) as they read after the fold reset, consumer-callables, and styling-hooks resolutions. Assent moves no requirement's maturity. Dialogue in CONTENT-8bhd83kgqv.
  - depends on `WORK:advice-w2s1am4dsx`: Fold reset may amend REQT-869jyzdes7
  - depends on `WORK:advice-b9meb8srdt`: Renderer contracts may amend three requirements
  - depends on `WORK:advice-3rcdz9e2bd`: Theming hook may add a requirement
With the three amendment-bearing findings sealed, the Work Planner re-read and presented each in-scope requirement statement and asked whether the unit builds to them. The stakeholder said yes.
- _Work Planner_ · state `addressed` · type `procedural` · priority `low` — Activity development for this pre-work session.

**Activities developed**
- None. All five activity descriptions hold at altitude; no narrower reading of any of them would satisfy its words and miss its intent.

**Resolved against the sources**
- Whether folding distance treats a pair differing only in its terminator as a change: closed in the records. ARCH-exgfx6rwdt (LineDiff) states the positions include such a pair, and the component's distances come from changed_positions rather than from each entry's change kind.
- Whether the component needs its own advice about file size: no. The ~600-line target is a shared principle that binds the worker without a record here.
- Whether views, folding, and the consumer renderers should be separate work units: closed by stakeholder decision, recorded in the epic.
- Whether a requirement is missing for the component accepting two texts: the first acceptance criterion carries it, and in-scope requirements plus criteria together are the scope.
- Whether the reference's dropped single trailing unchanged line should be ported: already closed as deliberately not carried over.

**Set aside as unanchored**
- Whether the component memoizes the engine call across renders or calls it each render: craft, entrusted to the worker.
- Whether the rows are a table or a grid: craft; the part that bears on a later unit is carried by the theming-hook advice. `WORK:advice-75g2zw0fn9`
  - **evidence**: .project/architecture/dioxus-diff-viewer.arch.md ARCH-exgfx6rwdt reads "positions of entries holding a change, including a pair whose terminators differ", matched by the changed_positions doc comment in rust-dioxus/src/line_diff_output.rs. skillz/.principles.md carries #prevent-megacode-files with the ~600-line target for regular code files. .project/work/rust-dioxus-port.workEpic.md records WORK-6ds59btb76 as "Kept as one work unit by stakeholder decision." The unit's first work_crit (WORK:crit-qbbvbrzptm) states that an app shows a diff by giving the component two strings and nothing else. WORK:report-e5srytp0k1 in this unit records src/index.tsx:507-515 dropping a single trailing unchanged line with no fold row, and that the folding requirements deliberately do not carry it over.
  - **Activity development for this pre-work session** · decision `advisory` · execution `not-needed`
    - _No activity needed developing; five candidates resolved against the sources and two set aside as craft._
    - **Context**:
      ***Re-orienting***: the session's coverage trail.
      ***Catch-up***:
      - Where: the five activities of WORK-6ds59btb76.
      - Purpose: show what this pass examined and closed on its own.
      - Situation: every activity read at altitude, so none was developed.
      - Goal: nothing the pass noticed goes unseen.
    - **Key points**:
      - All five activity descriptions hold; none would be satisfied by a narrower reading that misses its intent.
      - Five candidates closed against the records rather than becoming findings.
      - Two set aside as craft the worker owns.
    - **Resolution points**:
      - `record-as-the-coverage-trail`: the record stands as written; the items it lists are judged in their own right, not here.
    - **Substance**: Procedural: the visibility of what the pass examined, with no impact of its own.
    - **Actionability**: Workable: nothing to do beyond reading it.
    - **Specificity** `not-design-related`: The record describes the pass, not any design material in the plan.
  - **Response**: The coverage trail stands as written; each item it lists was judged in its own record.




## Audit Trail

### post-work

- **session** `AUDIT:session-99pe3an9sj` — WORK-6ds59btb76 (Diff viewer Dioxus component): the DiffViewer build, its engine additions, tests, and architecture capture · audit `post-work` · 2026-09-16 · status `in-progress` · expertise `Work Planner`, `Architect`, `Code Whisperer`, `Rust Tester`, `Auditor`, `REQM`, `Rust Coder`, `Dioxus Coder`, `Glossary-Aware Review`, `Octopus Content Coverage`, `Automatic Agent`
  - Independent analysis filed; triage and resolution pending.

Deliverable verification: all five activities built; 62 tests pass (35 component, 27 engine) and clippy reports nothing at the capture-time check. First criterion: views::two_texts_alone_show_old_lines_left_and_new_lines_right. Second: every README prop outside theming maps to a prop per WORK:report-sxyfvxcamh, with the WORDS/SENTENCES/CSS and resetCodeBlocks() return-value exclusions on ARCH-m4dkxzw6hh (DiffViewer). Third: tests/diff_viewer/styling_hooks.rs asserts classes, not structure. Fourth: git log since 2026-09-14 over src, test, README.md, package.json, yarn.lock, webpack.config.js returned no commits. Every in-scope child requirement has a named test. No `_u_` provisional identifiers in the changeset.

Related-architecture-set: dioxus-diff-viewer.arch.jsonl only; it holds no open_question records.

Lenses not loaded for this reading-only pass: Rust Observability, Logging, Dioxus UI Patterns, Appshell Expert, Forms Expert; the component has no I/O, no instrumentation, no app shell, and no forms. Load them before any remediation that writes Rust.

- **finding** `AUDIT:finding-nh5wz253yb` (F1) · type `coverage-gap` · severity `medium` · priority `high` · status `fixed` · reviewer _Architect_ · In dioxus-diff-viewer.arch.jsonl, records ARCH-m4dkxzw6hh (DiffViewer), ARCH-n7wmjnmt65 (LineDiffOptions), ARCH-kjjykjtt5r (PairedLineEntry)
  - The architecture has not caught up with what was built. DiffViewer still reads as a placeholder; LineDiffOptions lists three compare methods where four ship; PairedLineEntry omits the whitespace change marker; LineId and the public styling-hooks class vocabulary have no record; DiffViewer's supported requirements omit REQT-3928hx46s3 (Styling hooks).
  - **evidence**: dioxus-diff-viewer.arch.md DiffViewer summary reads "placeholder until the component work elaborates it"; LineDiffOptions definition reads "compare method (character, word, line)" against rust-dioxus/src/line_diff_options.rs:9-24 (Character, Word, Line, TrimmedLine); PairedLineEntry definition has no whitespace change against rust-dioxus/src/line_diff_output.rs:100-103; rust-dioxus/src/line_id.rs:12-17 (LineId) and rust-dioxus/src/styling_hooks.rs:10-50 (public dxdiff constants) have no software_object; DiffViewer supports REQT-2k7j51afde, REQT-qerexp825r, REQT-p2gkf77kjj, REQT-3ekk7hre3k, REQT-m9r3k5b1ge only. WORK:report-sxyfvxcamh names LineId and the whitespace marker as post-work capture.
  - **Architecture records trail what was built** · decision `obvious-essential` · execution `agent-trivial`
    - _DiffViewer still reads as a placeholder, two engine records miss shipped features, and LineId and the class vocabulary have no record._
    - **Context**:
      ***Re-orienting***: post-work architecture capture for the component.
      ***Catch-up***:
      - Where: dioxus-diff-viewer.arch.jsonl, DiffViewer and two engine records.
      - Purpose: show how the crate's parts fit.
      - Situation: records predate the build; the worker left capture to post-work.
      - Goal: records match the delivered crate before the unit closes.
    - **Key points**:
      - ARCH-m4dkxzw6hh (DiffViewer) summary still says placeholder, and its supported requirements omit REQT-3928hx46s3 (Styling hooks).
      - ARCH-n7wmjnmt65 (LineDiffOptions) lists three compare methods; four ship, including trimmed line.
      - ARCH-kjjykjtt5r (PairedLineEntry) omits the whitespace change marker.
      - `LineId` and the public `styling_hooks` class vocabulary have no software_object.
      - Final-stage entry requires pending capture to land before commits are recorded.
    - **Resolution points**:
      - `capture-delivered-architecture`: rewrite DiffViewer's summary, activities, and responsibilities from the built component and add REQT-3928hx46s3 to its supported requirements; add trimmed line to LineDiffOptions and the whitespace change marker to PairedLineEntry; add software_object records for LineId and the styling hooks class vocabulary, drafted at resolution after F14 settles the vocabulary.
    - **Substance**: Maintainability and contract correctness: architecture records are what the theming and programmatic-control units read, and they currently describe a placeholder.
    - **Actionability**: Workable; record edits drafted from the delivered code, sequenced after F14.
    - **Specificity** `under-design`: The architecture is under-captured relative to the implementation; the component's structure is settled in code and missing from the records.
  - **resolution**: ARCH-m4dkxzw6hh (DiffViewer) now describes the delivered component: a real summary, a line-number click activity, responsibilities for highlighting and selection, consumer rendering, and styling hook classes, and REQT-3928hx46s3 (Styling hooks) among its supported requirements. ARCH-n7wmjnmt65 (LineDiffOptions) names trimmed line comparison and ARCH-kjjykjtt5r (PairedLineEntry) the whitespace change marker, each with its requirement. New records ARCH-mt182qa8vt (LineId) and ARCH-38ktaqm4xw (StylingHookClasses) sit under the DiffViewer. Dialogue in CONTENT-vk9jnzmfe1.
The Architect drafted the capture from the delivered code after F14 settled the class and line id vocabulary, leaving the renderer payload and line text wording to F3 since that finding may change them. The Code Whisperer noted StylingHookClasses follows the requirement's own term and left glossary coverage of it to F2. No genuine fork was found, and the stakeholder accepted the default; all edited and new records stay DREAMED.
- **finding** `AUDIT:finding-178bhw4m00` (F2) · type `coverage-gap` · severity `medium` · priority `high` · status `in-review` · reviewer _Code Whisperer_ · In the crate's public names at rust-dioxus/src/lib.rs:19-28 and styling_hooks.rs, against the project glossary .glossary.jsonl
  - Public names carrying project-specific terms (fold reset trigger, hidden lines, paired line entry, line side, surrounding-line count, line ending chip, whitespace chip, the dxdiff prefix) have no glossary coverage; the project glossary is still empty, so the Naming Stewardship acceptance gate is unmet.
  - **evidence**: Globbing *.glossary.jsonl from rust-dioxus/src outward finds only the root .glossary.jsonl, whose generated .glossary.md records no prohibitions, abbreviations, or terms at the capture-time check. rust-dioxus/src/lib.rs:19-28 exports FoldResetTrigger, HiddenLines, LineNumberClick, ModifierKeys, LineSide, PairedLineEntry, InlineChanges, LineEndingChange. The pre-work Code Whisperer sign-off notes naming decisions worth curating go to the project glossary.
  - **Crate's project-specific terms have no glossary coverage** · decision `judgement-needed` · execution `agent-trivial`
    - _Public names like FoldResetTrigger, PairedLineEntry and the dxdiff prefix carry project meanings, and the project glossary is still empty._
    - **Context**:
      ***Re-orienting***: glossary coverage for the crate's public names.
      ***Catch-up***:
      - Where: rust-dioxus/src/lib.rs:19-28 exports, against root .glossary.jsonl.
      - Purpose: one vocabulary across code, requirements, architecture.
      - Situation: the only glossary in scope holds no entries.
      - Goal: the Code Whisperer's acceptance gate is met.
    - **Key points**:
      - The Code Whisperer's acceptance gate rejects public names whose non-universal terms no glossary in scope covers.
      - Self-evident terms need no entry (universality rule); glossary maintenance admits only terms with project-specific meaning or real confusion.
      - Candidates with project-specific meaning: fold reset trigger, paired line entry, line ending chip, whitespace chip, inline-change token, the `dxdiff` prefix.
      - Likely self-evident: hidden lines, line side, surrounding-line count, modifier keys, line number click.
    - **Resolution points**:
      - `admit-project-specific-terms`: draft TERM entries for the project-specific candidates in the root `.glossary.jsonl`, one sentence each drawn from the requirement text, and record the self-evident set as needing none; the stakeholder confirms which side each term falls on before the write.
    - **Substance**: Maintainability: the vocabulary is already consistent across code, requirements and architecture; the gap is the gate's record, which later units (theming, programmatic control) extend.
    - **Actionability**: Workable; a handful of glossary records, split by the stakeholder's call on each term.
    - **Specificity** `not-design-related`: Not a design question about the plan; the pre-work Code Whisperer sign-off already named the project glossary as the home for naming decisions.

- **finding** `AUDIT:finding-g8hwac1585` (F3) · type `performance-concern` · severity `medium` · priority `med` · status `in-review` · reviewer _Rust Coder_ · In RowRendering.rendered_text (sends text through the consumer's content renderer) at rust-dioxus/src/diff_viewer_rows.rs:270-275, with the prop at rust-dioxus/src/diff_viewer.rs:72
  - The line content renderer takes an owned String, so every rendered line and every inline token allocates a copy on each render; under character comparison that is one allocation per character, the cost the engine's token ranges were designed to avoid. The payload is public contract.
  - **evidence**: diff_viewer_rows.rs:272 calls renderer.call(text.to_owned()); diff_viewer.rs:72 declares Option<Callback<String, Element>>; line_diff_output.rs:10-13 states tokens carry ranges so character comparison costs one allocation per line instead of one per character. ARCH-rs0yp4vc5q (LineContentRenderer) states the payload as a line's or token's text. At triage, dioxus-core 0.7.10 (the locked version, Cargo.lock) src/events.rs:483 bounds Callback's impl block, including call at :519, with Args: 'static, so a borrowed &str payload cannot pass; a 'static shared payload such as Rc<str> can. LineSide.text is a public String (line_diff_output.rs:33-40), so sharing a line's text without a per-render allocation needs the engine output to hold it shared.
  - **Content renderer copies every line and token per render** · decision `judgement-needed` · execution `agent-nontrivial`
    - _The line content renderer takes an owned String, so each render copies every line and token; per character under character comparison._
    - **Context**:
      ***Re-orienting***: the line content renderer's payload type.
      ***Catch-up***:
      - Where: diff_viewer_rows.rs:270-275, prop at diff_viewer.rs:72.
      - Purpose: lets an app render line text, such as a syntax highlighter.
      - Situation: each call receives text.to_owned().
      - Goal: renderer calls read text without copying it.
    - **Key points**:
      - The payload is public contract (ARCH-rs0yp4vc5q, LineContentRenderer), so its type is costly to change after release.
      - The engine's tokens are ranges precisely to avoid a copy per token; the renderer reintroduces it on every render.
      - Dioxus 0.7.10 `Callback` requires a `'static` argument, so `&str` is impossible; a shared `Rc<str>` is allowed.
      - Sharing without allocation means the engine's public `LineSide.text` holds `Rc<str>` instead of `String`.
    - **Resolution points**:
      - `shared-line-text-with-range` (preferred): `LineSide.text` becomes `Rc<str>`; the renderer receives a `LineContent { line: Rc<str>, range: Range<usize> }` exposing `text() -> &str`, so a call costs a reference-count bump; ARCH-rs0yp4vc5q and ARCH-kjjykjtt5r (PairedLineEntry) restate the payload.
      - `per-call-rc-str`: pass `Rc<str>` built per call. Demoted: still one allocation per line and per token, only moving the copy.
    - **Substance**: Contract correctness and maintainability: a public payload that forces a copy per token is locked in once apps implement renderers, and it defeats the engine's own zero-copy design.
    - **Actionability**: Workable; the Callback bound check shows a shared payload compiles, and the engine and component tests cover the text paths the change touches.
    - **Specificity** `under-design`: ARCH-rs0yp4vc5q specifies the payload as a line's or token's text, leaving its Rust type to the worker; the worker's choice copies.

- **finding** `AUDIT:finding-mmh37s2ny4` (F4) · type `maintainability-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Code Whisperer_ · In RowRendering.inline_rows, split_side, and inline_side (choose each shown line's state class and change marker) at rust-dioxus/src/diff_viewer_rows.rs:125-129, 333-338, 345-348
  - The mapping from a shown line's side and change kind to its state class and its `-` or `+` marker is written three times, beside a fourth change-kind mapping in styling_hooks.rs:53-60; a change to marker or state rules needs every copy edited alike.
  - **evidence**: diff_viewer_rows.rs:125-129 maps Removed to (DXDIFF__REMOVED, "-") and Added to (DXDIFF__ADDED, "+"); :333-338 maps the old side to removed "-" and new side to added "+"; :345-348 repeats the side mapping; styling_hooks.rs:53-60 maps change kind to row state. Tests in tests/diff_viewer/views.rs and styling_hooks.rs cover the current output of all three sites.
  - **Side-to-state-and-marker mapping written three times** · decision `advisory` · execution `agent-trivial`
    - _Three sites map a shown line's side and change to its state class and marker; a rule change needs each edited alike._
    - **Context**:
      ***Re-orienting***: state class and change marker per shown line.
      ***Catch-up***:
      - Where: diff_viewer_rows.rs:125-129, 333-338, 345-348.
      - Purpose: mark removed lines `-` and added lines `+` with matching classes.
      - Situation: inline, split, and inline-modified sites each map it.
      - Goal: one place decides a line's state and marker.
    - **Key points**:
      - Inline unmodified maps change kind; split maps side plus change; inline modified maps side alone.
      - A fourth mapping, `row_state` in styling_hooks.rs:53-60, maps change kind to row state and is a different decision.
      - Views and styling-hooks tests cover all three sites' current output, so a consolidation is checkable.
    - **Resolution points**:
      - `one-shown-line-state`: a single function taking whether the side has a line, which side it is, and the entry's change kind, returning state class and marker; the three sites call it.
    - **Substance**: Maintainability, low: three short mappings in one file, covered by tests; a Suggestion under the pragmatism filter.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Implementation craft within one module.

- **finding** `AUDIT:finding-v1fj7yf21t` (F5) · type `maintainability-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Dioxus Coder_ · In DiffViewer's row list and RowRendering.line_text (render rows and consumer-rendered tokens) at rust-dioxus/src/diff_viewer.rs:140-143 and rust-dioxus/src/diff_viewer_rows.rs:255-257
  - Rows are keyed by entry position and tokens by token position. Consumer renderers return Elements that may hold components with state, and when the texts change those positions shift, so that state can attach to the wrong line.
  - **evidence**: diff_viewer.rs:140 keys "entry-{position}" and :143 "fold-{fold.start}"; diff_viewer_rows.rs:257 keys tokens "{position}"; line_content_renderer and fold_row_renderer return Element (diff_viewer.rs:63, :72). dioxus-coder.SKILL.md Key Discipline flags index-as-key as state loss on insertion. No test renders a stateful consumer component; the engine output carries no stable per-line identity beyond line numbers.
  - **Rows and tokens are keyed by position** · decision `judgement-needed` · execution `agent-trivial`
    - _Row and token keys are positions, so when texts change, state inside consumer-rendered content can land on a different line._
    - **Context**:
      ***Re-orienting***: list keys for rows and inline tokens.
      ***Catch-up***:
      - Where: diff_viewer.rs:140-143, diff_viewer_rows.rs:255-257.
      - Purpose: keys let Dioxus keep component state with its item.
      - Situation: consumer renderers return Elements that may hold state.
      - Goal: an app knows what identity its rendered content keeps.
    - **Key points**:
      - Rows key as `entry-{position}` and `fold-{start}`; tokens as their position within the line.
      - The engine output carries no per-line identity beyond line numbers, and line numbers shift with edits above a line just as positions do.
      - No test renders a stateful consumer component, and no requirement says what identity rendered content keeps across text changes.
      - For content that holds no state, as in the reference's highlighter use, positional keys behave correctly.
    - **Resolution points**:
      - `record-positional-identity`: add an exclusions entry on ARCH-m4dkxzw6hh (DiffViewer) stating that content from the line content renderer and the fold row renderer keeps no identity across changes to the compared texts, so apps keep state outside it; triage found no stable line identity to key by.
    - **Substance**: Contract correctness, low impact: the risk applies only to stateful consumer content, and no better key exists in the engine output.
    - **Actionability**: No viable key change found by triage; the workable direction records the limit where renderer authors read.
    - **Specificity** `under-design`: The renderer records specify payloads and leave identity across text changes unstated; the gap is a missing boundary statement, not an implementation choice.

- **finding** `AUDIT:finding-kqaqnw6e4n` (F6) · type `performance-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Rust Coder_ · In RowRendering.is_highlighted (decides a line's highlight state) at rust-dioxus/src/diff_viewer_rows.rs:82-87
  - Highlight lookup scans the highlighted-lines Vec with contains, and runs for each row and again for each shown line, so render cost grows with rows times highlighted lines.
  - **evidence**: diff_viewer_rows.rs:86 calls self.highlighted_lines.contains; called at :92 (split row), :150 (inline row), and :164 (each shown line's cells). diff_viewer.rs:68 types the prop Vec<LineId>; LineId derives Hash and Ord (line_id.rs:11). No test or requirement bounds highlight list size.
  - **Highlight lookup scans the list for every row and line** · decision `advisory` · execution `agent-trivial`
    - _Each row and each shown line scans the highlighted-lines list, so render work grows with rows times highlighted lines._
    - **Context**:
      ***Re-orienting***: deciding whether a line is highlighted.
      ***Catch-up***:
      - Where: RowRendering.is_highlighted, diff_viewer_rows.rs:82-87.
      - Purpose: mark lines the app listed by id.
      - Situation: a linear scan, run per row and again per shown line.
      - Goal: highlight checks stay cheap as lists grow.
    - **Key points**:
      - Called from the split row (:92), the inline row (:150), and each shown line's cells (:164), so most lines are checked twice.
      - The prop is `Vec<LineId>`; `LineId` already derives `Hash` and `Ord`.
      - The reference's highlighted lines are typically a handful of lines, and no requirement bounds the list size.
    - **Resolution points**:
      - `check-once-per-shown-line` (preferred): compute each shown line's highlight once and derive the row's from its shown lines, halving the scans without touching the public prop.
      - `set-typed-highlight-prop`: type the prop as a set. Demoted: changes a public prop for a list size no requirement names.
    - **Substance**: Efficiency, low: quadratic only in the size of an app's highlight list, which is small in the reference's uses.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Implementation craft; the requirement fixes which lines highlight, not how the check runs.

- **finding** `AUDIT:finding-3atdnx5q34` (F7) · type `coverage-gap` · severity `low` · priority `low` · status `in-review` · reviewer _Rust Tester_ · In RowRendering.fold_row and the title row (column span arithmetic) at rust-dioxus/src/diff_viewer_rows.rs:290-297 and rust-dioxus/src/diff_viewer.rs:117-134
  - Column spans for fold rows and titles are computed from the view and the line-number switch, and no test covers an inline fold row or titles with line numbers hidden, so a span error there would pass the suite.
  - **evidence**: tests/diff_viewer/folding.rs fold tests all use the split view with line numbers shown; content_rendering.rs title tests show line numbers; row_readings in mounted_viewer.rs:149-155 joins cell texts and does not read colspan. rendered_tree.rs exposes attribute() (used at views.rs:124), so colspan is assertable.
  - **No test checks fold-row or title column spans** · decision `obvious-essential` · execution `agent-trivial`
    - _Span arithmetic for fold rows and titles varies by view and line-number switch, but no test asserts a colspan._
    - **Context**:
      ***Re-orienting***: column spans in fold rows and titles.
      ***Catch-up***:
      - Where: diff_viewer_rows.rs:290-297, diff_viewer.rs:117-134.
      - Purpose: keep fold rows and titles aligned with line columns.
      - Situation: spans change with view and hidden line numbers.
      - Goal: a span error fails a test.
    - **Key points**:
      - `row_readings` (mounted_viewer.rs:149-155) joins cell texts and ignores `colspan`.
      - Fold tests run split with numbers shown; an inline fold row renders in a view-change test but its span is unasserted.
      - Title tests all show line numbers.
      - `rendered_tree.rs` exposes `attribute()`, already used at views.rs:124, so spans are assertable.
    - **Resolution points**:
      - `span-assertions`: add tests asserting the content colspan of a fold row in the inline view and in the split view with line numbers hidden, and each title's colspan with line numbers hidden in both views, each naming REQT-1tdrfvay4q (Fold rows) or REQT-vbaqm4y5zk (Column titles).
    - **Substance**: Test coverage: the combinations where the arithmetic differs are the untested ones, so a misaligned row would pass the suite.
    - **Actionability**: Workable; the fixture already reads attributes.
    - **Specificity** `not-design-related`: The adopted test strategy asserts row structure; spans are row structure the tests do not yet read.

- **finding** `AUDIT:finding-b8jmv5ye6e` (F8) · type `traceability-gap` · severity `low` · priority `low` · status `in-review` · reviewer _REQM_ · In the Files section (implementation files for the requirements) of diff-viewer.reqts.jsonl
  - The requirements' file records name only the engine's modules; the component's modules (diff_viewer.rs, diff_viewer_rows.rs, fold_planning.rs, fold_reset_trigger.rs, consumer_callbacks.rs, styling_hooks.rs, line_id.rs) are absent.
  - **evidence**: diff-viewer.reqts.md Files lists lib.rs, line_diff_engine.rs, line_diff_options.rs, line_diff_output.rs only; ls rust-dioxus/src shows seven further modules implementing the views, folding, selection, content rendering, and styling hooks requirements.
  - **Requirements file records omit the component modules** · decision `obvious-essential` · execution `agent-trivial`
    - _The requirements' Files section lists only the engine's modules; the seven component modules are missing._
    - **Context**:
      ***Re-orienting***: file records in diff-viewer.reqts.jsonl.
      ***Catch-up***:
      - Where: the requirements' Files section.
      - Purpose: point readers from requirements to implementing files.
      - Situation: written during the engine unit, not extended since.
      - Goal: every implementing module is listed.
    - **Key points**:
      - Listed: lib.rs, line_diff_engine.rs, line_diff_options.rs, line_diff_output.rs.
      - Missing: diff_viewer.rs, diff_viewer_rows.rs, fold_planning.rs, fold_reset_trigger.rs, consumer_callbacks.rs, styling_hooks.rs, line_id.rs.
    - **Resolution points**:
      - `add-component-file-records`: add one file record per missing module, each with a one-line description of what it implements; if F16 renames files, the records use the new names.
    - **Substance**: One line per file; low impact on traceability, since source annotations already carry REQT ids.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Record completeness, not design.

- **finding** `AUDIT:finding-vsmp3xw7z2` (F9) · type `performance-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Dioxus Coder_ · In RowRendering.gutter (renders a line number cell) at rust-dioxus/src/diff_viewer_rows.rs:229-246
  - Every gutter cell registers a click listener, including gutters with no line number and viewers given no click handler, so the markup offers clicks that do nothing.
  - **evidence**: diff_viewer_rows.rs:236-240 attaches onclick unconditionally and checks for a handler and line id inside it; tests/diff_viewer/line_selection.rs:110-126 confirms a numberless gutter reports nothing. At triage, dioxus-rsx 0.7.10 src/attribute.rs:215-220 accepts an if-expression attribute value only when the attribute is not an event, so a conditional listener means a separate element branch. No requirement states whether unclickable gutters carry a listener.
  - **Every gutter listens for clicks, even unclickable ones** · decision `advisory` · execution `agent-trivial`
    - _Gutters register a click listener even with no line number or no handler, so the markup offers clicks that do nothing._
    - **Context**:
      ***Re-orienting***: click listeners on line number cells.
      ***Catch-up***:
      - Where: RowRendering.gutter, diff_viewer_rows.rs:229-246.
      - Purpose: report line-number clicks to the app's handler.
      - Situation: the listener is attached whether or not a click can report.
      - Goal: only clickable gutters listen.
    - **Key points**:
      - The handler and line id are checked inside the closure, so behavior is correct; the cost is one listener per gutter cell.
      - RSX takes an if-expression value only for non-event attributes (dioxus-rsx 0.7.10 attribute.rs:215-220), so skipping the listener needs a second `td` branch.
      - Web renderers delegate events at the root, so the per-listener cost is registration, not a DOM listener each.
      - No requirement asks for clickable affordance on gutters.
    - **Resolution points**:
      - `listen-only-when-clickable`: render the gutter `td` in two branches, with `onclick` only when a handler is supplied and the gutter has a line id; an existing line-selection test covers the numberless gutter.
    - **Substance**: Maintainability and efficiency, low: behavior is already correct, and the change trades a duplicated element branch for fewer listeners.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Implementation craft inside the gutter; no plan or requirement material is involved.

- **finding** `AUDIT:finding-y15z5b9eh5` (F10) · severity `low` · priority `low` · status `withdrawn` · reviewer _Rust Tester_ · In tests/diff_viewer/reference_cases.rs (reference cases carried over from test/react-diff-viewer-test.tsx)
  - Candidate: the reference-case table asked for by the adopted test strategy might carry only part of the reference suite.
  - **evidence**: test/react-diff-viewer-test.tsx:27, :36, :45 define the reference suite's only three cases; tests/diff_viewer/reference_cases.rs:28-62 ports all three, each named by its case, and none differs from its reference count.
  - **resolution**: Not a gap: every reference case is carried over.

- **finding** `AUDIT:finding-dp148f85v5` (F11) · severity `low` · priority `low` · status `withdrawn` · reviewer _Code Whisperer_ · In is_far_from_changes (decides whether an entry folds) at rust-dioxus/src/fold_planning.rs:76-89
  - Candidate: folding's binary search relies on changed positions being ascending, a property stated in a doc comment.
  - **evidence**: fold_planning.rs:77-80 uses partition_point on diff.changed_positions; line_diff_output.rs:143 documents them ascending; tests/line_diff_engine.rs:643-661 (changed_positions_index_the_entries_that_hold_a_change) asserts ordered multi-position vectors, and tests/diff_viewer/folding.rs exercises folds on both sides of a change.
  - **resolution**: Encoded: an engine test and a caller back the ordering, so it is a contract rather than a comment.

- **finding** `AUDIT:finding-r4qbzwh04h` (F12) · severity `low` · priority `low` · status `withdrawn` · reviewer _Architect_ · In the DiffViewer component body (indexes engine entries by planned position) at rust-dioxus/src/diff_viewer.rs:140-144
  - Candidate: the no-tracing decision leaves panic paths unobserved, since rows index diff.entries by position.
  - **evidence**: diff_viewer.rs:140 and :144 index diff.entries with positions produced by plan_rows over the same diff (fold_planning.rs:39-71), which yields only positions below entries.len(); the component performs no I/O and returns no errors (WORK:report-sxyfvxcamh decision on tracing).
  - **resolution**: Not a failure path: positions come from the same diff they index, so no runtime condition reaches a panic.

- **finding** `AUDIT:finding-xmsrxq1vy1` (F13) · severity `low` · priority `low` · status `withdrawn` · reviewer _Work Planner_ · In work_unit.plan_guidance of WORK-6ds59btb76
  - Candidate: plan guidance still says pre-work settles fold reset and the two renderers, which are settled.
  - **evidence**: The .workUnit.md Plan / Guidance keeps the pre-work sentence; WORK:report-dpc63wa9ns already records it as a low observation and that pre-work leaves consented plan text as written.
  - **resolution**: Already recorded; consented plan text stays as written.

- **finding** `AUDIT:finding-gstnn6y646` (F14) · type `coverage-gap` · severity `medium` · priority `high` · status `fixed` · reviewer _Architect_ · In the public class constants (styling_hooks.rs:10-15) and LineId's text parsing (line_id.rs:42-56), against REQT-3928hx46s3 (Styling hooks) and REQT-zaens35zqy (Highlighted lines)
  - Two public behaviors ship with no requirement driving them: the viewer-level classes dxdiff-viewer, dxdiff-split-view, and dxdiff-inline-view, which apps and themes will style against, and parsing a line id from text such as `L-20`.
  - **evidence**: REQT-3928hx46s3 names the title, row, gutter, change marker, content cell, inline-change token, line ending chip, line ending arrow, whitespace chip, and fold row, plus change or highlight state; styling_hooks.rs:10-15 adds the viewer element and a view state beyond that list, applied at diff_viewer.rs:113-124. REQT-zaens35zqy defines the id's text form for highlighting; line_id.rs:42-56 implements FromStr, tested at tests/diff_viewer/line_selection.rs:13-24, and no requirement states parsing.
  - **Two public behaviors ship without a requirement** · decision `judgement-needed` · execution `agent-trivial`
    - _Viewer-level classes and parsing a line id from text such as L-20 are public, but no requirement asks for either._
    - **Context**:
      ***Re-orienting***: public behavior beyond the in-scope requirements.
      ***Catch-up***:
      - Where: styling_hooks.rs:10-15 and line_id.rs:42-56.
      - Purpose: classes apps style against; typed line ids apps highlight by.
      - Situation: both shipped while REQT-3928hx46s3 and REQT-zaens35zqy name neither.
      - Goal: every public behavior traces to a requirement.
    - **Key points**:
      - `dxdiff-viewer`, `dxdiff-split-view`, `dxdiff-inline-view` sit on the table (diff_viewer.rs:113-124); REQT-3928hx46s3 (Styling hooks) lists row-level kinds only.
      - `LineId: FromStr` accepts `L-20` / `R-3` and rejects other text, with a test (line_selection.rs:13-24); REQT-zaens35zqy (Highlighted lines) defines the text form but not reading it back.
      - The reference takes highlight ids as strings, so an app holding reference-style ids needs a way in.
      - Once apps style against or parse these, removing them is a breaking change.
    - **Resolution points**:
      - `requirements-for-both` (preferred): amend REQT-3928hx46s3 to name the viewer element and its split or inline view state; add a child under Line selection requiring the viewer's line id to be readable from its text form, rejecting other text.
      - `drop-line-id-parsing`: keep the classes requirement amendment, and remove `FromStr` from the public API. Demoted: an app carrying reference-style string ids then has no supported conversion.
    - **Substance**: Requirements coverage and contract correctness: public API with no requirement behind it can change without any record noticing, and apps will depend on both.
    - **Actionability**: Workable; one statement amendment and one new child requirement, drafted at resolution.
    - **Specificity** `under-design`: The requirements specify the row-level class vocabulary and the id's text form, and leave the viewer-level classes and text parsing unstated although both shipped.
  - **resolution**: REQT-3928hx46s3 (Styling hooks) now names the viewer itself and its split or inline view class. New child REQT-jgkndzqkyz (Line ids from text) under Line selection requires a line id's text form to read back into a line id and other text to be reported as not a line id. `LineId`'s `FromStr` carries the new annotation, and the mixed display-and-parsing test is split into one display test and two parsing tests naming it. Dialogue in CONTENT-3rjpj2d0bn.
  - depends on `AUDIT:finding-nh5wz253yb`: Architecture capture names the same vocabulary
The Architect proposed requirements for both behaviors rather than dropping line id parsing, because an app holding reference-style string ids would otherwise have no supported conversion. The Rust Tester noted existing tests already covered both behaviors, so only their requirement names needed to follow; applying that, the display-and-parsing test was split so each test names one requirement. The stakeholder accepted the proposal as drafted, leaving the Styling hooks amendment at draft maturity and the new child at DREAMED.
- **finding** `AUDIT:finding-38bwxzd4y0` (F15) · type `performance-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Rust Coder_ · In shown_side and escaped_terminator (build each shown line's tokens and chip text) at rust-dioxus/src/diff_viewer_rows.rs:369-372 and :391-393
  - Each render allocates per shown line where borrowing would serve: inline tokens are collected into a new Vec before rendering, and each terminator chip builds a String that is then formatted again.
  - **evidence**: diff_viewer_rows.rs:369-372 collects old_inline_tokens or new_inline_tokens into Vec<(TokenKind, &str)> stored in ShownLine.tokens (:35), consumed only by iteration in line_text (:253-262); :391-393 returns terminator.escape_default().to_string(), used only inside "{escaped_terminator(terminator)}" at :204, where EscapeDefault's Display would format directly. Rust Coder post-work Lens 1 (Zero-Copy).
  - **Tokens and line ending chips allocate per render** · decision `advisory` · execution `agent-trivial`
    - _Inline tokens are collected into a new Vec per shown line, and each line ending chip builds a String only to format it again._
    - **Context**:
      ***Re-orienting***: per-render allocations while building shown lines.
      ***Catch-up***:
      - Where: shown_side and escaped_terminator, diff_viewer_rows.rs:369-372, 391-393.
      - Purpose: gather a line's tokens and chip text for rendering.
      - Situation: both allocate where the data is only iterated or formatted.
      - Goal: rendering reads the engine output in place.
    - **Key points**:
      - `ShownLine.tokens` holds a collected `Vec<(TokenKind, &str)>` read only by iteration in `line_text` (:253-262).
      - `escaped_terminator` returns `escape_default().to_string()`, used only inside a format string at :204; `EscapeDefault` formats directly.
      - One Vec per modified shown line and one String per chip, every render.
    - **Resolution points**:
      - `iterate-in-place`: `ShownLine` keeps the side whose tokens it shows and `line_text` iterates the entry's token iterator directly; `escaped_terminator` returns the `EscapeDefault` value for the format string.
    - **Substance**: Efficiency, low: small allocations per modified line and per chip; internal, no contract involved.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Implementation craft inside one module.

- **finding** `AUDIT:finding-2jd3nrt9a7` (F16) · type `maintainability-concern` · severity `low` · priority `low` · status `in-review` · reviewer _Rust Coder_ · In rust-dioxus/src/diff_viewer_rows.rs (primary type RowRendering) and rust-dioxus/tests/diff_viewer/mounted_viewer.rs (primary type MountedApp)
  - Two new files are named apart from the one type each exists for: diff_viewer_rows.rs holds RowRendering, and mounted_viewer.rs holds MountedApp.
  - **evidence**: diff_viewer_rows.rs:16 declares pub(crate) struct RowRendering, the file's only non-private type; tests/diff_viewer/mounted_viewer.rs:17 declares pub struct MountedApp, its only type. rust-coder.SKILL.md Filename to Primary Type Alignment calls a mismatch a naming smell resolved by renaming the file or the type. Rust Coder post-work Lens 5.
  - **Two new files are named apart from their one type** · decision `advisory` · execution `agent-trivial`
    - _diff_viewer_rows.rs holds RowRendering and mounted_viewer.rs holds MountedApp, a filename and primary type mismatch._
    - **Context**:
      ***Re-orienting***: filenames of two new modules.
      ***Catch-up***:
      - Where: rust-dioxus/src/diff_viewer_rows.rs, tests/diff_viewer/mounted_viewer.rs.
      - Purpose: row rendering, and the test fixture mounting an app.
      - Situation: each file's only type carries a different name.
      - Goal: a reader finds each type by its filename.
    - **Key points**:
      - Rust Coder's filename and primary type alignment rule calls this a naming smell, resolved by renaming the file or the type.
      - `MountedApp::new` takes any app function, so the type name is the accurate one there.
      - `RowRendering` is crate-private; its file name reads as a list of rows.
    - **Resolution points**:
      - `rename-files-to-types`: rename `diff_viewer_rows.rs` to `row_rendering.rs` and `mounted_viewer.rs` to `mounted_app.rs`, updating module declarations and the requirements file records from F8.
    - **Substance**: Maintainability, low: two filenames, no behavior or public contract.
    - **Actionability**: Workable.
    - **Specificity** `not-design-related`: Naming craft within the crate; no plan material involved.

- **finding** `AUDIT:finding-mhr3w1cq46` (F17) · severity `low` · priority `low` · status `withdrawn` · reviewer _REQM_ · Across rust-dioxus/src, against the in-scope requirements of diff-viewer.reqts.jsonl
  - Candidate: in-scope requirements implemented without traceability annotations, or annotations naming requirements that no longer exist.
  - **evidence**: grep for each in-scope child requirement id over rust-dioxus/src found at least one `REQT-<id> (Label)` annotation for every one; only the group anchors REQT-2k7j51afde, REQT-qerexp825r, REQT-p2gkf77kjj, REQT-3ekk7hre3k carry none, as groups state themes rather than behavior. Every REQT id in src resolved to a record in diff-viewer.reqts.jsonl at the capture-time check.
  - **resolution**: Traceability holds for every in-scope child requirement; the file-record gap stays with F8.

- **finding** `AUDIT:finding-nngvnj6kx9` (F18) · severity `low` · priority `low` · status `withdrawn` · reviewer _Rust Coder_ · Across rust-dioxus/src and rust-dioxus/tests
  - Candidate: lint suppressions, unsafe code, or blocking async futures in the changeset (Rust Coder post-work Lenses 2-4).
  - **evidence**: grep for `#[allow`, `unsafe`, and `async ` over rust-dioxus/src and rust-dioxus/tests returned no matches at the capture-time check.
  - **resolution**: Nothing for these lenses to evaluate.







## Reports


### 2026-09-15T20:17:00Z — Work Planner
<a id="work:report-e5srytp0k1"></a>
`WORK:report-e5srytp0k1`

**Commits**
- `ef948f4`: view, folding, selection, and content requirements drafted DREAMED for this unit's scope.
- `ad61492`: custom fold rows receive the first hidden line's numbers.

**Clarifications**
- The reference fold renderer names its arguments start line numbers but passes the line after a middle fold and the last hidden line of a trailing fold; the stakeholder chose the first hidden line, now in the Fold rows requirement.

**Observations**
- `src/index.tsx:507-515` drops a single trailing unchanged line with no fold row; the folding requirements deliberately do not carry that over, so do not port it from the reference; medium severity for anyone porting line by line.
- `src/index.tsx:169` hands the click handler the mouse event as well as the line id; the Line number clicks requirement names only the id, and what else a Dioxus handler receives is part of the renderer and handler design pre-work settles.

**For the next worker**
- The UI group loads in full per the Rust Roundup rule; Appshell Expert and Forms Expert have little bearing on a standalone component and are loaded for completeness, not for guidance to apply.


### 2026-09-16T16:31:00Z — Work Planner
<a id="work:report-dpc63wa9ns"></a>
`WORK:report-dpc63wa9ns`

**Commits**
- `7a851d1`: pre-work findings filed and triaged across the three reviewers.
- `838cea3`: fold state kept inside the crate, reset through an optional handle.
- `daf6675`: the reset handle named for its one job.
- `8d17c5b`: programmatic control spun out as its own backlog unit.
- `b3d58c8`: the three consumer callables' payloads recorded.
- `c7ad1c8`: the markup's styling hooks made a requirement.

**Decisions**
- An app never holds state or identities it cannot keep valid: fold state stays in the crate, and every consumer payload carries document coordinates or plain data, never fold ids or framework event types. The stakeholder set this rule; the reasoning is in the content notes on the fold-reset advice.
- Reading change and fold state, and steering folds from app code, is beyond the reference's surface and lives in WORK-98bxwcd28f (Programmatic control of the diff viewer), which depends on this unit. Nothing in this unit's remaining findings may widen what a consumer can read or steer.

**Observations**
- WORK-b86cyjcn7m (Diff viewer theming) is still an embedded stub in the port epic rather than a materialized file; materializing it needs its importance, urgency, and priority from the stakeholder. Low severity.
- The plan guidance still lists fold reset and the two renderers as pre-work's to settle; they are settled, and pre-work leaves consented plan text as written. Low severity.

**For the next worker**
- Terms minted into requirements and architecture get proposed to the stakeholder before they are written; an unproposed term cost two turns this session.
- A record's `__rev` is not stored in the file, so a grep cannot find it; only an unprojected toon read returns it, and a `fields` projection that asks for it is rejected.


### 2026-09-16T18:21:00Z — Work Planner
<a id="work:report-p0z9ja6z6a"></a>
`WORK:report-p0z9ja6z6a`

**Commits**
- `b05f8aa`: styling hooks given the `dxdiff` class prefix.
- `15f9517`: reference set completed; omissions home named as DiffViewer exclusions.
- `0ff601c`: requirement-terms naming rule carried into plan guidance.
- `49adff3`: test strategy covers styling hooks; assent to build recorded.
- Main worktree `5b63383`: empty project glossary and principles registries, kept out of the npm package.
- This entry's commit: pre-work sign-offs recorded and in-scope requirements moved to NEXT.

**Decisions**
- The stakeholder chose `dxdiff` as the class prefix rather than leaving it to the worker; the requirement moved to draft maturity for that steering.
- Project terms and principles, including naming decisions from this unit, belong in the root `.glossary.jsonl` and `.principles.jsonl`; both started empty, and entries earn a place only per the glossary-maintenance admission test.

**Observations**
- The generated root `.principles.md` links `./work-organizer/principles-maintenance.SKILL.md`, which resolves only inside the skills library; the template lives there, so the fix is a proposal to that repository. Low severity.

**For the next worker**
- Confirm the rendered-output harness first: at this session's check `dioxus-ssr` was absent from the local cargo registry and `rust-dioxus/Cargo.toml` had no dioxus dependency.
- Re-read a record just before proposing a change to it: another session committed to this unit mid-session, and a proposal built on an earlier read had to be withdrawn.
- A toon write with a mismatched `rev` returns the on-disk `__rev` in its error, which is the cheapest way to obtain one for a single-row update.


### 2026-09-16T20:10:00Z — Coder
<a id="work:report-dxvtmjvym2"></a>
`WORK:report-dxvtmjvym2`

**Commits**
- This entry's commit: the unit accepted into now, with the intention alignment's requirement and architecture changes.

**Clarifications**
- Expanded folds across input changes: the stakeholder chose automatic reset when the compared texts or surrounding-line count change; REQT-v748c7mjr6 (Expanding folds) now says so. Every other prop change leaves expansions alone.
- Line ids: a typed `LineId` enum (`Old(n)`, `New(n)`) whose text form stays `L-20` / `R-3`, as REQT-zaens35zqy (Highlighted lines) states; the id is never rendered, since gutters show the bare number as in the reference (`src/index.tsx:250`). The stakeholder accepted `L-20` for now.
- Compare methods: `CHARS`, `WORDS_WITH_SPACE`, `LINES` map to `Character`, `Word`, `Line`. The stakeholder asked for trimmed-line comparison and excluded `WORDS`, `SENTENCES`, `CSS`, recorded as an exclusion on ARCH-m4dkxzw6hh (DiffViewer).
- Trimmed-line comparison: whitespace-only edits still count as changed lines, as in the reference, where the compare method only shapes inline tokens within a modified pair (`src/compute-lines.ts:223-227`). A leading or trailing whitespace difference on a modified pair shows a `WS` chip on both sides, only under trimmed-line comparison, alongside any content change. New requirements REQT-z9r0pc53jg (Trimmed line comparison), REQT-hq1fzjaxg8 (Leading or trailing whitespace changes), REQT-27a1gxq15f (Whitespace chips); styling hooks and chip color amended to name the whitespace chip.

**Decisions**
- Trimmed-line comparison and whitespace change marking land in the engine inside this unit rather than reopening the done engine unit.
- The trimmed token keeps its full text; jsdiff renders the trimmed text and drops indentation, which the port does not carry over.
- No tracing instrumentation: the component has no I/O and no failure path, as with the engine.
- `LineId` and the whitespace change marker on `PairedLineEntry` enter the architecture when post-work captures what was built.

**Observations**
- Compare methods (REQT-rvcg21axa8) and Line changes (REQT-wqffp3d26e) groups still read IMPLEMENTED/NEEDS VERIFICATION while each now holds a NEXT child. Low severity.

**For the next worker**
- `dioxus-ssr` 0.7.9 is in `~/.cargo/registry/cache` as a `.crate`, not unpacked under `registry/src`, which is why earlier checks missed it; eidos/appshell uses it as a dev-dependency with `dioxus_ssr::render(&vdom)` after `rebuild_in_place` (`tests/breadcrumbs.rs:84`).
- Click simulation without a browser: `dioxus/packages/core/tests/event_propagation.rs:9-19` installs `SerializedHtmlEventConverter` and calls `runtime().handle_event("click", event, ElementId(..))` with `SerializedMouseData`, which carries modifier keys; listener element ids come from the `rebuild_to_vec` mutation stream.
- A local jsdiff source for reference-method semantics is at `~/.vscode/extensions/hbenl.vscode-mocha-test-adapter-2.14.4/node_modules/diff` (5.2.0; the reference pins `^4.0.1`).
- The work_unit `state` enum is backlog, ready, working, reviewing, done; the phase directory `now/` pairs with `working`.


### 2026-09-16T21:16:00Z — Coder
<a id="work:report-sxyfvxcamh"></a>
`WORK:report-sxyfvxcamh`

**Commits**
- `2034c44`: trimmed line comparison and whitespace change marking in the engine.
- `f6fef34`: the DiffViewer component, its tests, and the dioxus dependency.
- This entry's commit: the resetCodeBlocks() return-value exclusion on ARCH-m4dkxzw6hh (DiffViewer), and this report.

**Decisions**
- The texts, compare method, inline-changes switch, line offset, and surrounding-line count are `ReadSignal` props: the engine memo and the fold-reset rule track them without cloning the texts, and a changed value marks only its readers dirty.
- Expanded folds are tagged with the basis they were made under (an input generation plus the trigger's reset count); a stale basis reads as folded, so no signal is written during rendering.
- `FoldResetTrigger` holds a reset counter owned by the app's component, not the fold state, so one trigger can reset several viewers and the crate keeps every fold id.
- The styling vocabulary adds `dxdiff-viewer`, `dxdiff-split-view`, `dxdiff-inline-view`, `dxdiff-modified` (row state), and `dxdiff-empty` (the absent side of a split row) to the kinds the requirement names; the constants live in the public `styling_hooks` module.
- Whitespace change marking does not depend on inline changes being on, since REQT-hq1fzjaxg8 (Leading or trailing whitespace changes) states no such condition.

**Observations**
- REQT-qcnxhemvhn (Folded unchanged lines) says a negative surrounding-line count counts as 0; the prop is `usize`, so a negative count is unrepresentable rather than treated. Needs stakeholder steering on the requirement text or the prop type. Medium severity.
- REQT-4zyjfjrhd3 (Line ending chips): the inline view shows an unchanged pair on one row, so that row carries both sides' terminator chips, old then new. A reading of the requirement, awaiting steering. Low severity.
- REQT-3928hx46s3 (Styling hooks) asks every listed element for a change or highlight state class; the title has neither state, so it carries only `dxdiff-title`. A reading, awaiting steering. Low severity.
- A trailing whitespace edit on a text's last line never shows, because REQT-9tze98pt6g (Trailing whitespace) trims each text's end before comparison. Existing behavior, not introduced here. Low severity.

**For the reviewer**
- Component tests live in `rust-dioxus/tests/diff_viewer/`, one file per requirement group, each test naming its REQT; engine additions are at the end of the compare-methods and line-changes sections of `tests/line_diff_engine.rs`.
- The first acceptance criterion is `views::two_texts_alone_show_old_lines_left_and_new_lines_right`; the third is exercised by `tests/diff_viewer/styling_hooks.rs`, which asserts classes rather than element structure.
- README props map to props as: splitView to `view`, disableWordDiff to `mark_inline_changes`, compareMethod to `compare`, hideLineNumbers to `show_line_numbers`, renderContent to `line_content_renderer`, onLineNumberClick to `on_line_number_click`, highlightLines to `highlighted_lines`, showDiffOnly to `fold_unchanged_lines`, extraLinesSurroundingDiff to `surrounding_line_count`, codeFoldMessageRenderer to `fold_row_renderer`, leftTitle and rightTitle to `left_title` and `right_title`, linesOffset to `line_offset`, and resetCodeBlocks() to `FoldResetTrigger::reset`.

**For the next worker**
- `dioxus_ssr` output carries no element ids, so clicks cannot be dispatched from it; `tests/diff_viewer/rendered_tree.rs` applies mutations the way Dioxus's native-dom writer does (`~/dev/dioxus/packages/native-dom/src/mutation_writer.rs`) and exposes elements, texts, and listener ids.
- A `ReadSignal` prop change marks memos dirty during the parent's render, which queues another render one `process_events` plus `render_immediate` pass does not reach; the fixture settles until two rounds apply no mutations.
- RSX merges repeated `class:` attributes only as format strings (`class: "{CONST}"`), and `"<{x}>"` fails to parse as a format string in RSX text.
- The dev-dependency `dioxus-html` with feature `serialize` supplies `SerializedMouseData` and `SerializedHtmlEventConverter`; the `dioxus` lib feature leaves it off.


### 2026-09-16T21:50:00Z — Coder
<a id="work:report-cgnyah17b8"></a>
`WORK:report-cgnyah17b8`

**Commits**
- `ba018e2`: signal props take plain defaults.
- `ac7d916`: an arrow between inline line ending chips.
- `1736acc`: titles marked as carrying no state class.
- `.project` `8c2f22d`, `63e0ed7`, and this entry's commit with `51ed3bc`: the requirement amendments below, and this report.

**Clarifications**
- Surrounding-line count: the stakeholder kept the prop non-negative by type, and REQT-qcnxhemvhn (Folded unchanged lines) no longer mentions negative counts.
- Inline line ending chips: the stakeholder asked for an arrow; an inline unchanged line reads old chip, `→`, new chip, now stated in REQT-4zyjfjrhd3 (Line ending chips). The arrow carries `dxdiff-line-ending-arrow`, named among the kinds in REQT-3928hx46s3 (Styling hooks), and `aria-label="changed to"`.
- Title state class: the stakeholder kept the element list and added "if relevant" to REQT-3928hx46s3; a comment at the title row records that no change or highlight state applies.

**Observations**
- The three readings listed as awaiting steering in `WORK:report-sxyfvxcamh` are settled as above; its fourth observation (last-line trailing whitespace) stands. Low severity.

**For the reviewer**
- The arrow is covered by `views::an_inline_unchanged_line_shows_its_old_chip_an_arrow_and_its_new_chip` and `styling_hooks::the_inline_line_ending_arrow_carries_its_kind_and_state`.


### 2026-09-16T21:53:00Z — Work Planner
<a id="work:report-v31y05ctbp"></a>
`WORK:report-v31y05ctbp`

**Commits**
- This entry's commit: post-work independent analysis filed, sign-offs moved to evaluating, and this checkpoint report.

**Observations**
- The pre-work test-strategy advice WORK:advice-kxmvd4g6zg (Test strategy for the component) is still `in-review`; the delivered tests follow it, so post-work seals it when the Rust Tester signs off. Low severity.

**For the next worker**
- Resume at post-work Phase 2: the nine `pending` findings under AUDIT:session-99pe3an9sj need triage (presentation, finding_type, move to in-review) before the orientation and the first finding; the four withdrawn candidates go into the octopus ESUM of self-resolved candidates.
- F3 (content renderer allocates an owned String per token) is unverified on one point: whether a Dioxus 0.7 `Callback` accepts a shared input such as `Rc<str>`; read the dioxus-core Callback source before triaging it.
- Rust Observability, Logging, Dioxus UI Patterns, Appshell Expert, and Forms Expert were not loaded for this reading-only pass; load the whole Rust Roundup and UI group before any remediation writes Rust.
- `cargo clippy --all-targets` on `rust-dioxus/` reported nothing at this check, so a new warning after a fix comes from the fix.






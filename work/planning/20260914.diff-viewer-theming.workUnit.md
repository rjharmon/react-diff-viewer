
# Diff viewer theming

`WORK-b86cyjcn7m` · created 2026-09-14 · slug `diff-viewer-theming`
state `backlog` · maturity `draft` · kind `preplanned` · source `ad-hoc` · discipline `coding` · variety `greenfield`
importance `high` · urgency `low` · priority `med` · epic `EPIC-77znwqj94w`

> **JSONL is authoritative.** This document is auto-generated from `WORK-b86cyjcn7m`'s `.workUnit.jsonl`. Do not hand-edit; changes will be overwritten on the next write.



## Status Summary

Materialized from the port epic with its style-override design settled and recorded. Next is pre-work review: the Architect and Rust Tester sign-offs, then worker acceptance.

## Problem / Context

- The viewer renders its `dxdiff` classes and ships no colors at all, so a mounted viewer is an unstyled table.
- The reference carries its light and dark palettes in emotion style objects a consumer replaces per instance, and Rust has no counterpart to that shape.
- What a consumer may override, and how their own rules reach the viewer, is the contract this unit settles in CSS rather than in a Rust API.

## Plan / Guidance

- One stylesheet the crate embeds and the component renders into the document, inside a cascade layer, so an app's own rules take effect at any specificity.
- Palette defaults are declared at the document root rather than on the viewer element: a value declared on the element would beat any assignment an app makes on an ancestor.
- Overriding one mounted viewer is ordinary CSS against the `dxdiff` classes and the `--dxdiff-` names; the crate adds no Rust type for it.
- Colors come from the reference palettes; what else the sheet carries is what the viewer needs to read as the reference does.
- The design is settled and recorded in the architecture this unit references; hold it as a constraint rather than a starting point.



## Required Skills

- [`coder/coder.SKILL.md`](../coder/coder.SKILL.md) — roles: `implementer`

- [`rust-coder/rust-coder.SKILL.md`](../rust-coder/rust-coder.SKILL.md) — roles: `implementer`

- [`dioxus-coder/dioxus-coder.SKILL.md`](../dioxus-coder/dioxus-coder.SKILL.md) — roles: `implementer`

- [`dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md`](../dioxus-ui-patterns/dioxus-ui-patterns.SKILL.md) — roles: `implementer`

- [`appshell-expert/appshell-expert.css-theming.SKILL.md`](../appshell-expert/appshell-expert.css-theming.SKILL.md) — roles: `implementer`

- [`architect/architect.SKILL.md`](../architect/architect.SKILL.md) — roles: `prework-advice`, `reviewer`

- [`rust-tester/rust-tester.SKILL.md`](../rust-tester/rust-tester.SKILL.md) — roles: `domain-tester`, `prework-advice`

- [`code-whisperer/code-whisperer.SKILL.md`](../code-whisperer/code-whisperer.SKILL.md) — roles: `postwork-audit`




## References

**ARCH**
- `ARCH:dcisn-pgydgmajhx` — the layered-stylesheet decision _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-d87pprx5vn` — the palette properties _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-7kwnstr5rt` — the theme choice _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-38ktaqm4xw` — the dxdiff class vocabulary _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`

- `ARCH-m4dkxzw6hh` — the component being themed _(role: context)_ · `dioxus-diff-viewer.arch.jsonl`


**REQT**
- `REQT-78dg0g6a2h` — theme palettes _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-q356bvvv15` — style delivery _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-kcm5ba45sp` — palette override _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-1accrb4jhp` — named colors _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-ey9f1s27r1` — yielding rules _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-sc8expw3q8` — theme selection _(role: scope)_ · `diff-viewer.reqts.jsonl`

- `REQT-8y5fp2aarz` — chip color _(role: scope)_ · `diff-viewer.reqts.jsonl`


**WORKUNIT**
- `WORK-6ds59btb76` — diff viewer component _(role: dependency)_ · `20260915.diff-viewer-component.workUnit.jsonl`


**OTHER**
- `EPIC-77znwqj94w` — port epic _(role: context)_ · `rust-dioxus-port.workEpic.jsonl`





## Focus Files

- `rust-dioxus/src/dxdiff.css` — The stylesheet this unit writes: the palette properties, the rules reading them, and the cascade layer holding both. Does not exist yet.

- `rust-dioxus/src/diff_viewer.rs` — The component that renders the stylesheet into the document and writes the chosen theme onto the viewer element.

- `rust-dioxus/src/styling_hooks.rs` — The `dxdiff` class names the stylesheet's rules target; the delivered vocabulary this unit styles against rather than extends.

- `rust-dioxus/src/lib.rs` — The crate's public names, which this unit adds the theme choice to.

- `src/styles.ts` — The reference's two palettes and the rules that read them: the colors this unit carries, and what the reference styles beyond color.

- `rust-dioxus/tests/diff_viewer/main.rs` — The component suite's module list, one file per requirement group, where a theming test file is declared.






## Activities


### Carry the reference palettes
<a id="work:activity-7128v321cm"></a>
`WORK:activity-7128v321cm`

Turn each color the `react-diff-viewer` package uses into a `dxdiff`-prefixed custom property, declaring the light values as the defaults at the document root and the dark values under the reader's color-scheme preference. Every color the reference names is carried, including the ones no current element reads yet, and each property is named for what it colors rather than for the reference's own variable spelling.






### Style the viewer from those properties
<a id="work:activity-t1634170zm"></a>
`WORK:activity-t1634170zm`

Write the rules that style every element the viewer renders, reading color from the custom properties and nothing else, and carrying whatever layout, spacing, and text handling the viewer needs to read as the reference does. The whole sheet sits in the crate's cascade layer. Chips are styled to stay legible on each line background they appear on.






### Deliver the stylesheet
<a id="work:activity-a0shjq117z"></a>
`WORK:activity-a0shjq117z`

Render the embedded stylesheet into the document from the component, so an app that mounts a viewer adds nothing of its own. Covers what happens when more than one viewer is mounted.






### Add the theme choice
<a id="work:activity-x2c99qzcx4"></a>
`WORK:activity-x2c99qzcx4`

Give the component a prop choosing light, dark, or the reader's preference, and write the chosen theme onto the viewer element so two mounted viewers may differ. The default follows the reader.






### Cover the theming requirements with tests
<a id="work:activity-214gpr071h"></a>
`WORK:activity-214gpr071h`

Deliver tests showing each in-scope requirement met, including the ones no assertion over the rendered tree can reach, since the stylesheet leaves no node behind in the viewer's own markup. Where a requirement cannot be reached by any available harness, say so in the report rather than leaving the gap silent.








## Acceptance Criteria

- [ ] A viewer mounted in an app that adds no stylesheet of its own reads as the `react-diff-viewer` package does, in both light and dark. · maturity `DREAMED`

- [ ] An app restyles the viewer, app-wide or one mounted viewer at a time, with its own CSS alone. · maturity `DREAMED`

- [ ] The crate's public names gain the theme choice and nothing else. · maturity `DREAMED`




## Sign-Offs

### Pre-Work

- **Architect** — state `pending`

- **Rust Tester** — state `pending`



### Post-Work

- **Architect** — state `pending`

- **Code Whisperer** — state `pending`

- **Rust Tester** — state `pending`







## Reports


### 2026-09-17T19:50:00Z — Work Planner
<a id="work:report-qeyc77q9kp"></a>
`WORK:report-qeyc77q9kp`

**Commits**
- `.project` `7ca66e6`: the theme requirements and the architecture that settles how they are met.
- This entry's commit: this unit materialized out of the port epic, with its plan, scope, and sign-offs.

**Clarifications**
- The Rust style-override design: the stakeholder chose CSS cascade layers, with per-viewer overriding done as ordinary CSS against local `--dxdiff-` names rather than a Rust palette type. No typed theme struct and no inline style plumbing.
- Whether the stylesheet carries layout as well as color settled inside the requirements rather than beside them: `REQT-q356bvvv15` (Style delivery) says an app adds no stylesheet of its own, which holds only if the sheet carries the viewer's layout too.

**Decisions**
- Palette defaults are declared at the document root rather than on the viewer element. A custom property declared on an element beats any value inherited from an ancestor, so declaring on `.dxdiff-viewer` would have defeated an app wrapper's assignment, which is the per-instance path the stakeholder asked for.
- The Code Whisperer is seated for post-work only: one CSS file, one prop, and one render call give pre-work no structure to shape, and post-work still audits what gets built.
- The chunk stays a single work unit; its one risk signal, architectural uncertainty over the override design, cleared when that design settled.

**Observations**
- `document::Style` deduplicates only when `href` is set (`dioxus-document-0.7.10/src/elements/style.rs:129-133`), so an inline-contents style inserts once per mounting component and two mounted viewers put two identical style elements in the head. Harmless as CSS, and the worker's call. Low severity.
- Requirement 3.2.1 (Theme selection) now reads before 3.2.4 (Theme palettes), because appending avoided a consent-gated renumbering of three delivered siblings. Low severity.

**For the reviewer**
- The three acceptance criteria are deliberately not restatements of the seven in-scope requirements: the first checks the whole against the reference, the second checks the override contract end to end, and the third is a negative criterion guarding against a Rust API growing where CSS was chosen.
- The Work Planner's requirements-clarity gate is already satisfied: every in-scope requirement was written this session against the recorded architecture, and area 3.2 now covers the colors, their delivery, their selection, and their override.

**For the next worker**
- `document::Style` is reachable with the crate's current `dioxus = { default-features = false, features = ["lib"] }` line, because the `lib` feature enables `document`. No manifest change is forced by the delivery choice.
- That component returns an empty node, so the stylesheet leaves nothing in the viewer's rendered tree and `tests/diff_viewer/rendered_tree.rs` cannot see it. The test activity names this rather than assuming a harness exists.
- The viewer's root element is `table.dxdiff-viewer` (`rust-dioxus/src/diff_viewer.rs:131`), which is where a theme attribute lands.
- The reference's palettes are at `src/styles.ts:90-113` (light) and `src/styles.ts:116-140` (dark), 24 names each; the rest of that file is emotion rule construction the port does not carry over.
- This project has no weaver tooling: no `.weave` file, no `.weaver/` submodule. `.project/` is a plain worktree on branch `project`, so each worktree commits separately and nothing needs weaving.






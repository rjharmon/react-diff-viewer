# Shared Principles

> **GENERATED: DO NOT MODIFY**. This file is generated from `.principles.jsonl` by the toon tools via `work-organizer/principles.md.ejs`. Direct edits will be overwritten on the next generation. To add, edit, or retire a principle, load [`work-organizer/principles-maintenance.SKILL.md`](./work-organizer/principles-maintenance.SKILL.md) first, then edit `.principles.jsonl`.

A registry of cross-cutting work-authoring principles. A principle's body lives in one of three places: inline in the companion `.principles.jsonl` file, in the SKILL file named by a `(load from ...)` pointer after the entry's name, or nowhere at all when the hook itself is the complete protocol. The first two forms are JIT-loaded on hook match; the third is already in front of you.

**How to use this index**: when you encounter a situation matching a hook below, load the matching body before proceeding. For an inline-body entry, **toon read the entry from the companion `.principles.jsonl`**. When a `(load from ...)` pointer is shown, **load the SKILL file** it names. When the entry says the hook is self-loaded, there is nothing further to load. The hook only flags applicability; the substantive guidance is in the body.

```
toon read .principles.jsonl --filter '{"path":"name","value":"<principle-name>"}'
```

## From me to an agent

> I created this to establish shared principles, essential process and create genuine connection with you because I care about the experience we are creating for each other.

## Index


